#Requires -Version 5.1
<#
.SYNOPSIS
    BugFreeUmbrella module loader — 357+ proxy functions generated by tools/Build-Module.ps1.

.DESCRIPTION
    Each exported function is a thin wrapper that invokes the corresponding
    script under scripts/ via call operator (&) with ShouldProcess passthrough.
    Do not edit by hand — regenerate with: pwsh -File tools/Build-Module.ps1

.NOTES
    File Name  : BugFreeUmbrella.psm1
    Author     : Carme99
    Prerequisite: PowerShell 7.0+ recommended (5.1 minimum for import)
    Version    : 5.0.0
#>
Set-StrictMode -Version Latest
$ErrorActionPreference = 'Stop'

# Resolve scripts root via parent traversal (avoids direct scripts path literal for hygiene)
$script:ModuleRoot = $PSScriptRoot
$script:RepoRootResolved = (Resolve-Path (Join-Path (Join-Path $script:ModuleRoot '..') '..' -ErrorAction SilentlyContinue) -ErrorAction SilentlyContinue)
if (-not $script:RepoRootResolved) {
    $script:RepoRootResolved = (Get-Item $script:ModuleRoot).Parent.Parent
    if (-not $script:RepoRootResolved) { $script:RepoRootResolved = $script:ModuleRoot }
}
else { $script:RepoRootResolved = $script:RepoRootResolved.Path }
$script:ScriptsRoot = Join-Path $script:RepoRootResolved 'scripts'

if ($PSVersionTable.PSVersion.Major -lt 7) {
    Write-Warning 'BugFreeUmbrella is tested on PowerShell 7+. Some scripts may not work on Windows PowerShell 5.1.'
}

# Dot-source Public and Private helpers (CLI v2) — required for Get-BUScript / Invoke-BUScript
$publicPath = Join-Path $PSScriptRoot 'Public'
if (Test-Path -LiteralPath $publicPath) {
    Get-ChildItem -Path $publicPath -Filter '*.ps1' -ErrorAction SilentlyContinue | ForEach-Object {
        . $_.FullName
    }
}
$privatePath = Join-Path $PSScriptRoot 'Private'
if (Test-Path -LiteralPath $privatePath) {
    Get-ChildItem -Path $privatePath -Filter '*.ps1' -ErrorAction SilentlyContinue | ForEach-Object {
        . $_.FullName
    }
}

# Fallback catalog helper if Public not loaded
if (-not (Get-Command Get-BUCatalog -ErrorAction SilentlyContinue)) {
    $script:BUCatalogCache = $null
    $script:BUCatalogMtime = $null
    $script:BUCatalogPath = $null
    function Get-BUCatalog {
        <#
        .SYNOPSIS
            Gets the catalog with mtime cache.
        #>
        [CmdletBinding()]
        param()
        $candidates = @()
        if ($env:BU_CATALOG_PATH -and -not [string]::IsNullOrWhiteSpace($env:BU_CATALOG_PATH)) { $candidates += $env:BU_CATALOG_PATH }
        $probe = $PSScriptRoot
        if ([string]::IsNullOrWhiteSpace($probe)) { $probe = (Get-Location).Path }
        $walk = $probe
        for ($i = 0; $i -lt 6; $i++) {
            $candidates += (Join-Path $walk 'scripts/.catalog/metadata.json')
            $parent = Split-Path -Parent $walk
            if ([string]::IsNullOrWhiteSpace($parent) -or $parent -eq $walk) { break }
            $walk = $parent
        }
        $catalogPath = $null
        foreach ($c in $candidates) {
            try { $resolved = [System.IO.Path]::GetFullPath($c) } catch { $resolved = $c }
            if (Test-Path -LiteralPath $resolved) { $catalogPath = $resolved; break }
        }
        if (-not $catalogPath) { throw "Catalog not found" }
        $raw = Get-Content -LiteralPath $catalogPath -Raw -ErrorAction Stop
        $catalog = $raw | ConvertFrom-Json -ErrorAction Stop
        return $catalog
    }
}

#region Generated Functions — DO NOT EDIT BETWEEN MARKERS
# BEGIN GENERATED — catalog wrappers

function Add-LenovoFriendlyModelNames {
    <#
    .SYNOPSIS
        Adds Lenovo friendly model names to Intune device Notes and Entra ID device extension attributes.
    .DESCRIPTION
        Proxies to scripts/endpoints/intune/maintenance/Add-LenovoFriendlyModelNames.ps1.
        Original category: endpoints/intune/maintenance
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Add-LenovoFriendlyModelNames -Verbose
    .NOTES
        Backing script: scripts/endpoints/intune/maintenance/Add-LenovoFriendlyModelNames.ps1
        Category: endpoints/intune/maintenance
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/intune/maintenance/Add-LenovoFriendlyModelNames.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [switch]$AuditOnly,
        [Parameter()]
        [switch]$FailIfMissingMappings,
        [Parameter()]
        [switch]$UpdateNotes,
        [Parameter()]
        [switch]$UpdateExtensionAttributes,
        [Parameter()]
        [String]$ExtensionAttributeName,
        [Parameter()]
        [String]$NotesPrefix,
        [Parameter()]
        [String]$NotesSeparator,
        [Parameter()]
        [switch]$VerboseOutput,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/intune/maintenance/Add-LenovoFriendlyModelNames.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/intune/maintenance/Add-LenovoFriendlyModelNames.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Backup-GroupPolicies {
    <#
    .SYNOPSIS
        Backs up all Group Policy Objects in the domain to a specified location.
    .DESCRIPTION
        Proxies to scripts/infrastructure/windows/group-policy/Backup-GroupPolicies.ps1.
        Original category: infrastructure/windows/group-policy
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Backup-GroupPolicies -Verbose
    .NOTES
        Backing script: scripts/infrastructure/windows/group-policy/Backup-GroupPolicies.ps1
        Category: infrastructure/windows/group-policy
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/infrastructure/windows/group-policy/Backup-GroupPolicies.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(Mandatory)]
        [String]$BackupPath,
        [Parameter()]
        [String]$Comment,
        [Parameter()]
        [Int32]$RetentionDays,
        [Parameter()]
        [switch]$CompressBackup,
        [Parameter()]
        [String]$GPOName,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'infrastructure/windows/group-policy/Backup-GroupPolicies.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/infrastructure/windows/group-policy/Backup-GroupPolicies.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Backup-IISConfiguration {
    <#
    .SYNOPSIS
        Backup and restore IIS configuration.
    .DESCRIPTION
        Proxies to scripts/infrastructure/web/iis/Backup-IISConfiguration.ps1.
        Original category: infrastructure/web/iis
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Backup-IISConfiguration -Verbose
    .NOTES
        Backing script: scripts/infrastructure/web/iis/Backup-IISConfiguration.ps1
        Category: infrastructure/web/iis
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/infrastructure/web/iis/Backup-IISConfiguration.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [String]$BackupPath,
        [Parameter()]
        [String]$BackupName,
        [Parameter()]
        [switch]$IncludeCertificates,
        [Parameter()]
        [switch]$IncludeContentFiles,
        [Parameter()]
        [switch]$Restore,
        [Parameter()]
        [String]$RestoreFrom,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'infrastructure/web/iis/Backup-IISConfiguration.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/infrastructure/web/iis/Backup-IISConfiguration.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Block-AMDDriver {
    <#
    .SYNOPSIS
        Block the AMD Radeon driver via DeviceInstallation policy
    .DESCRIPTION
        Proxies to scripts/endpoints/devices/drivers/BlockAMDDriver.ps1.
        Original category: endpoints/devices/drivers
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Block-AMDDriver -Verbose
    .NOTES
        Backing script: scripts/endpoints/devices/drivers/BlockAMDDriver.ps1
        Category: endpoints/devices/drivers
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/devices/drivers/BlockAMDDriver.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/devices/drivers/BlockAMDDriver.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/devices/drivers/BlockAMDDriver.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Compare-ConfigurationDrift {
    <#
    .SYNOPSIS
        Detects configuration drift in Intune policies and settings.
    .DESCRIPTION
        Proxies to scripts/endpoints/intune/maintenance/Compare-ConfigurationDrift.ps1.
        Original category: endpoints/intune/maintenance
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Compare-ConfigurationDrift -Verbose
    .NOTES
        Backing script: scripts/endpoints/intune/maintenance/Compare-ConfigurationDrift.ps1
        Category: endpoints/intune/maintenance
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/intune/maintenance/Compare-ConfigurationDrift.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/intune/maintenance/Compare-ConfigurationDrift.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/intune/maintenance/Compare-ConfigurationDrift.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Export-IntuneConfiguration {
    <#
    .SYNOPSIS
        Exports Intune configuration for backup or migration.
    .DESCRIPTION
        Proxies to scripts/endpoints/intune/maintenance/Export-IntuneConfiguration.ps1.
        Original category: endpoints/intune/maintenance
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Export-IntuneConfiguration -Verbose
    .NOTES
        Backing script: scripts/endpoints/intune/maintenance/Export-IntuneConfiguration.ps1
        Category: endpoints/intune/maintenance
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/intune/maintenance/Export-IntuneConfiguration.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [String]$OutputPath,
        [Parameter()]
        [String[]]$ConfigTypes,
        [Parameter()]
        [switch]$IncludeAssignments,
        [Parameter()]
        [switch]$CompressOutput,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/intune/maintenance/Export-IntuneConfiguration.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/intune/maintenance/Export-IntuneConfiguration.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Export-WingetPackageList {
    <#
    .SYNOPSIS
        Exports installed winget packages from Intune-managed devices.
    .DESCRIPTION
        Proxies to scripts/endpoints/intune/deployment/Export-WingetPackageList.ps1.
        Original category: endpoints/intune/deployment
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Export-WingetPackageList -Verbose
    .NOTES
        Backing script: scripts/endpoints/intune/deployment/Export-WingetPackageList.ps1
        Category: endpoints/intune/deployment
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/intune/deployment/Export-WingetPackageList.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [String]$ExportFormat,
        [Parameter()]
        [String]$OutputPath,
        [Parameter()]
        [String]$SourceFilter,
        [Parameter()]
        [switch]$IncludeSystemApps,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/intune/deployment/Export-WingetPackageList.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/intune/deployment/Export-WingetPackageList.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Find-GPOConflicts {
    <#
    .SYNOPSIS
        Identifies potential Group Policy conflicts and issues.
    .DESCRIPTION
        Proxies to scripts/infrastructure/windows/group-policy/Find-GPOConflicts.ps1.
        Original category: infrastructure/windows/group-policy
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Find-GPOConflicts -Verbose
    .NOTES
        Backing script: scripts/infrastructure/windows/group-policy/Find-GPOConflicts.ps1
        Category: infrastructure/windows/group-policy
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/infrastructure/windows/group-policy/Find-GPOConflicts.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [String]$Scope,
        [Parameter()]
        [String]$TargetOU,
        [Parameter()]
        [String]$OutputPath,
        [Parameter()]
        [switch]$IncludeInheritance,
        [Parameter()]
        [switch]$CheckDuplicateSettings,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'infrastructure/windows/group-policy/Find-GPOConflicts.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/infrastructure/windows/group-policy/Find-GPOConflicts.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Find-InactiveADComputers {
    <#
    .SYNOPSIS
        Identifies and reports inactive computer accounts in Active Directory.
    .DESCRIPTION
        Proxies to scripts/infrastructure/windows/active-directory/Find-InactiveADComputers.ps1.
        Original category: infrastructure/windows/active-directory
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Find-InactiveADComputers -Verbose
    .NOTES
        Backing script: scripts/infrastructure/windows/active-directory/Find-InactiveADComputers.ps1
        Category: infrastructure/windows/active-directory
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/infrastructure/windows/active-directory/Find-InactiveADComputers.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [Int32]$InactiveDays,
        [Parameter()]
        [String]$SearchBase,
        [Parameter()]
        [switch]$IncludeDisabled,
        [Parameter()]
        [String]$OperatingSystem,
        [Parameter()]
        [switch]$DisableInactive,
        [Parameter()]
        [switch]$ExportHTML,
        [Parameter()]
        [switch]$ExportCSV,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'infrastructure/windows/active-directory/Find-InactiveADComputers.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/infrastructure/windows/active-directory/Find-InactiveADComputers.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Find-PolicyConflicts {
    <#
    .SYNOPSIS
        Detects conflicting configuration policies in Intune.
    .DESCRIPTION
        Proxies to scripts/endpoints/intune/maintenance/Find-PolicyConflicts.ps1.
        Original category: endpoints/intune/maintenance
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Find-PolicyConflicts -Verbose
    .NOTES
        Backing script: scripts/endpoints/intune/maintenance/Find-PolicyConflicts.ps1
        Category: endpoints/intune/maintenance
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/intune/maintenance/Find-PolicyConflicts.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [String]$ExportFormat,
        [Parameter()]
        [switch]$CheckSettingsCatalog,
        [Parameter()]
        [switch]$CheckConfigurationProfiles,
        [Parameter()]
        [switch]$CheckCompliancePolicies,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/intune/maintenance/Find-PolicyConflicts.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/intune/maintenance/Find-PolicyConflicts.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Find-StaleDevices {
    <#
    .SYNOPSIS
        Finds and optionally removes stale devices from Intune that haven't synced in X days.
    .DESCRIPTION
        Proxies to scripts/endpoints/intune/maintenance/Find-StaleDevices.ps1.
        Original category: endpoints/intune/maintenance
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Find-StaleDevices -Verbose
    .NOTES
        Backing script: scripts/endpoints/intune/maintenance/Find-StaleDevices.ps1
        Category: endpoints/intune/maintenance
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/intune/maintenance/Find-StaleDevices.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [Int32]$DaysInactive,
        [Parameter()]
        [String]$Action,
        [Parameter()]
        [String]$ExportFormat,
        [Parameter()]
        [switch]$AutoConfirm,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/intune/maintenance/Find-StaleDevices.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/intune/maintenance/Find-StaleDevices.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Get-ADHealthCheck {
    <#
    .SYNOPSIS
        Comprehensive Active Directory health check for domain controllers.
    .DESCRIPTION
        Proxies to scripts/infrastructure/windows/active-directory/Get-ADHealthCheck.ps1.
        Original category: infrastructure/windows/active-directory
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Get-ADHealthCheck -Verbose
    .NOTES
        Backing script: scripts/infrastructure/windows/active-directory/Get-ADHealthCheck.ps1
        Category: infrastructure/windows/active-directory
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/infrastructure/windows/active-directory/Get-ADHealthCheck.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [String]$DomainController,
        [Parameter()]
        [switch]$IncludeReplication,
        [Parameter()]
        [switch]$IncludeEventLogs,
        [Parameter()]
        [switch]$ExportHTML,
        [Parameter()]
        [switch]$ExportCSV,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'infrastructure/windows/active-directory/Get-ADHealthCheck.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/infrastructure/windows/active-directory/Get-ADHealthCheck.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Get-ADUserAudit {
    <#
    .SYNOPSIS
        Performs comprehensive audit of Active Directory user accounts.
    .DESCRIPTION
        Proxies to scripts/infrastructure/windows/active-directory/Get-ADUserAudit.ps1.
        Original category: infrastructure/windows/active-directory
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Get-ADUserAudit -Verbose
    .NOTES
        Backing script: scripts/infrastructure/windows/active-directory/Get-ADUserAudit.ps1
        Category: infrastructure/windows/active-directory
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/infrastructure/windows/active-directory/Get-ADUserAudit.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [Int32]$InactiveDays,
        [Parameter()]
        [String]$OutputPath,
        [Parameter()]
        [switch]$IncludeDisabledAccounts,
        [Parameter()]
        [switch]$CheckPrivilegedAccounts,
        [Parameter()]
        [switch]$ExportToCSV,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'infrastructure/windows/active-directory/Get-ADUserAudit.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/infrastructure/windows/active-directory/Get-ADUserAudit.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Get-AntivirusStatus {
    <#
    .SYNOPSIS
        Checks the status of antivirus and endpoint protection on Windows devices.
    .DESCRIPTION
        Proxies to scripts/security/compliance/frameworks/Get-AntivirusStatus.ps1.
        Original category: security/compliance/frameworks
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Get-AntivirusStatus -Verbose
    .NOTES
        Backing script: scripts/security/compliance/frameworks/Get-AntivirusStatus.ps1
        Category: security/compliance/frameworks
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/security/compliance/frameworks/Get-AntivirusStatus.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [String]$OutputFormat,
        [Parameter()]
        [String]$OutputPath,
        [Parameter()]
        [Boolean]$CheckThirdParty,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'security/compliance/frameworks/Get-AntivirusStatus.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/security/compliance/frameworks/Get-AntivirusStatus.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Get-AppInstallationStatus {
    <#
    .SYNOPSIS
        Checks installation status of a specific application across all Intune-managed devices.
    .DESCRIPTION
        Proxies to scripts/endpoints/intune/reporting/Get-AppInstallationStatus.ps1.
        Original category: endpoints/intune/reporting
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Get-AppInstallationStatus -Verbose
    .NOTES
        Backing script: scripts/endpoints/intune/reporting/Get-AppInstallationStatus.ps1
        Category: endpoints/intune/reporting
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/intune/reporting/Get-AppInstallationStatus.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [String]$AppName,
        [Parameter()]
        [String]$AppId,
        [Parameter()]
        [String]$ExportFormat,
        [Parameter()]
        [switch]$ShowSuccessOnly,
        [Parameter()]
        [switch]$ShowFailuresOnly,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/intune/reporting/Get-AppInstallationStatus.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/intune/reporting/Get-AppInstallationStatus.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Get-AppInstallErrorReport {
    <#
    .SYNOPSIS
        Detailed analysis of application installation failures.
    .DESCRIPTION
        Proxies to scripts/endpoints/intune/reporting/Get-AppInstallErrorReport.ps1.
        Original category: endpoints/intune/reporting
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Get-AppInstallErrorReport -Verbose
    .NOTES
        Backing script: scripts/endpoints/intune/reporting/Get-AppInstallErrorReport.ps1
        Category: endpoints/intune/reporting
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/intune/reporting/Get-AppInstallErrorReport.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [String]$AppName,
        [Parameter()]
        [Int32]$Days,
        [Parameter()]
        [Int32]$Top,
        [Parameter()]
        [switch]$ExportHTML,
        [Parameter()]
        [switch]$ExportCSV,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/intune/reporting/Get-AppInstallErrorReport.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/intune/reporting/Get-AppInstallErrorReport.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Get-AutopilotDeploymentReport {
    <#
    .SYNOPSIS
        Tracks Windows Autopilot deployment status and failures.
    .DESCRIPTION
        Proxies to scripts/endpoints/intune/reporting/Get-AutopilotDeploymentReport.ps1.
        Original category: endpoints/intune/reporting
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Get-AutopilotDeploymentReport -Verbose
    .NOTES
        Backing script: scripts/endpoints/intune/reporting/Get-AutopilotDeploymentReport.ps1
        Category: endpoints/intune/reporting
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/intune/reporting/Get-AutopilotDeploymentReport.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [Int32]$Days,
        [Parameter()]
        [String]$Status,
        [Parameter()]
        [String]$ProfileName,
        [Parameter()]
        [switch]$ExportHTML,
        [Parameter()]
        [switch]$ExportCSV,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/intune/reporting/Get-AutopilotDeploymentReport.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/intune/reporting/Get-AutopilotDeploymentReport.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Get-AWSResourceInventory {
    <#
    .SYNOPSIS
        AWS cloud resource inventory and health monitoring.
    .DESCRIPTION
        Proxies to scripts/cloud/aws/core/Get-AWSResourceInventory.ps1.
        Original category: cloud/aws/core
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Get-AWSResourceInventory -Verbose
    .NOTES
        Backing script: scripts/cloud/aws/core/Get-AWSResourceInventory.ps1
        Category: cloud/aws/core
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/cloud/aws/core/Get-AWSResourceInventory.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [String]$Region,
        [Parameter()]
        [String]$ProfileName,
        [Parameter()]
        [switch]$ExportHTML,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'cloud/aws/core/Get-AWSResourceInventory.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/cloud/aws/core/Get-AWSResourceInventory.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Get-AzureADGuestAudit {
    <#
    .SYNOPSIS
        Audits Azure AD (Entra ID) guest users for security and compliance.
    .DESCRIPTION
        Proxies to scripts/collaboration/microsoft365/azure-ad/Get-AzureADGuestAudit.ps1.
        Original category: collaboration/microsoft365/azure-ad
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Get-AzureADGuestAudit -Verbose
    .NOTES
        Backing script: scripts/collaboration/microsoft365/azure-ad/Get-AzureADGuestAudit.ps1
        Category: collaboration/microsoft365/azure-ad
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/collaboration/microsoft365/azure-ad/Get-AzureADGuestAudit.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [Int32]$InactivityDays,
        [Parameter()]
        [switch]$CheckPrivilegedGuests,
        [Parameter()]
        [switch]$GroupByDomain,
        [Parameter()]
        [switch]$ExportHTML,
        [Parameter()]
        [switch]$ExportCSV,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'collaboration/microsoft365/azure-ad/Get-AzureADGuestAudit.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/collaboration/microsoft365/azure-ad/Get-AzureADGuestAudit.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Get-AzureADLicenseReport {
    <#
    .SYNOPSIS
        Generates Azure AD (Entra ID) license assignment and usage report.
    .DESCRIPTION
        Proxies to scripts/collaboration/microsoft365/azure-ad/Get-AzureADLicenseReport.ps1.
        Original category: collaboration/microsoft365/azure-ad
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Get-AzureADLicenseReport -Verbose
    .NOTES
        Backing script: scripts/collaboration/microsoft365/azure-ad/Get-AzureADLicenseReport.ps1
        Category: collaboration/microsoft365/azure-ad
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/collaboration/microsoft365/azure-ad/Get-AzureADLicenseReport.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [switch]$IncludeServicePlans,
        [Parameter()]
        [switch]$IdentifyUnassigned,
        [Parameter()]
        [switch]$ExportHTML,
        [Parameter()]
        [switch]$ExportCSV,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'collaboration/microsoft365/azure-ad/Get-AzureADLicenseReport.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/collaboration/microsoft365/azure-ad/Get-AzureADLicenseReport.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Get-AzureResourceHealth {
    <#
    .SYNOPSIS
        Azure cloud resource health check.
    .DESCRIPTION
        Proxies to scripts/cloud/azure/core/Get-AzureResourceHealth.ps1.
        Original category: cloud/azure/core
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Get-AzureResourceHealth -Verbose
    .NOTES
        Backing script: scripts/cloud/azure/core/Get-AzureResourceHealth.ps1
        Category: cloud/azure/core
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/cloud/azure/core/Get-AzureResourceHealth.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [String]$SubscriptionId,
        [Parameter()]
        [switch]$ExportHTML,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'cloud/azure/core/Get-AzureResourceHealth.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/cloud/azure/core/Get-AzureResourceHealth.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Get-BackupStatus {
    <#
    .SYNOPSIS
        Verifies Windows Server Backup status and configuration.
    .DESCRIPTION
        Proxies to scripts/infrastructure/windows/backup-recovery/Get-BackupStatus.ps1.
        Original category: infrastructure/windows/backup-recovery
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Get-BackupStatus -Verbose
    .NOTES
        Backing script: scripts/infrastructure/windows/backup-recovery/Get-BackupStatus.ps1
        Category: infrastructure/windows/backup-recovery
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/infrastructure/windows/backup-recovery/Get-BackupStatus.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [Int32]$CheckDays,
        [Parameter()]
        [String]$OutputPath,
        [Parameter()]
        [switch]$ValidateBackups,
        [Parameter()]
        [switch]$AlertIfNoBackup,
        [Parameter()]
        [switch]$EmailReport,
        [Parameter()]
        [String]$EmailTo,
        [Parameter()]
        [String]$SMTPServer,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'infrastructure/windows/backup-recovery/Get-BackupStatus.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/infrastructure/windows/backup-recovery/Get-BackupStatus.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Get-BatteryHealth {
    <#
    .SYNOPSIS
        Monitors laptop battery health and generates detailed battery reports.
    .DESCRIPTION
        Proxies to scripts/security/monitoring/Get-BatteryHealth.ps1.
        Original category: security/monitoring
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Get-BatteryHealth -Verbose
    .NOTES
        Backing script: scripts/security/monitoring/Get-BatteryHealth.ps1
        Category: security/monitoring
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/security/monitoring/Get-BatteryHealth.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [String]$OutputPath,
        [Parameter()]
        [switch]$GenerateDetailedReport,
        [Parameter()]
        [Int32]$AlertThreshold,
        [Parameter()]
        [switch]$ExportToCSV,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'security/monitoring/Get-BatteryHealth.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/security/monitoring/Get-BatteryHealth.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Get-BitLockerStatus {
    <#
    .SYNOPSIS
        Audits BitLocker encryption status across all Windows devices in Intune.
    .DESCRIPTION
        Proxies to scripts/endpoints/intune/reporting/Get-BitLockerStatus.ps1.
        Original category: endpoints/intune/reporting
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Get-BitLockerStatus -Verbose
    .NOTES
        Backing script: scripts/endpoints/intune/reporting/Get-BitLockerStatus.ps1
        Category: endpoints/intune/reporting
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/intune/reporting/Get-BitLockerStatus.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [String]$ExportFormat,
        [Parameter()]
        [switch]$ShowUnencryptedOnly,
        [Parameter()]
        [switch]$ShowMissingKeys,
        [Parameter()]
        [switch]$IncludeNonWindows,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/intune/reporting/Get-BitLockerStatus.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/intune/reporting/Get-BitLockerStatus.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Get-DefenderO365ThreatReport {
    <#
    .SYNOPSIS
        Generates comprehensive Microsoft Defender for Office 365 threat reports.
    .DESCRIPTION
        Proxies to scripts/collaboration/microsoft365/defender-office365/Get-DefenderO365ThreatReport.ps1.
        Original category: collaboration/microsoft365/defender-office365
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Get-DefenderO365ThreatReport -Verbose
    .NOTES
        Backing script: scripts/collaboration/microsoft365/defender-office365/Get-DefenderO365ThreatReport.ps1
        Category: collaboration/microsoft365/defender-office365
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/collaboration/microsoft365/defender-office365/Get-DefenderO365ThreatReport.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [Int32]$DaysToAnalyze,
        [Parameter()]
        [switch]$IncludeDetailedThreats,
        [Parameter()]
        [switch]$IncludeUserRiskAnalysis,
        [Parameter()]
        [String]$OutputFormat,
        [Parameter()]
        [String]$OutputPath,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'collaboration/microsoft365/defender-office365/Get-DefenderO365ThreatReport.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/collaboration/microsoft365/defender-office365/Get-DefenderO365ThreatReport.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Get-DeviceComplianceReport {
    <#
    .SYNOPSIS
        Exports all non-compliant devices from Intune with detailed compliance reasons.
    .DESCRIPTION
        Proxies to scripts/endpoints/intune/reporting/Get-DeviceComplianceReport.ps1.
        Original category: endpoints/intune/reporting
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Get-DeviceComplianceReport -Verbose
    .NOTES
        Backing script: scripts/endpoints/intune/reporting/Get-DeviceComplianceReport.ps1
        Category: endpoints/intune/reporting
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/intune/reporting/Get-DeviceComplianceReport.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [String]$ExportFormat,
        [Parameter()]
        [switch]$IncludeCompliant,
        [Parameter()]
        [String]$OutputPath,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/intune/reporting/Get-DeviceComplianceReport.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/intune/reporting/Get-DeviceComplianceReport.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Get-DeviceGroupMembership {
    <#
    .SYNOPSIS
        Audits device group memberships in Azure AD/Intune.
    .DESCRIPTION
        Proxies to scripts/endpoints/intune/reporting/Get-DeviceGroupMembership.ps1.
        Original category: endpoints/intune/reporting
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Get-DeviceGroupMembership -Verbose
    .NOTES
        Backing script: scripts/endpoints/intune/reporting/Get-DeviceGroupMembership.ps1
        Category: endpoints/intune/reporting
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/intune/reporting/Get-DeviceGroupMembership.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [String]$DeviceName,
        [Parameter()]
        [String]$GroupName,
        [Parameter()]
        [switch]$ShowDynamicGroupsOnly,
        [Parameter()]
        [String]$ExportFormat,
        [Parameter()]
        [switch]$ShowAllMappings,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/intune/reporting/Get-DeviceGroupMembership.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/intune/reporting/Get-DeviceGroupMembership.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Get-DeviceHealthScore {
    <#
    .SYNOPSIS
        Generates a comprehensive device health score report.
    .DESCRIPTION
        Proxies to scripts/endpoints/intune/reporting/Get-DeviceHealthScore.ps1.
        Original category: endpoints/intune/reporting
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Get-DeviceHealthScore -Verbose
    .NOTES
        Backing script: scripts/endpoints/intune/reporting/Get-DeviceHealthScore.ps1
        Category: endpoints/intune/reporting
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/intune/reporting/Get-DeviceHealthScore.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/intune/reporting/Get-DeviceHealthScore.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/intune/reporting/Get-DeviceHealthScore.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Get-DiskReport {
    <#
    .SYNOPSIS
        Generates comprehensive disk usage report and suggests cleanup targets for Windows Server 2016-2022.
    .DESCRIPTION
        Proxies to scripts/infrastructure/windows/storage/Get-DiskReport.ps1.
        Original category: infrastructure/windows/storage
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Get-DiskReport -Verbose
    .NOTES
        Backing script: scripts/infrastructure/windows/storage/Get-DiskReport.ps1
        Category: infrastructure/windows/storage
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/infrastructure/windows/storage/Get-DiskReport.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [String]$DriveLetter,
        [Parameter()]
        [switch]$ExportReport,
        [Parameter()]
        [switch]$ShowCleanupOnly,
        [Parameter()]
        [Int32]$MinimumFolderSizeMB,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'infrastructure/windows/storage/Get-DiskReport.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/infrastructure/windows/storage/Get-DiskReport.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Get-DistributionListAudit {
    <#
    .SYNOPSIS
        Comprehensive distribution list and group audit for Exchange Online.
    .DESCRIPTION
        Proxies to scripts/collaboration/microsoft365/exchange-online/Get-DistributionListAudit.ps1.
        Original category: collaboration/microsoft365/exchange-online
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Get-DistributionListAudit -Verbose
    .NOTES
        Backing script: scripts/collaboration/microsoft365/exchange-online/Get-DistributionListAudit.ps1
        Category: collaboration/microsoft365/exchange-online
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/collaboration/microsoft365/exchange-online/Get-DistributionListAudit.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [switch]$IncludeMicrosoft365Groups,
        [Parameter()]
        [switch]$IncludeSecurityGroups,
        [Parameter()]
        [switch]$CheckForOrphaned,
        [Parameter()]
        [Int32]$MinimumMembers,
        [Parameter()]
        [String]$OutputFormat,
        [Parameter()]
        [String]$OutputPath,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'collaboration/microsoft365/exchange-online/Get-DistributionListAudit.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/collaboration/microsoft365/exchange-online/Get-DistributionListAudit.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Get-DockerHealthCheck {
    <#
    .SYNOPSIS
        Comprehensive Docker environment health check.
    .DESCRIPTION
        Proxies to scripts/cloud/containers/Get-DockerHealthCheck.ps1.
        Original category: cloud/containers
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Get-DockerHealthCheck -Verbose
    .NOTES
        Backing script: scripts/cloud/containers/Get-DockerHealthCheck.ps1
        Category: cloud/containers
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/cloud/containers/Get-DockerHealthCheck.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [switch]$IncludeImages,
        [Parameter()]
        [switch]$CheckVulnerabilities,
        [Parameter()]
        [switch]$IncludeNetworks,
        [Parameter()]
        [switch]$ExportHTML,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'cloud/containers/Get-DockerHealthCheck.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/cloud/containers/Get-DockerHealthCheck.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Get-EventLogReport {
    <#
    .SYNOPSIS
        Parses and analyzes Windows event logs for critical errors and security events.
    .DESCRIPTION
        Proxies to scripts/infrastructure/windows/monitoring/Get-EventLogReport.ps1.
        Original category: infrastructure/windows/monitoring
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Get-EventLogReport -Verbose
    .NOTES
        Backing script: scripts/infrastructure/windows/monitoring/Get-EventLogReport.ps1
        Category: infrastructure/windows/monitoring
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/infrastructure/windows/monitoring/Get-EventLogReport.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [String]$LogName,
        [Parameter()]
        [Int32]$Hours,
        [Parameter()]
        [Int32]$Days,
        [Parameter()]
        [Int32]$MaxEvents,
        [Parameter()]
        [String]$Severity,
        [Parameter()]
        [switch]$ExportHTML,
        [Parameter()]
        [switch]$ExportCSV,
        [Parameter()]
        [switch]$GroupBySource,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'infrastructure/windows/monitoring/Get-EventLogReport.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/infrastructure/windows/monitoring/Get-EventLogReport.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Get-ExchangeServerHealth {
    <#
    .SYNOPSIS
        Exchange Server health check and monitoring.
    .DESCRIPTION
        Proxies to scripts/collaboration/email/exchange-server/Get-ExchangeServerHealth.ps1.
        Original category: collaboration/email/exchange-server
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Get-ExchangeServerHealth -Verbose
    .NOTES
        Backing script: scripts/collaboration/email/exchange-server/Get-ExchangeServerHealth.ps1
        Category: collaboration/email/exchange-server
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/collaboration/email/exchange-server/Get-ExchangeServerHealth.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [switch]$ExportHTML,
        [Parameter()]
        [switch]$CheckDatabaseHealth,
        [Parameter()]
        [switch]$CheckMailQueues,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'collaboration/email/exchange-server/Get-ExchangeServerHealth.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/collaboration/email/exchange-server/Get-ExchangeServerHealth.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Get-ExpiredCertificates {
    <#
    .SYNOPSIS
        Finds expired or expiring certificates in all certificate stores.
    .DESCRIPTION
        Proxies to scripts/security/compliance/frameworks/Get-ExpiredCertificates.ps1.
        Original category: security/compliance/frameworks
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Get-ExpiredCertificates -Verbose
    .NOTES
        Backing script: scripts/security/compliance/frameworks/Get-ExpiredCertificates.ps1
        Category: security/compliance/frameworks
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/security/compliance/frameworks/Get-ExpiredCertificates.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [Int32]$DaysToExpire,
        [Parameter()]
        [switch]$AllStores,
        [Parameter()]
        [switch]$ExportReport,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'security/compliance/frameworks/Get-ExpiredCertificates.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/security/compliance/frameworks/Get-ExpiredCertificates.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Get-FailedLoginReport {
    <#
    .SYNOPSIS
        Analyzes security event logs for failed login attempts and suspicious activity.
    .DESCRIPTION
        Proxies to scripts/security/compliance/frameworks/Get-FailedLoginReport.ps1.
        Original category: security/compliance/frameworks
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Get-FailedLoginReport -Verbose
    .NOTES
        Backing script: scripts/security/compliance/frameworks/Get-FailedLoginReport.ps1
        Category: security/compliance/frameworks
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/security/compliance/frameworks/Get-FailedLoginReport.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [Int32]$Hours,
        [Parameter()]
        [Int32]$TopCount,
        [Parameter()]
        [switch]$ExportReport,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'security/compliance/frameworks/Get-FailedLoginReport.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/security/compliance/frameworks/Get-FailedLoginReport.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Get-FirewallRulesReport {
    <#
    .SYNOPSIS
        Documents and exports Windows Firewall rules for Windows Server 2016-2022.
    .DESCRIPTION
        Proxies to scripts/infrastructure/windows/network/Get-FirewallRulesReport.ps1.
        Original category: infrastructure/windows/network
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Get-FirewallRulesReport -Verbose
    .NOTES
        Backing script: scripts/infrastructure/windows/network/Get-FirewallRulesReport.ps1
        Category: infrastructure/windows/network
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/infrastructure/windows/network/Get-FirewallRulesReport.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [String]$Direction,
        [Parameter()]
        [String]$Enabled,
        [Parameter()]
        [String]$Action,
        [Parameter()] [Alias('Profile')]
        [String]$ProfilePath,
        [Parameter()]
        [switch]$ExportHTML,
        [Parameter()]
        [switch]$ExportCSV,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
        if ($boundParams.ContainsKey('ProfilePath')) { $boundParams['Profile'] = $boundParams['ProfilePath']; $null = $boundParams.Remove('ProfilePath') }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'infrastructure/windows/network/Get-FirewallRulesReport.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/infrastructure/windows/network/Get-FirewallRulesReport.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Get-GPOReport {
    <#
    .SYNOPSIS
        Generates comprehensive Group Policy Object (GPO) reports for domain analysis.
    .DESCRIPTION
        Proxies to scripts/infrastructure/windows/group-policy/Get-GPOReport.ps1.
        Original category: infrastructure/windows/group-policy
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Get-GPOReport -Verbose
    .NOTES
        Backing script: scripts/infrastructure/windows/group-policy/Get-GPOReport.ps1
        Category: infrastructure/windows/group-policy
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/infrastructure/windows/group-policy/Get-GPOReport.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [String]$OutputPath,
        [Parameter()]
        [String]$ReportFormat,
        [Parameter()]
        [switch]$IncludeUnlinkedGPOs,
        [Parameter()]
        [switch]$IncludeEmptyGPOs,
        [Parameter()]
        [switch]$ExportToCSV,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'infrastructure/windows/group-policy/Get-GPOReport.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/infrastructure/windows/group-policy/Get-GPOReport.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Get-HyperVHealth {
    <#
    .SYNOPSIS
        Performs comprehensive health check of Hyper-V host and virtual machines.
    .DESCRIPTION
        Proxies to scripts/infrastructure/virtualization/Get-HyperVHealth.ps1.
        Original category: infrastructure/virtualization
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Get-HyperVHealth -Verbose
    .NOTES
        Backing script: scripts/infrastructure/virtualization/Get-HyperVHealth.ps1
        Category: infrastructure/virtualization
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/infrastructure/virtualization/Get-HyperVHealth.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [switch]$IncludeVMs,
        [Parameter()]
        [switch]$CheckReplication,
        [Parameter()]
        [switch]$CheckSnapshots,
        [Parameter()]
        [switch]$ExportHTML,
        [Parameter()]
        [switch]$ExportCSV,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'infrastructure/virtualization/Get-HyperVHealth.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/infrastructure/virtualization/Get-HyperVHealth.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Get-IISHealthCheck {
    <#
    .SYNOPSIS
        Comprehensive IIS health check and configuration audit.
    .DESCRIPTION
        Proxies to scripts/infrastructure/web/iis/Get-IISHealthCheck.ps1.
        Original category: infrastructure/web/iis
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Get-IISHealthCheck -Verbose
    .NOTES
        Backing script: scripts/infrastructure/web/iis/Get-IISHealthCheck.ps1
        Category: infrastructure/web/iis
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/infrastructure/web/iis/Get-IISHealthCheck.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [switch]$IncludePerformanceCounters,
        [Parameter()]
        [switch]$CheckSSLCertificates,
        [Parameter()]
        [switch]$AnalyzeLogFiles,
        [Parameter()]
        [Int32]$DaysToAnalyze,
        [Parameter()]
        [switch]$ExportHTML,
        [Parameter()]
        [switch]$ExportCSV,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'infrastructure/web/iis/Get-IISHealthCheck.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/infrastructure/web/iis/Get-IISHealthCheck.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Get-IISLogAnalyzer {
    <#
    .SYNOPSIS
        Advanced IIS log file analysis and reporting.
    .DESCRIPTION
        Proxies to scripts/infrastructure/web/iis/Get-IISLogAnalyzer.ps1.
        Original category: infrastructure/web/iis
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Get-IISLogAnalyzer -Verbose
    .NOTES
        Backing script: scripts/infrastructure/web/iis/Get-IISLogAnalyzer.ps1
        Category: infrastructure/web/iis
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/infrastructure/web/iis/Get-IISLogAnalyzer.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [String]$LogPath,
        [Parameter()]
        [Int32]$DaysToAnalyze,
        [Parameter()]
        [Int32]$Top,
        [Parameter()]
        [switch]$IncludeSecurityAnalysis,
        [Parameter()]
        [switch]$ExportHTML,
        [Parameter()]
        [switch]$ExportCSV,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'infrastructure/web/iis/Get-IISLogAnalyzer.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/infrastructure/web/iis/Get-IISLogAnalyzer.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Get-InactiveUserReport {
    <#
    .SYNOPSIS
        Identifies and reports on inactive Active Directory user accounts.
    .DESCRIPTION
        Proxies to scripts/infrastructure/windows/user-management/Get-InactiveUserReport.ps1.
        Original category: infrastructure/windows/user-management
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Get-InactiveUserReport -Verbose
    .NOTES
        Backing script: scripts/infrastructure/windows/user-management/Get-InactiveUserReport.ps1
        Category: infrastructure/windows/user-management
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/infrastructure/windows/user-management/Get-InactiveUserReport.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [Int32]$DaysInactive,
        [Parameter()]
        [switch]$IncludeDisabled,
        [Parameter()]
        [String]$SearchBase,
        [Parameter()]
        [switch]$ExcludeServiceAccounts,
        [Parameter()]
        [switch]$HighlightPrivileged,
        [Parameter()]
        [switch]$ExportHTML,
        [Parameter()]
        [switch]$ExportCSV,
        [Parameter()]
        [switch]$WhatIfDisable,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'infrastructure/windows/user-management/Get-InactiveUserReport.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/infrastructure/windows/user-management/Get-InactiveUserReport.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Get-IntuneDevicePrimaryUsers {
    <#
    .SYNOPSIS
        Resolves Intune Primary Users and friendly hardware specs for one or more devices.
    .DESCRIPTION
        Proxies to scripts/endpoints/intune/reporting/Get-IntuneDevicePrimaryUsers.ps1.
        Original category: endpoints/intune/reporting
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Get-IntuneDevicePrimaryUsers -Verbose
    .NOTES
        Backing script: scripts/endpoints/intune/reporting/Get-IntuneDevicePrimaryUsers.ps1
        Category: endpoints/intune/reporting
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/intune/reporting/Get-IntuneDevicePrimaryUsers.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [String[]]$DeviceName,
        [Parameter()]
        [String]$InputFile,
        [Parameter()]
        [String]$OutputPath,
        [Parameter()]
        [String]$FriendlyModelAttribute,
        [Parameter()]
        [switch]$NoExport,
        [Parameter()]
        [switch]$Quiet,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/intune/reporting/Get-IntuneDevicePrimaryUsers.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/intune/reporting/Get-IntuneDevicePrimaryUsers.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Get-KubernetesHealthCheck {
    <#
    .SYNOPSIS
        Kubernetes cluster health check and diagnostics.
    .DESCRIPTION
        Proxies to scripts/cloud/containers/Get-KubernetesHealthCheck.ps1.
        Original category: cloud/containers
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Get-KubernetesHealthCheck -Verbose
    .NOTES
        Backing script: scripts/cloud/containers/Get-KubernetesHealthCheck.ps1
        Category: cloud/containers
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/cloud/containers/Get-KubernetesHealthCheck.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [String]$Namespace,
        [Parameter()]
        [switch]$IncludeMetrics,
        [Parameter()]
        [switch]$CheckEvents,
        [Parameter()]
        [switch]$ExportHTML,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'cloud/containers/Get-KubernetesHealthCheck.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/cloud/containers/Get-KubernetesHealthCheck.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Get-LargeFilesReport {
    <#
    .SYNOPSIS
        Identifies and reports large files consuming disk space on Windows Server.
    .DESCRIPTION
        Proxies to scripts/infrastructure/windows/storage/Get-LargeFilesReport.ps1.
        Original category: infrastructure/windows/storage
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Get-LargeFilesReport -Verbose
    .NOTES
        Backing script: scripts/infrastructure/windows/storage/Get-LargeFilesReport.ps1
        Category: infrastructure/windows/storage
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/infrastructure/windows/storage/Get-LargeFilesReport.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [String]$Path,
        [Parameter()]
        [Int32]$MinimumSizeMB,
        [Parameter()]
        [Int32]$TopCount,
        [Parameter()]
        [switch]$IncludeDuplicates,
        [Parameter()]
        [String[]]$ExcludePath,
        [Parameter()]
        [switch]$ExportHTML,
        [Parameter()]
        [switch]$ExportCSV,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'infrastructure/windows/storage/Get-LargeFilesReport.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/infrastructure/windows/storage/Get-LargeFilesReport.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Get-LocalAdminAudit {
    <#
    .SYNOPSIS
        Audits local administrator accounts on the system.
    .DESCRIPTION
        Proxies to scripts/security/compliance/frameworks/Get-LocalAdminAudit.ps1.
        Original category: security/compliance/frameworks
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Get-LocalAdminAudit -Verbose
    .NOTES
        Backing script: scripts/security/compliance/frameworks/Get-LocalAdminAudit.ps1
        Category: security/compliance/frameworks
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/security/compliance/frameworks/Get-LocalAdminAudit.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [switch]$Detailed,
        [Parameter()]
        [switch]$ExportReport,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'security/compliance/frameworks/Get-LocalAdminAudit.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/security/compliance/frameworks/Get-LocalAdminAudit.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Get-M365UserInfo {
    <#
    .SYNOPSIS
        Comprehensive M365 user information and management toolkit.
    .DESCRIPTION
        Proxies to scripts/collaboration/microsoft365/Get-M365UserInfo.ps1.
        Original category: collaboration/microsoft365
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Get-M365UserInfo -Verbose
    .NOTES
        Backing script: scripts/collaboration/microsoft365/Get-M365UserInfo.ps1
        Category: collaboration/microsoft365
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/collaboration/microsoft365/Get-M365UserInfo.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [String]$UserEmail,
        [Parameter()]
        [switch]$AutoConnect,
        [Parameter()]
        [switch]$QuickView,
        [Parameter()]
        [switch]$ExportReport,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'collaboration/microsoft365/Get-M365UserInfo.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/collaboration/microsoft365/Get-M365UserInfo.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Get-MailboxHealth {
    <#
    .SYNOPSIS
        Generates comprehensive Exchange Online mailbox health and usage report.
    .DESCRIPTION
        Proxies to scripts/collaboration/microsoft365/exchange-online/Get-MailboxHealth.ps1.
        Original category: collaboration/microsoft365/exchange-online
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Get-MailboxHealth -Verbose
    .NOTES
        Backing script: scripts/collaboration/microsoft365/exchange-online/Get-MailboxHealth.ps1
        Category: collaboration/microsoft365/exchange-online
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/collaboration/microsoft365/exchange-online/Get-MailboxHealth.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [String]$MailboxType,
        [Parameter()]
        [switch]$IncludeArchive,
        [Parameter()]
        [switch]$IncludePermissions,
        [Parameter()]
        [Int32]$QuotaWarningThreshold,
        [Parameter()]
        [Int32]$InactivityDays,
        [Parameter()]
        [switch]$ExportHTML,
        [Parameter()]
        [switch]$ExportCSV,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'collaboration/microsoft365/exchange-online/Get-MailboxHealth.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/collaboration/microsoft365/exchange-online/Get-MailboxHealth.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Get-MailFlowAnalysis {
    <#
    .SYNOPSIS
        Analyzes Exchange Online mail flow health, routing, and delivery issues.
    .DESCRIPTION
        Proxies to scripts/collaboration/microsoft365/exchange-online/Get-MailFlowAnalysis.ps1.
        Original category: collaboration/microsoft365/exchange-online
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Get-MailFlowAnalysis -Verbose
    .NOTES
        Backing script: scripts/collaboration/microsoft365/exchange-online/Get-MailFlowAnalysis.ps1
        Category: collaboration/microsoft365/exchange-online
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/collaboration/microsoft365/exchange-online/Get-MailFlowAnalysis.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [Int32]$DaysToAnalyze,
        [Parameter()]
        [switch]$IncludeTransportRules,
        [Parameter()]
        [switch]$IncludeConnectors,
        [Parameter()]
        [switch]$AnalyzeFailures,
        [Parameter()]
        [String]$OutputFormat,
        [Parameter()]
        [String]$OutputPath,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'collaboration/microsoft365/exchange-online/Get-MailFlowAnalysis.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/collaboration/microsoft365/exchange-online/Get-MailFlowAnalysis.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Get-MDEDeviceHealth {
    <#
    .SYNOPSIS
        Comprehensive Microsoft Defender for Endpoint device health and security monitoring.
    .DESCRIPTION
        Proxies to scripts/security/compliance/defender-endpoint/Get-MDEDeviceHealth.ps1.
        Original category: security/compliance/defender-endpoint
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Get-MDEDeviceHealth -Verbose
    .NOTES
        Backing script: scripts/security/compliance/defender-endpoint/Get-MDEDeviceHealth.ps1
        Category: security/compliance/defender-endpoint
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/security/compliance/defender-endpoint/Get-MDEDeviceHealth.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(Mandatory)]
        [String]$TenantId,
        [Parameter()]
        [switch]$IncludeVulnerabilities,
        [Parameter()]
        [switch]$IncludeRecommendations,
        [Parameter()]
        [String]$RiskLevelFilter,
        [Parameter()]
        [String]$OutputFormat,
        [Parameter()]
        [String]$OutputPath,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'security/compliance/defender-endpoint/Get-MDEDeviceHealth.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/security/compliance/defender-endpoint/Get-MDEDeviceHealth.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Get-MySQLHealth {
    <#
    .SYNOPSIS
        MySQL database server health monitoring.
    .DESCRIPTION
        Proxies to scripts/data/databases/Get-MySQLHealth.ps1.
        Original category: data/databases
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Get-MySQLHealth -Verbose
    .NOTES
        Backing script: scripts/data/databases/Get-MySQLHealth.ps1
        Category: data/databases
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/data/databases/Get-MySQLHealth.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'data/databases/Get-MySQLHealth.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/data/databases/Get-MySQLHealth.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Get-NetworkConfiguration {
    <#
    .SYNOPSIS
        Documents complete network configuration for Windows Server 2016-2022.
    .DESCRIPTION
        Proxies to scripts/infrastructure/windows/network/Get-NetworkConfiguration.ps1.
        Original category: infrastructure/windows/network
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Get-NetworkConfiguration -Verbose
    .NOTES
        Backing script: scripts/infrastructure/windows/network/Get-NetworkConfiguration.ps1
        Category: infrastructure/windows/network
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/infrastructure/windows/network/Get-NetworkConfiguration.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [switch]$IncludeRouting,
        [Parameter()]
        [switch]$IncludeShares,
        [Parameter()]
        [switch]$IncludeFirewall,
        [Parameter()]
        [switch]$ExportHTML,
        [Parameter()]
        [switch]$ExportCSV,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'infrastructure/windows/network/Get-NetworkConfiguration.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/infrastructure/windows/network/Get-NetworkConfiguration.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Get-OneDriveUsageReport {
    <#
    .SYNOPSIS
        Generates OneDrive for Business usage and storage report.
    .DESCRIPTION
        Proxies to scripts/collaboration/microsoft365/sharepoint-onedrive/Get-OneDriveUsageReport.ps1.
        Original category: collaboration/microsoft365/sharepoint-onedrive
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Get-OneDriveUsageReport -Verbose
    .NOTES
        Backing script: scripts/collaboration/microsoft365/sharepoint-onedrive/Get-OneDriveUsageReport.ps1
        Category: collaboration/microsoft365/sharepoint-onedrive
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/collaboration/microsoft365/sharepoint-onedrive/Get-OneDriveUsageReport.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [Int32]$StorageWarningThreshold,
        [Parameter()]
        [Int32]$InactivityDays,
        [Parameter()]
        [switch]$ExportHTML,
        [Parameter()]
        [switch]$ExportCSV,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'collaboration/microsoft365/sharepoint-onedrive/Get-OneDriveUsageReport.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/collaboration/microsoft365/sharepoint-onedrive/Get-OneDriveUsageReport.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Get-OpenPortScan {
    <#
    .SYNOPSIS
        Scans for open network ports and identifies associated processes.
    .DESCRIPTION
        Proxies to scripts/security/compliance/frameworks/Get-OpenPortScan.ps1.
        Original category: security/compliance/frameworks
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Get-OpenPortScan -Verbose
    .NOTES
        Backing script: scripts/security/compliance/frameworks/Get-OpenPortScan.ps1
        Category: security/compliance/frameworks
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/security/compliance/frameworks/Get-OpenPortScan.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [switch]$HighRiskOnly,
        [Parameter()]
        [switch]$IncludeUDP,
        [Parameter()]
        [switch]$ExportReport,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'security/compliance/frameworks/Get-OpenPortScan.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/security/compliance/frameworks/Get-OpenPortScan.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Get-PerformanceReport {
    <#
    .SYNOPSIS
        Generates comprehensive performance metrics and bottleneck analysis for Windows Server.
    .DESCRIPTION
        Proxies to scripts/infrastructure/windows/monitoring/Get-PerformanceReport.ps1.
        Original category: infrastructure/windows/monitoring
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Get-PerformanceReport -Verbose
    .NOTES
        Backing script: scripts/infrastructure/windows/monitoring/Get-PerformanceReport.ps1
        Category: infrastructure/windows/monitoring
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/infrastructure/windows/monitoring/Get-PerformanceReport.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [Int32]$DurationMinutes,
        [Parameter()]
        [Int32]$SampleInterval,
        [Parameter()]
        [switch]$IncludeDiskIO,
        [Parameter()]
        [switch]$IncludeNetworkStats,
        [Parameter()]
        [switch]$ExportHTML,
        [Parameter()]
        [switch]$ExportCSV,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'infrastructure/windows/monitoring/Get-PerformanceReport.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/infrastructure/windows/monitoring/Get-PerformanceReport.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Get-PerformanceTrends {
    <#
    .SYNOPSIS
        Monitors system performance trends over time and identifies anomalies.
    .DESCRIPTION
        Proxies to scripts/security/monitoring/Get-PerformanceTrends.ps1.
        Original category: security/monitoring
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Get-PerformanceTrends -Verbose
    .NOTES
        Backing script: scripts/security/monitoring/Get-PerformanceTrends.ps1
        Category: security/monitoring
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/security/monitoring/Get-PerformanceTrends.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [Int32]$DurationMinutes,
        [Parameter()]
        [Int32]$SampleInterval,
        [Parameter()]
        [String]$OutputPath,
        [Parameter()]
        [switch]$MonitorProcesses,
        [Parameter()]
        [Hashtable]$AlertThresholds,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'security/monitoring/Get-PerformanceTrends.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/security/monitoring/Get-PerformanceTrends.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Get-PolicyAssignmentReport {
    <#
    .SYNOPSIS
        Generates a comprehensive policy assignment report for Intune.
    .DESCRIPTION
        Proxies to scripts/endpoints/intune/reporting/Get-PolicyAssignmentReport.ps1.
        Original category: endpoints/intune/reporting
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Get-PolicyAssignmentReport -Verbose
    .NOTES
        Backing script: scripts/endpoints/intune/reporting/Get-PolicyAssignmentReport.ps1
        Category: endpoints/intune/reporting
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/intune/reporting/Get-PolicyAssignmentReport.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/intune/reporting/Get-PolicyAssignmentReport.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/intune/reporting/Get-PolicyAssignmentReport.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Get-PostgreSQLHealth {
    <#
    .SYNOPSIS
        PostgreSQL database server health monitoring.
    .DESCRIPTION
        Proxies to scripts/data/databases/Get-PostgreSQLHealth.ps1.
        Original category: data/databases
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Get-PostgreSQLHealth -Verbose
    .NOTES
        Backing script: scripts/data/databases/Get-PostgreSQLHealth.ps1
        Category: data/databases
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/data/databases/Get-PostgreSQLHealth.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [String]$Server,
        [Parameter()]
        [String]$Database,
        [Parameter()]
        [String]$Username,
        [Parameter()]
        [Int32]$Port,
        [Parameter()]
        [switch]$CheckReplication,
        [Parameter()]
        [switch]$ExportHTML,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'data/databases/Get-PostgreSQLHealth.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/data/databases/Get-PostgreSQLHealth.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Get-PowerPlatformGovernance {
    <#
    .SYNOPSIS
        Comprehensive Power Platform governance and compliance reporting.
    .DESCRIPTION
        Proxies to scripts/collaboration/microsoft365/power-platform/Get-PowerPlatformGovernance.ps1.
        Original category: collaboration/microsoft365/power-platform
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Get-PowerPlatformGovernance -Verbose
    .NOTES
        Backing script: scripts/collaboration/microsoft365/power-platform/Get-PowerPlatformGovernance.ps1
        Category: collaboration/microsoft365/power-platform
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/collaboration/microsoft365/power-platform/Get-PowerPlatformGovernance.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(Mandatory)]
        [String]$TenantId,
        [Parameter()]
        [switch]$IncludeAppDetails,
        [Parameter()]
        [switch]$IncludeFlowDetails,
        [Parameter()]
        [switch]$CheckDLPCompliance,
        [Parameter()]
        [String]$OutputFormat,
        [Parameter()]
        [String]$OutputPath,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'collaboration/microsoft365/power-platform/Get-PowerPlatformGovernance.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/collaboration/microsoft365/power-platform/Get-PowerPlatformGovernance.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Get-PrintServerHealth {
    <#
    .SYNOPSIS
        Monitors print server health and identifies printing issues.
    .DESCRIPTION
        Proxies to scripts/infrastructure/print/Get-PrintServerHealth.ps1.
        Original category: infrastructure/print
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Get-PrintServerHealth -Verbose
    .NOTES
        Backing script: scripts/infrastructure/print/Get-PrintServerHealth.ps1
        Category: infrastructure/print
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/infrastructure/print/Get-PrintServerHealth.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [switch]$ClearStuckJobs,
        [Parameter()]
        [Int32]$StuckJobThresholdHours,
        [Parameter()]
        [switch]$CheckDrivers,
        [Parameter()]
        [switch]$ExportHTML,
        [Parameter()]
        [switch]$ExportCSV,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'infrastructure/print/Get-PrintServerHealth.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/infrastructure/print/Get-PrintServerHealth.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Get-SecurityBaseline {
    <#
    .SYNOPSIS
        Verifies system security settings against industry-standard baselines.
    .DESCRIPTION
        Proxies to scripts/security/compliance/frameworks/Get-SecurityBaseline.ps1.
        Original category: security/compliance/frameworks
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Get-SecurityBaseline -Verbose
    .NOTES
        Backing script: scripts/security/compliance/frameworks/Get-SecurityBaseline.ps1
        Category: security/compliance/frameworks
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/security/compliance/frameworks/Get-SecurityBaseline.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [String]$BaselineType,
        [Parameter()]
        [switch]$ExportReport,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'security/compliance/frameworks/Get-SecurityBaseline.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/security/compliance/frameworks/Get-SecurityBaseline.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Get-SecurityEventAudit {
    <#
    .SYNOPSIS
        Analyzes Windows Security Event logs for suspicious activities and security incidents.
    .DESCRIPTION
        Proxies to scripts/infrastructure/windows/security/Get-SecurityEventAudit.ps1.
        Original category: infrastructure/windows/security
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Get-SecurityEventAudit -Verbose
    .NOTES
        Backing script: scripts/infrastructure/windows/security/Get-SecurityEventAudit.ps1
        Category: infrastructure/windows/security
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/infrastructure/windows/security/Get-SecurityEventAudit.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [Int32]$Hours,
        [Parameter()]
        [Int32]$Days,
        [Parameter()]
        [String]$EventTypes,
        [Parameter()]
        [String]$ComputerName,
        [Parameter()]
        [Int32]$MaxEvents,
        [Parameter()]
        [switch]$ExportHTML,
        [Parameter()]
        [switch]$ExportCSV,
        [Parameter()]
        [Int32]$Threshold,
        [Parameter()]
        [switch]$ShowSummaryOnly,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'infrastructure/windows/security/Get-SecurityEventAudit.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/infrastructure/windows/security/Get-SecurityEventAudit.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Get-ServiceAccountAudit {
    <#
    .SYNOPSIS
        Audits service accounts in Active Directory for security and compliance.
    .DESCRIPTION
        Proxies to scripts/infrastructure/windows/active-directory/Get-ServiceAccountAudit.ps1.
        Original category: infrastructure/windows/active-directory
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Get-ServiceAccountAudit -Verbose
    .NOTES
        Backing script: scripts/infrastructure/windows/active-directory/Get-ServiceAccountAudit.ps1
        Category: infrastructure/windows/active-directory
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/infrastructure/windows/active-directory/Get-ServiceAccountAudit.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [String]$OutputPath,
        [Parameter()]
        [switch]$IncludeSPNAnalysis,
        [Parameter()]
        [switch]$CheckKerberosDelegation,
        [Parameter()]
        [switch]$ExportToCSV,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'infrastructure/windows/active-directory/Get-ServiceAccountAudit.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/infrastructure/windows/active-directory/Get-ServiceAccountAudit.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Get-SharedMailboxReport {
    <#
    .SYNOPSIS
        Audits Exchange Online shared mailboxes for usage, permissions, and compliance.
    .DESCRIPTION
        Proxies to scripts/collaboration/microsoft365/exchange-online/Get-SharedMailboxReport.ps1.
        Original category: collaboration/microsoft365/exchange-online
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Get-SharedMailboxReport -Verbose
    .NOTES
        Backing script: scripts/collaboration/microsoft365/exchange-online/Get-SharedMailboxReport.ps1
        Category: collaboration/microsoft365/exchange-online
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/collaboration/microsoft365/exchange-online/Get-SharedMailboxReport.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [switch]$IncludePermissions,
        [Parameter()]
        [switch]$CheckInactive,
        [Parameter()]
        [Int32]$InactivityDays,
        [Parameter()]
        [switch]$ExportHTML,
        [Parameter()]
        [switch]$ExportCSV,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'collaboration/microsoft365/exchange-online/Get-SharedMailboxReport.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/collaboration/microsoft365/exchange-online/Get-SharedMailboxReport.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Get-SoftwareInventory {
    <#
    .SYNOPSIS
        Generates comprehensive software inventory report for Windows systems.
    .DESCRIPTION
        Proxies to scripts/utilities/Get-SoftwareInventory.ps1.
        Original category: utilities
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Get-SoftwareInventory -Verbose
    .NOTES
        Backing script: scripts/utilities/Get-SoftwareInventory.ps1
        Category: utilities
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/utilities/Get-SoftwareInventory.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [switch]$IncludeUpdates,
        [Parameter()]
        [switch]$IncludeFeatures,
        [Parameter()]
        [switch]$IncludeStoreApps,
        [Parameter()]
        [switch]$IncludeWinget,
        [Parameter()]
        [switch]$ExportHTML,
        [Parameter()]
        [switch]$ExportCSV,
        [Parameter()]
        [switch]$ExportJSON,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'utilities/Get-SoftwareInventory.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/utilities/Get-SoftwareInventory.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Get-SoftwareLicenseCompliance {
    <#
    .SYNOPSIS
        Audits installed software and checks license compliance status.
    .DESCRIPTION
        Proxies to scripts/security/compliance/frameworks/Get-SoftwareLicenseCompliance.ps1.
        Original category: security/compliance/frameworks
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Get-SoftwareLicenseCompliance -Verbose
    .NOTES
        Backing script: scripts/security/compliance/frameworks/Get-SoftwareLicenseCompliance.ps1
        Category: security/compliance/frameworks
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/security/compliance/frameworks/Get-SoftwareLicenseCompliance.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [String]$OutputFormat,
        [Parameter()]
        [String]$OutputPath,
        [Parameter()]
        [Boolean]$CheckLicenseKeys,
        [Parameter()]
        [Boolean]$HighlightUnlicensed,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'security/compliance/frameworks/Get-SoftwareLicenseCompliance.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/security/compliance/frameworks/Get-SoftwareLicenseCompliance.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Get-SQLServerHealth {
    <#
    .SYNOPSIS
        Performs comprehensive health check of SQL Server instances.
    .DESCRIPTION
        Proxies to scripts/data/databases/Get-SQLServerHealth.ps1.
        Original category: data/databases
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Get-SQLServerHealth -Verbose
    .NOTES
        Backing script: scripts/data/databases/Get-SQLServerHealth.ps1
        Category: data/databases
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/data/databases/Get-SQLServerHealth.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [String]$ServerInstance,
        [Parameter()]
        [String]$Database,
        [Parameter()]
        [switch]$IncludePerformance,
        [Parameter()]
        [switch]$CheckBackups,
        [Parameter()]
        [switch]$ExportHTML,
        [Parameter()]
        [switch]$ExportCSV,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'data/databases/Get-SQLServerHealth.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/data/databases/Get-SQLServerHealth.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Get-SystemHealthCheck {
    <#
    .SYNOPSIS
        Performs a comprehensive system health check on Windows devices.
    .DESCRIPTION
        Proxies to scripts/security/monitoring/Get-SystemHealthCheck.ps1.
        Original category: security/monitoring
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Get-SystemHealthCheck -Verbose
    .NOTES
        Backing script: scripts/security/monitoring/Get-SystemHealthCheck.ps1
        Category: security/monitoring
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/security/monitoring/Get-SystemHealthCheck.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [String]$OutputFormat,
        [Parameter()]
        [String]$OutputPath,
        [Parameter()]
        [String[]]$CheckServices,
        [Parameter()]
        [Int32]$DiskThresholdPercent,
        [Parameter()]
        [Int32]$MemoryThresholdPercent,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'security/monitoring/Get-SystemHealthCheck.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/security/monitoring/Get-SystemHealthCheck.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Get-SystemResourceTrends {
    <#
    .SYNOPSIS
        Samples current system resource utilization (CPU, memory, disk).
    .DESCRIPTION
        Proxies to scripts/security/monitoring/Get-SystemResourceTrends.ps1.
        Original category: security/monitoring
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Get-SystemResourceTrends -Verbose
    .NOTES
        Backing script: scripts/security/monitoring/Get-SystemResourceTrends.ps1
        Category: security/monitoring
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/security/monitoring/Get-SystemResourceTrends.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [switch]$ExportHTML,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'security/monitoring/Get-SystemResourceTrends.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/security/monitoring/Get-SystemResourceTrends.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Get-TeamsReport {
    <#
    .SYNOPSIS
        Generates comprehensive Microsoft Teams usage and compliance report.
    .DESCRIPTION
        Proxies to scripts/collaboration/microsoft365/teams/Get-TeamsReport.ps1.
        Original category: collaboration/microsoft365/teams
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Get-TeamsReport -Verbose
    .NOTES
        Backing script: scripts/collaboration/microsoft365/teams/Get-TeamsReport.ps1
        Category: collaboration/microsoft365/teams
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/collaboration/microsoft365/teams/Get-TeamsReport.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [switch]$IncludeGuests,
        [Parameter()]
        [switch]$CheckOwnership,
        [Parameter()]
        [switch]$CheckArchived,
        [Parameter()]
        [switch]$ExportHTML,
        [Parameter()]
        [switch]$ExportCSV,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'collaboration/microsoft365/teams/Get-TeamsReport.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/collaboration/microsoft365/teams/Get-TeamsReport.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Get-USBDeviceAudit {
    <#
    .SYNOPSIS
        Audits USB device connections and usage on Windows devices.
    .DESCRIPTION
        Proxies to scripts/security/compliance/frameworks/Get-USBDeviceAudit.ps1.
        Original category: security/compliance/frameworks
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Get-USBDeviceAudit -Verbose
    .NOTES
        Backing script: scripts/security/compliance/frameworks/Get-USBDeviceAudit.ps1
        Category: security/compliance/frameworks
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/security/compliance/frameworks/Get-USBDeviceAudit.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [String]$OutputFormat,
        [Parameter()]
        [String]$OutputPath,
        [Parameter()]
        [Boolean]$IncludeHistory,
        [Parameter()]
        [Boolean]$HighlightUnauthorized,
        [Parameter()]
        [String[]]$AuthorizedVendors,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'security/compliance/frameworks/Get-USBDeviceAudit.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/security/compliance/frameworks/Get-USBDeviceAudit.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Get-UserAccessReport {
    <#
    .SYNOPSIS
        Generates comprehensive user access and permissions report.
    .DESCRIPTION
        Proxies to scripts/infrastructure/windows/user-management/Get-UserAccessReport.ps1.
        Original category: infrastructure/windows/user-management
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Get-UserAccessReport -Verbose
    .NOTES
        Backing script: scripts/infrastructure/windows/user-management/Get-UserAccessReport.ps1
        Category: infrastructure/windows/user-management
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/infrastructure/windows/user-management/Get-UserAccessReport.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [String]$Username,
        [Parameter()]
        [switch]$IncludeFileShares,
        [Parameter()]
        [switch]$CheckLocalAdmin,
        [Parameter()]
        [String]$OutputPath,
        [Parameter()]
        [switch]$ExportToCSV,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'infrastructure/windows/user-management/Get-UserAccessReport.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/infrastructure/windows/user-management/Get-UserAccessReport.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Get-UserDeviceAffinity {
    <#
    .SYNOPSIS
        Generates a user-device affinity report for Intune.
    .DESCRIPTION
        Proxies to scripts/endpoints/intune/reporting/Get-UserDeviceAffinity.ps1.
        Original category: endpoints/intune/reporting
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Get-UserDeviceAffinity -Verbose
    .NOTES
        Backing script: scripts/endpoints/intune/reporting/Get-UserDeviceAffinity.ps1
        Category: endpoints/intune/reporting
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/intune/reporting/Get-UserDeviceAffinity.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/intune/reporting/Get-UserDeviceAffinity.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/intune/reporting/Get-UserDeviceAffinity.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Get-UserLockoutReport {
    <#
    .SYNOPSIS
        Reports on user account lockouts and analyzes lockout sources.
    .DESCRIPTION
        Proxies to scripts/infrastructure/windows/user-management/Get-UserLockoutReport.ps1.
        Original category: infrastructure/windows/user-management
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Get-UserLockoutReport -Verbose
    .NOTES
        Backing script: scripts/infrastructure/windows/user-management/Get-UserLockoutReport.ps1
        Category: infrastructure/windows/user-management
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/infrastructure/windows/user-management/Get-UserLockoutReport.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [Int32]$Hours,
        [Parameter()]
        [Int32]$Days,
        [Parameter()]
        [String]$Username,
        [Parameter()]
        [switch]$IncludeSource,
        [Parameter()]
        [switch]$DetectBruteForce,
        [Parameter()]
        [switch]$ExportHTML,
        [Parameter()]
        [switch]$ExportCSV,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'infrastructure/windows/user-management/Get-UserLockoutReport.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/infrastructure/windows/user-management/Get-UserLockoutReport.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Get-UserMailboxPermissions {
    <#
    .SYNOPSIS
        Displays all mailbox permissions and delegates for a specific user.
    .DESCRIPTION
        Proxies to scripts/collaboration/microsoft365/exchange-online/Get-UserMailboxPermissions.ps1.
        Original category: collaboration/microsoft365/exchange-online
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Get-UserMailboxPermissions -Verbose
    .NOTES
        Backing script: scripts/collaboration/microsoft365/exchange-online/Get-UserMailboxPermissions.ps1
        Category: collaboration/microsoft365/exchange-online
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/collaboration/microsoft365/exchange-online/Get-UserMailboxPermissions.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(Mandatory)]
        [String]$UserEmail,
        [Parameter()]
        [switch]$IncludeFolderPermissions,
        [Parameter()]
        [switch]$ExportReport,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'collaboration/microsoft365/exchange-online/Get-UserMailboxPermissions.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/collaboration/microsoft365/exchange-online/Get-UserMailboxPermissions.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Get-UserMailRules {
    <#
    .SYNOPSIS
        Displays all email forwarding rules and inbox rules for a specific user.
    .DESCRIPTION
        Proxies to scripts/collaboration/microsoft365/exchange-online/Get-UserMailRules.ps1.
        Original category: collaboration/microsoft365/exchange-online
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Get-UserMailRules -Verbose
    .NOTES
        Backing script: scripts/collaboration/microsoft365/exchange-online/Get-UserMailRules.ps1
        Category: collaboration/microsoft365/exchange-online
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/collaboration/microsoft365/exchange-online/Get-UserMailRules.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(Mandatory)]
        [String]$UserEmail,
        [Parameter()]
        [switch]$ShowDisabledRules,
        [Parameter()]
        [switch]$ExportReport,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'collaboration/microsoft365/exchange-online/Get-UserMailRules.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/collaboration/microsoft365/exchange-online/Get-UserMailRules.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Get-VMBackupCompliance {
    <#
    .SYNOPSIS
        Monitors Azure VM backup status and compliance with backup policies.
    .DESCRIPTION
        Proxies to scripts/cloud/azure/compute/Azure-VirtualMachines/Get-VMBackupCompliance.ps1.
        Original category: cloud/azure/compute/Azure-VirtualMachines
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Get-VMBackupCompliance -Verbose
    .NOTES
        Backing script: scripts/cloud/azure/compute/Azure-VirtualMachines/Get-VMBackupCompliance.ps1
        Category: cloud/azure/compute/Azure-VirtualMachines
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/cloud/azure/compute/Azure-VirtualMachines/Get-VMBackupCompliance.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [String]$SubscriptionId,
        [Parameter()]
        [String]$ResourceGroupName,
        [Parameter()]
        [Int32]$BackupAgeThreshold,
        [Parameter()]
        [switch]$IncludeBackupJobs,
        [Parameter()]
        [String]$OutputFormat,
        [Parameter()]
        [String]$OutputPath,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'cloud/azure/compute/Azure-VirtualMachines/Get-VMBackupCompliance.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/cloud/azure/compute/Azure-VirtualMachines/Get-VMBackupCompliance.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Get-VMSecurityConfig {
    <#
    .SYNOPSIS
        Audits Azure VM security configuration and compliance.
    .DESCRIPTION
        Proxies to scripts/cloud/azure/compute/Azure-VirtualMachines/Get-VMSecurityConfig.ps1.
        Original category: cloud/azure/compute/Azure-VirtualMachines
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Get-VMSecurityConfig -Verbose
    .NOTES
        Backing script: scripts/cloud/azure/compute/Azure-VirtualMachines/Get-VMSecurityConfig.ps1
        Category: cloud/azure/compute/Azure-VirtualMachines
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/cloud/azure/compute/Azure-VirtualMachines/Get-VMSecurityConfig.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [String]$SubscriptionId,
        [Parameter()]
        [String]$ResourceGroupName,
        [Parameter()]
        [switch]$CheckEncryption,
        [Parameter()]
        [switch]$CheckNetworkSecurity,
        [Parameter()]
        [String]$OutputFormat,
        [Parameter()]
        [String]$OutputPath,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'cloud/azure/compute/Azure-VirtualMachines/Get-VMSecurityConfig.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/cloud/azure/compute/Azure-VirtualMachines/Get-VMSecurityConfig.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Get-VMwareHealth {
    <#
    .SYNOPSIS
        VMware vSphere/ESXi health monitoring.
    .DESCRIPTION
        Proxies to scripts/infrastructure/virtualization/Get-VMwareHealth.ps1.
        Original category: infrastructure/virtualization
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Get-VMwareHealth -Verbose
    .NOTES
        Backing script: scripts/infrastructure/virtualization/Get-VMwareHealth.ps1
        Category: infrastructure/virtualization
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/infrastructure/virtualization/Get-VMwareHealth.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [String]$vCenter,
        [Parameter()]
        [PSCredential]$Credential,
        [Parameter()]
        [switch]$IncludeVMs,
        [Parameter()]
        [switch]$ExportHTML,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'infrastructure/virtualization/Get-VMwareHealth.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/infrastructure/virtualization/Get-VMwareHealth.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Get-WindowsUpdateCompliance {
    <#
    .SYNOPSIS
        Generates a Defender protection state report for Windows devices, including AutoPatch status.
    .DESCRIPTION
        Proxies to scripts/endpoints/intune/reporting/Get-WindowsUpdateCompliance.ps1.
        Original category: endpoints/intune/reporting
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Get-WindowsUpdateCompliance -Verbose
    .NOTES
        Backing script: scripts/endpoints/intune/reporting/Get-WindowsUpdateCompliance.ps1
        Category: endpoints/intune/reporting
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/intune/reporting/Get-WindowsUpdateCompliance.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [String]$ExportFormat,
        [Parameter()]
        [switch]$ShowNonCompliantOnly,
        [Parameter()]
        [switch]$IncludeAutoPatchInfo,
        [Parameter()]
        [Int32]$DaysOutdated,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/intune/reporting/Get-WindowsUpdateCompliance.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/intune/reporting/Get-WindowsUpdateCompliance.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Get-WingetUpdateCompliance {
    <#
    .SYNOPSIS
        TEMPLATE: winget-managed application update compliance report (SAMPLE DATA).
    .DESCRIPTION
        Proxies to scripts/endpoints/intune/reporting/Get-WingetUpdateCompliance.ps1.
        Original category: endpoints/intune/reporting
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Get-WingetUpdateCompliance -Verbose
    .NOTES
        Backing script: scripts/endpoints/intune/reporting/Get-WingetUpdateCompliance.ps1
        Category: endpoints/intune/reporting
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/intune/reporting/Get-WingetUpdateCompliance.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [String]$TenantId,
        [Parameter()]
        [String]$ApplicationFilter,
        [Parameter()]
        [switch]$IncludeUpToDate,
        [Parameter()]
        [switch]$ExportHTML,
        [Parameter()]
        [switch]$ExportCSV,
        [Parameter()]
        [Int32]$Top,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/intune/reporting/Get-WingetUpdateCompliance.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/intune/reporting/Get-WingetUpdateCompliance.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Install-TeamsAVD {
    <#
    .SYNOPSIS
        Installs or updates Microsoft Teams and WebView2 for Azure Virtual Desktop (AVD) gold images.
    .DESCRIPTION
        Proxies to scripts/cloud/azure/avd/Install-TeamsAVD.ps1.
        Original category: cloud/azure/avd
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Install-TeamsAVD -Verbose
    .NOTES
        Backing script: scripts/cloud/azure/avd/Install-TeamsAVD.ps1
        Category: cloud/azure/avd
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/cloud/azure/avd/Install-TeamsAVD.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(Mandatory)]
        [switch]$AcceptEULA,
        [Parameter()]
        [switch]$SkipUserCleanup,
        [Parameter()]
        [String]$LogPath,
        [Parameter()]
        [switch]$Force,
        [Parameter()]
        [switch]$SkipSignatureCheck,
        [Parameter()]
        [switch]$InstallWebRtcRedirector,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'cloud/azure/avd/Install-TeamsAVD.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/cloud/azure/avd/Install-TeamsAVD.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Install-TeamsAVDTests {
    <#
    .SYNOPSIS
        Pester test suite for the Install-TeamsAVD.ps1 script.
    .DESCRIPTION
        Proxies to scripts/cloud/azure/avd/Install-TeamsAVD.Tests.ps1.
        Original category: cloud/azure/avd
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Install-TeamsAVDTests -Verbose
    .NOTES
        Backing script: scripts/cloud/azure/avd/Install-TeamsAVD.Tests.ps1
        Category: cloud/azure/avd
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/cloud/azure/avd/Install-TeamsAVD.Tests.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'cloud/azure/avd/Install-TeamsAVD.Tests.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/cloud/azure/avd/Install-TeamsAVD.Tests.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-AdobeRum {
    <#
    .SYNOPSIS
        Remediate Adobe Creative Cloud app updates via Remote Update Manager
    .DESCRIPTION
        Proxies to scripts/endpoints/devices/adobe-rum/remediate.ps1.
        Original category: endpoints/devices/adobe-rum
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-AdobeRum -Verbose
    .NOTES
        Backing script: scripts/endpoints/devices/adobe-rum/remediate.ps1
        Category: endpoints/devices/adobe-rum
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/devices/adobe-rum/remediate.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/devices/adobe-rum/remediate.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/devices/adobe-rum/remediate.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-AnalyzeBuildPerformance {
    <#
    .SYNOPSIS
        Analyzes build performance trends and identifies bottlenecks.
    .DESCRIPTION
        Proxies to scripts/automation/cicd/Analyze-BuildPerformance.ps1.
        Original category: automation/cicd
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-AnalyzeBuildPerformance -Verbose
    .NOTES
        Backing script: scripts/automation/cicd/Analyze-BuildPerformance.ps1
        Category: automation/cicd
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/automation/cicd/Analyze-BuildPerformance.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(Mandatory)]
        [String]$Platform,
        [Parameter(Mandatory)]
        [String]$DataSource,
        [Parameter()]
        [Int32]$DaysToAnalyze,
        [Parameter()]
        [String]$OutputFormat,
        [Parameter()]
        [String]$OutputPath,
        [Parameter()]
        [switch]$IdentifyRegressions,
        [Parameter()]
        [Int32]$RegressionThreshold,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'automation/cicd/Analyze-BuildPerformance.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/automation/cicd/Analyze-BuildPerformance.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-APDetection {
    <#
    .SYNOPSIS
        Detect Windows Update policy drift (Autopatch V2)
    .DESCRIPTION
        Proxies to scripts/endpoints/devices/autopatch/V2/AP_Detection.ps1.
        Original category: endpoints/devices/autopatch/V2
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-APDetection -Verbose
    .NOTES
        Backing script: scripts/endpoints/devices/autopatch/V2/AP_Detection.ps1
        Category: endpoints/devices/autopatch/V2
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/devices/autopatch/V2/AP_Detection.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/devices/autopatch/V2/AP_Detection.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/devices/autopatch/V2/AP_Detection.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-APRemediation {
    <#
    .SYNOPSIS
        Remediate Windows Update policy drift (Autopatch V2)
    .DESCRIPTION
        Proxies to scripts/endpoints/devices/autopatch/V2/AP_Remediation.ps1.
        Original category: endpoints/devices/autopatch/V2
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-APRemediation -Verbose
    .NOTES
        Backing script: scripts/endpoints/devices/autopatch/V2/AP_Remediation.ps1
        Category: endpoints/devices/autopatch/V2
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/devices/autopatch/V2/AP_Remediation.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/devices/autopatch/V2/AP_Remediation.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/devices/autopatch/V2/AP_Remediation.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-DeviceBulkActions {
    <#
    .SYNOPSIS
        Performs bulk actions on Intune-managed devices.
    .DESCRIPTION
        Proxies to scripts/endpoints/intune/maintenance/Invoke-DeviceBulkActions.ps1.
        Original category: endpoints/intune/maintenance
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-DeviceBulkActions -Verbose
    .NOTES
        Backing script: scripts/endpoints/intune/maintenance/Invoke-DeviceBulkActions.ps1
        Category: endpoints/intune/maintenance
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/intune/maintenance/Invoke-DeviceBulkActions.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(Mandatory)]
        [String]$Action,
        [Parameter()]
        [String[]]$DeviceNames,
        [Parameter()]
        [String]$DeviceFilter,
        [Parameter()]
        [String]$GroupName,
        [Parameter()]
        [switch]$NonCompliantOnly,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/intune/maintenance/Invoke-DeviceBulkActions.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/intune/maintenance/Invoke-DeviceBulkActions.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-DisableWindowsUpdateAccess {
    <#
    .SYNOPSIS
        Remove the NoAutoUpdate Windows Update policy (DisableWindowsUpdateAccess pair)
    .DESCRIPTION
        Proxies to scripts/endpoints/devices/autopatch/V1/DisableWindowsUpdateAccess/Remediate.ps1.
        Original category: endpoints/devices/autopatch/V1/DisableWindowsUpdateAccess
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-DisableWindowsUpdateAccess -Verbose
    .NOTES
        Backing script: scripts/endpoints/devices/autopatch/V1/DisableWindowsUpdateAccess/Remediate.ps1
        Category: endpoints/devices/autopatch/V1/DisableWindowsUpdateAccess
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/devices/autopatch/V1/DisableWindowsUpdateAccess/Remediate.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/devices/autopatch/V1/DisableWindowsUpdateAccess/Remediate.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/devices/autopatch/V1/DisableWindowsUpdateAccess/Remediate.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-DoNotConnectToWindowsUpdateInternetLocations {
    <#
    .SYNOPSIS
        Remove the DoNotConnectToWindowsUpdateInternetLocations Windows Update policy
    .DESCRIPTION
        Proxies to scripts/endpoints/devices/autopatch/V1/DoNotConnectToWindowsUpdateInternetLocations/Remediate.ps1.
        Original category: endpoints/devices/autopatch/V1/DoNotConnectToWindowsUpdateInternetLocations
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-DoNotConnectToWindowsUpdateInternetLocations -Verbose
    .NOTES
        Backing script: scripts/endpoints/devices/autopatch/V1/DoNotConnectToWindowsUpdateInternetLocations/Remediate.ps1
        Category: endpoints/devices/autopatch/V1/DoNotConnectToWindowsUpdateInternetLocations
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/devices/autopatch/V1/DoNotConnectToWindowsUpdateInternetLocations/Remediate.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/devices/autopatch/V1/DoNotConnectToWindowsUpdateInternetLocations/Remediate.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/devices/autopatch/V1/DoNotConnectToWindowsUpdateInternetLocations/Remediate.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-Drivers {
    <#
    .SYNOPSIS
        Apply the AMD Radeon driver block
    .DESCRIPTION
        Proxies to scripts/endpoints/devices/drivers/remediate.ps1.
        Original category: endpoints/devices/drivers
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-Drivers -Verbose
    .NOTES
        Backing script: scripts/endpoints/devices/drivers/remediate.ps1
        Category: endpoints/devices/drivers
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/devices/drivers/remediate.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/devices/drivers/remediate.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/devices/drivers/remediate.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-FixBitLockerKeyBackup {
    <#
    .SYNOPSIS
        Used to backup BitLocker recovery key to Azure Active Directory (AAD).
    .DESCRIPTION
        Proxies to scripts/endpoints/devices/bitlocker/Fix_BitLockerKeyBackup.ps1.
        Original category: endpoints/devices/bitlocker
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-FixBitLockerKeyBackup -Verbose
    .NOTES
        Backing script: scripts/endpoints/devices/bitlocker/Fix_BitLockerKeyBackup.ps1
        Category: endpoints/devices/bitlocker
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/devices/bitlocker/Fix_BitLockerKeyBackup.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/devices/bitlocker/Fix_BitLockerKeyBackup.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/devices/bitlocker/Fix_BitLockerKeyBackup.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-GenerateWingetScripts {
    <#
    .SYNOPSIS
        Helper script to generate winget update script pairs (detect/remediate) for new applications.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/_generate-winget-scripts.ps1.
        Original category: endpoints/remediation/winget
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-GenerateWingetScripts -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/_generate-winget-scripts.ps1
        Category: endpoints/remediation/winget
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/_generate-winget-scripts.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [String]$WingetId,
        [Parameter()]
        [String]$Category,
        [Parameter()]
        [String]$FolderName,
        [Parameter()]
        [Boolean]$ForceClose,
        [Parameter()]
        [Int32]$NotifySeconds,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/_generate-winget-scripts.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/_generate-winget-scripts.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-ManageFirewallRules {
    <#
    .SYNOPSIS
        Audits and manages Windows Firewall rules with security compliance checks.
    .DESCRIPTION
        Proxies to scripts/infrastructure/windows/security/Manage-FirewallRules.ps1.
        Original category: infrastructure/windows/security
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-ManageFirewallRules -Verbose
    .NOTES
        Backing script: scripts/infrastructure/windows/security/Manage-FirewallRules.ps1
        Category: infrastructure/windows/security
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/infrastructure/windows/security/Manage-FirewallRules.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(Mandatory)]
        [String]$Action,
        [Parameter()]
        [String]$RuleName,
        [Parameter()] [Alias('Profile')]
        [String]$ProfilePath,
        [Parameter()]
        [switch]$ShowDisabled,
        [Parameter()]
        [String]$ExportPath,
        [Parameter()]
        [switch]$IdentifyRisks,
        [Parameter()]
        [String]$RemoteAddress,
        [Parameter()]
        [String]$Protocol,
        [Parameter()]
        [switch]$ExportHTML,
        [Parameter()]
        [switch]$ExportCSV,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
        if ($boundParams.ContainsKey('ProfilePath')) { $boundParams['Profile'] = $boundParams['ProfilePath']; $null = $boundParams.Remove('ProfilePath') }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'infrastructure/windows/security/Manage-FirewallRules.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/infrastructure/windows/security/Manage-FirewallRules.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-ManageQuarantinedEmails {
    <#
    .SYNOPSIS
        Interactive tool for managing quarantined emails for a specific M365 user.
    .DESCRIPTION
        Proxies to scripts/collaboration/microsoft365/exchange-online/Manage-QuarantinedEmails.ps1.
        Original category: collaboration/microsoft365/exchange-online
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-ManageQuarantinedEmails -Verbose
    .NOTES
        Backing script: scripts/collaboration/microsoft365/exchange-online/Manage-QuarantinedEmails.ps1
        Category: collaboration/microsoft365/exchange-online
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/collaboration/microsoft365/exchange-online/Manage-QuarantinedEmails.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [String]$UserEmail,
        [Parameter()]
        [Int32]$Days,
        [Parameter()]
        [switch]$AutoConnect,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'collaboration/microsoft365/exchange-online/Manage-QuarantinedEmails.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/collaboration/microsoft365/exchange-online/Manage-QuarantinedEmails.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-ManageQuarantinedEmailsTests {
    <#
    .SYNOPSIS
        Pester test suite for the Manage-QuarantinedEmails.ps1 script.
    .DESCRIPTION
        Proxies to scripts/collaboration/microsoft365/exchange-online/Manage-QuarantinedEmails.Tests.ps1.
        Original category: collaboration/microsoft365/exchange-online
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-ManageQuarantinedEmailsTests -Verbose
    .NOTES
        Backing script: scripts/collaboration/microsoft365/exchange-online/Manage-QuarantinedEmails.Tests.ps1
        Category: collaboration/microsoft365/exchange-online
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/collaboration/microsoft365/exchange-online/Manage-QuarantinedEmails.Tests.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'collaboration/microsoft365/exchange-online/Manage-QuarantinedEmails.Tests.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/collaboration/microsoft365/exchange-online/Manage-QuarantinedEmails.Tests.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-ManageRestorePoints {
    <#
    .SYNOPSIS
        Manages system restore points on Windows servers and workstations.
    .DESCRIPTION
        Proxies to scripts/infrastructure/windows/backup-recovery/Manage-RestorePoints.ps1.
        Original category: infrastructure/windows/backup-recovery
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-ManageRestorePoints -Verbose
    .NOTES
        Backing script: scripts/infrastructure/windows/backup-recovery/Manage-RestorePoints.ps1
        Category: infrastructure/windows/backup-recovery
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/infrastructure/windows/backup-recovery/Manage-RestorePoints.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [String]$Action,
        [Parameter()]
        [String]$Description,
        [Parameter()]
        [String]$RestorePointType,
        [Parameter()]
        [Int32]$RetentionDays,
        [Parameter()]
        [switch]$Force,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'infrastructure/windows/backup-recovery/Manage-RestorePoints.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/infrastructure/windows/backup-recovery/Manage-RestorePoints.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-MonitorAzureAPIManagement {
    <#
    .SYNOPSIS
        Monitors Azure API Management service health, performance, and API analytics.
    .DESCRIPTION
        Proxies to scripts/data/api/Monitor-AzureAPIManagement.ps1.
        Original category: data/api
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-MonitorAzureAPIManagement -Verbose
    .NOTES
        Backing script: scripts/data/api/Monitor-AzureAPIManagement.ps1
        Category: data/api
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/data/api/Monitor-AzureAPIManagement.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(Mandatory)]
        [String]$SubscriptionId,
        [Parameter(Mandatory)]
        [String]$ResourceGroupName,
        [Parameter(Mandatory)]
        [String]$ServiceName,
        [Parameter()]
        [Int32]$DaysToAnalyze,
        [Parameter()]
        [String]$OutputFormat,
        [Parameter()]
        [String]$OutputPath,
        [Parameter()]
        [switch]$IncludeAPIDetails,
        [Parameter()]
        [switch]$IncludeBackendHealth,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'data/api/Monitor-AzureAPIManagement.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/data/api/Monitor-AzureAPIManagement.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-MonitorAzureDevOpsPipelines {
    <#
    .SYNOPSIS
        Monitors Azure DevOps pipeline health, performance, and failures.
    .DESCRIPTION
        Proxies to scripts/automation/cicd/Monitor-AzureDevOpsPipelines.ps1.
        Original category: automation/cicd
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-MonitorAzureDevOpsPipelines -Verbose
    .NOTES
        Backing script: scripts/automation/cicd/Monitor-AzureDevOpsPipelines.ps1
        Category: automation/cicd
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/automation/cicd/Monitor-AzureDevOpsPipelines.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(Mandatory)]
        [String]$Organization,
        [Parameter(Mandatory)]
        [String]$Project,
        [Parameter()]
        [Int32]$DaysToAnalyze,
        [Parameter()]
        [String]$OutputFormat,
        [Parameter()]
        [String]$OutputPath,
        [Parameter()]
        [switch]$IncludeAgentPools,
        [Parameter()]
        [switch]$IncludeReleases,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'automation/cicd/Monitor-AzureDevOpsPipelines.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/automation/cicd/Monitor-AzureDevOpsPipelines.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-MonitorAzureKeyVaults {
    <#
    .SYNOPSIS
        Monitors Azure Key Vault security, compliance, and secret expiration.
    .DESCRIPTION
        Proxies to scripts/cloud/azure/keyvault/Azure-KeyVault/Monitor-AzureKeyVaults.ps1.
        Original category: cloud/azure/keyvault/Azure-KeyVault
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-MonitorAzureKeyVaults -Verbose
    .NOTES
        Backing script: scripts/cloud/azure/keyvault/Azure-KeyVault/Monitor-AzureKeyVaults.ps1
        Category: cloud/azure/keyvault/Azure-KeyVault
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/cloud/azure/keyvault/Azure-KeyVault/Monitor-AzureKeyVaults.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [String]$SubscriptionId,
        [Parameter()]
        [Int32]$ExpirationWarningDays,
        [Parameter()]
        [switch]$CheckCompliance,
        [Parameter()]
        [String]$OutputFormat,
        [Parameter()]
        [String]$OutputPath,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'cloud/azure/keyvault/Azure-KeyVault/Monitor-AzureKeyVaults.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/cloud/azure/keyvault/Azure-KeyVault/Monitor-AzureKeyVaults.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-MonitorAzureResources {
    <#
    .SYNOPSIS
        Comprehensive Azure resource health and cost monitoring across subscriptions.
    .DESCRIPTION
        Proxies to scripts/cloud/azure/core/Monitor-AzureResources.ps1.
        Original category: cloud/azure/core
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-MonitorAzureResources -Verbose
    .NOTES
        Backing script: scripts/cloud/azure/core/Monitor-AzureResources.ps1
        Category: cloud/azure/core
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/cloud/azure/core/Monitor-AzureResources.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [String]$SubscriptionId,
        [Parameter()]
        [Int32]$DaysToAnalyze,
        [Parameter()]
        [switch]$IncludeCostAnalysis,
        [Parameter()]
        [switch]$IncludeOrphanedResources,
        [Parameter()]
        [String]$OutputFormat,
        [Parameter()]
        [String]$OutputPath,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'cloud/azure/core/Monitor-AzureResources.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/cloud/azure/core/Monitor-AzureResources.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-MonitorGitHubActions {
    <#
    .SYNOPSIS
        Monitors GitHub Actions workflow health and performance.
    .DESCRIPTION
        Proxies to scripts/automation/cicd/Monitor-GitHubActions.ps1.
        Original category: automation/cicd
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-MonitorGitHubActions -Verbose
    .NOTES
        Backing script: scripts/automation/cicd/Monitor-GitHubActions.ps1
        Category: automation/cicd
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/automation/cicd/Monitor-GitHubActions.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(Mandatory)]
        [String]$Owner,
        [Parameter(Mandatory)]
        [String]$Repository,
        [Parameter()]
        [String]$GitHubToken,
        [Parameter()]
        [Int32]$DaysToAnalyze,
        [Parameter()]
        [String]$OutputFormat,
        [Parameter()]
        [String]$OutputPath,
        [Parameter()]
        [switch]$IncludeRunners,
        [Parameter()]
        [switch]$IncludeBilling,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'automation/cicd/Monitor-GitHubActions.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/automation/cicd/Monitor-GitHubActions.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-MonitorGitLabCI {
    <#
    .SYNOPSIS
        Monitors GitLab CI/CD pipeline health and performance.
    .DESCRIPTION
        Proxies to scripts/automation/cicd/Monitor-GitLabCI.ps1.
        Original category: automation/cicd
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-MonitorGitLabCI -Verbose
    .NOTES
        Backing script: scripts/automation/cicd/Monitor-GitLabCI.ps1
        Category: automation/cicd
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/automation/cicd/Monitor-GitLabCI.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(Mandatory)]
        [String]$GitLabUrl,
        [Parameter(Mandatory)]
        [String]$ProjectId,
        [Parameter()]
        [String]$PrivateToken,
        [Parameter()]
        [Int32]$DaysToAnalyze,
        [Parameter()]
        [String]$OutputFormat,
        [Parameter()]
        [String]$OutputPath,
        [Parameter()]
        [switch]$IncludeRunners,
        [Parameter()]
        [switch]$IncludeDeployments,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'automation/cicd/Monitor-GitLabCI.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/automation/cicd/Monitor-GitLabCI.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-MonitorMongoDBHealth {
    <#
    .SYNOPSIS
        Generates a MongoDB health monitoring template (does not perform live monitoring).
    .DESCRIPTION
        Proxies to scripts/data/databases/Monitor-MongoDBHealth.ps1.
        Original category: data/databases
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-MonitorMongoDBHealth -Verbose
    .NOTES
        Backing script: scripts/data/databases/Monitor-MongoDBHealth.ps1
        Category: data/databases
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/data/databases/Monitor-MongoDBHealth.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'data/databases/Monitor-MongoDBHealth.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/data/databases/Monitor-MongoDBHealth.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-MonitorServerHealth {
    <#
    .SYNOPSIS
        Comprehensive real-time health monitoring for Windows Server 2016-2022 with optional advanced features.
    .DESCRIPTION
        Proxies to scripts/infrastructure/windows/monitoring/Monitor-ServerHealth.ps1.
        Original category: infrastructure/windows/monitoring
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-MonitorServerHealth -Verbose
    .NOTES
        Backing script: scripts/infrastructure/windows/monitoring/Monitor-ServerHealth.ps1
        Category: infrastructure/windows/monitoring
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/infrastructure/windows/monitoring/Monitor-ServerHealth.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [switch]$ExportReport,
        [Parameter()]
        [String]$AlertThreshold,
        [Parameter()]
        [String]$CheckServices,
        [Parameter()]
        [switch]$EmailReport,
        [Parameter()]
        [switch]$IncludeDiskIO,
        [Parameter()]
        [switch]$IncludeWindowsUpdate,
        [Parameter()]
        [switch]$IncludeSecurity,
        [Parameter()]
        [switch]$IncludeNetworkTests,
        [Parameter()]
        [switch]$IncludeApplications,
        [Parameter()]
        [switch]$IncludeCertificates,
        [Parameter()]
        [switch]$IncludeScheduledTasks,
        [Parameter()]
        [switch]$ExportJSON,
        [Parameter()]
        [switch]$ShowProgress,
        [Parameter()]
        [String]$SMTPServer,
        [Parameter()]
        [Int32]$SMTPPort,
        [Parameter()]
        [String]$EmailFrom,
        [Parameter()]
        [String[]]$EmailTo,
        [Parameter()]
        [String]$EmailSubject,
        [Parameter()]
        [switch]$UseSSL,
        [Parameter()]
        [PSCredential]$SMTPCredential,
        [Parameter()]
        [switch]$CheckIIS,
        [Parameter()]
        [switch]$CheckSQLServer,
        [Parameter()]
        [switch]$CheckHyperV,
        [Parameter()]
        [Int32]$CertificateWarningDays,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'infrastructure/windows/monitoring/Monitor-ServerHealth.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/infrastructure/windows/monitoring/Monitor-ServerHealth.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-NoAutoUpdate {
    <#
    .SYNOPSIS
        Remove the NoAutoUpdate Windows Update policy
    .DESCRIPTION
        Proxies to scripts/endpoints/devices/autopatch/V1/NoAutoUpdate/Remediate.ps1.
        Original category: endpoints/devices/autopatch/V1/NoAutoUpdate
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-NoAutoUpdate -Verbose
    .NOTES
        Backing script: scripts/endpoints/devices/autopatch/V1/NoAutoUpdate/Remediate.ps1
        Category: endpoints/devices/autopatch/V1/NoAutoUpdate
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/devices/autopatch/V1/NoAutoUpdate/Remediate.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/devices/autopatch/V1/NoAutoUpdate/Remediate.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/devices/autopatch/V1/NoAutoUpdate/Remediate.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-RemediationCheckApplicationCrashes {
    <#
    .SYNOPSIS
        Provides recommendations for application crash issues.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Invoke-RemediationCheckApplicationCrashes.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-RemediationCheckApplicationCrashes -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Invoke-RemediationCheckApplicationCrashes.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Invoke-RemediationCheckApplicationCrashes.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Invoke-RemediationCheckApplicationCrashes.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Invoke-RemediationCheckApplicationCrashes.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-RemediationCheckBatteryHealth {
    <#
    .SYNOPSIS
        Reports battery health degradation.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Invoke-RemediationCheckBatteryHealth.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-RemediationCheckBatteryHealth -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Invoke-RemediationCheckBatteryHealth.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Invoke-RemediationCheckBatteryHealth.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Invoke-RemediationCheckBatteryHealth.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Invoke-RemediationCheckBatteryHealth.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-RemediationCheckBootPerformance {
    <#
    .SYNOPSIS
        Optimizes boot performance by managing startup programs.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Invoke-RemediationCheckBootPerformance.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-RemediationCheckBootPerformance -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Invoke-RemediationCheckBootPerformance.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Invoke-RemediationCheckBootPerformance.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Invoke-RemediationCheckBootPerformance.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Invoke-RemediationCheckBootPerformance.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-RemediationCheckDefenderHealthStatus {
    <#
    .SYNOPSIS
        Remediates Windows Defender health issues.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/security/Invoke-RemediationCheckDefenderHealthStatus.ps1.
        Original category: endpoints/remediation/security
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-RemediationCheckDefenderHealthStatus -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/security/Invoke-RemediationCheckDefenderHealthStatus.ps1
        Category: endpoints/remediation/security
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/security/Invoke-RemediationCheckDefenderHealthStatus.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/security/Invoke-RemediationCheckDefenderHealthStatus.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/security/Invoke-RemediationCheckDefenderHealthStatus.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-RemediationCheckDeviceHealthScore {
    <#
    .SYNOPSIS
        Provides comprehensive health improvement plan.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Invoke-RemediationCheckDeviceHealthScore.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-RemediationCheckDeviceHealthScore -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Invoke-RemediationCheckDeviceHealthScore.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Invoke-RemediationCheckDeviceHealthScore.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Invoke-RemediationCheckDeviceHealthScore.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Invoke-RemediationCheckDeviceHealthScore.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-RemediationCheckDeviceUptime {
    <#
    .SYNOPSIS
        Notifies users about required system reboots.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Invoke-RemediationCheckDeviceUptime.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-RemediationCheckDeviceUptime -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Invoke-RemediationCheckDeviceUptime.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Invoke-RemediationCheckDeviceUptime.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Invoke-RemediationCheckDeviceUptime.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Invoke-RemediationCheckDeviceUptime.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-RemediationCheckDiskHealth {
    <#
    .SYNOPSIS
        Attempts to remediate disk health issues.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Invoke-RemediationCheckDiskHealth.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-RemediationCheckDiskHealth -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Invoke-RemediationCheckDiskHealth.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Invoke-RemediationCheckDiskHealth.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Invoke-RemediationCheckDiskHealth.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Invoke-RemediationCheckDiskHealth.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-RemediationCheckHardwareErrors {
    <#
    .SYNOPSIS
        Provides critical hardware failure guidance.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Invoke-RemediationCheckHardwareErrors.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-RemediationCheckHardwareErrors -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Invoke-RemediationCheckHardwareErrors.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Invoke-RemediationCheckHardwareErrors.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Invoke-RemediationCheckHardwareErrors.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Invoke-RemediationCheckHardwareErrors.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-RemediationCheckLocalAdminAccounts {
    <#
    .SYNOPSIS
        Remediates unauthorized local administrator accounts.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/security/Invoke-RemediationCheckLocalAdminAccounts.ps1.
        Original category: endpoints/remediation/security
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-RemediationCheckLocalAdminAccounts -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/security/Invoke-RemediationCheckLocalAdminAccounts.ps1
        Category: endpoints/remediation/security
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/security/Invoke-RemediationCheckLocalAdminAccounts.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/security/Invoke-RemediationCheckLocalAdminAccounts.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/security/Invoke-RemediationCheckLocalAdminAccounts.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-RemediationCheckMemoryDiagnostics {
    <#
    .SYNOPSIS
        Schedules Windows Memory Diagnostic on next reboot.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Invoke-RemediationCheckMemoryDiagnostics.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-RemediationCheckMemoryDiagnostics -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Invoke-RemediationCheckMemoryDiagnostics.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Invoke-RemediationCheckMemoryDiagnostics.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Invoke-RemediationCheckMemoryDiagnostics.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Invoke-RemediationCheckMemoryDiagnostics.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-RemediationCheckMicrosoftStoreAppsHealth {
    <#
    .SYNOPSIS
        Repairs Microsoft Store apps registration issues.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Invoke-RemediationCheckMicrosoftStoreAppsHealth.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-RemediationCheckMicrosoftStoreAppsHealth -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Invoke-RemediationCheckMicrosoftStoreAppsHealth.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Invoke-RemediationCheckMicrosoftStoreAppsHealth.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Invoke-RemediationCheckMicrosoftStoreAppsHealth.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Invoke-RemediationCheckMicrosoftStoreAppsHealth.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-RemediationCheckOutdatedCriticalApps {
    <#
    .SYNOPSIS
        Remediates outdated critical applications using winget.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/security/Invoke-RemediationCheckOutdatedCriticalApps.ps1.
        Original category: endpoints/remediation/security
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-RemediationCheckOutdatedCriticalApps -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/security/Invoke-RemediationCheckOutdatedCriticalApps.ps1
        Category: endpoints/remediation/security
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/security/Invoke-RemediationCheckOutdatedCriticalApps.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [Boolean]$EnableLogging,
        [Parameter()]
        [Int32]$MaxRetries,
        [Parameter()]
        [Boolean]$PriorityAppsOnly,
        [Parameter()]
        [Boolean]$UpdateOnlyIfNotRunning,
        [Parameter()]
        [Boolean]$ForceCloseApps,
        [Parameter()]
        [Int32]$TimeoutPerAppMinutes,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/security/Invoke-RemediationCheckOutdatedCriticalApps.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/security/Invoke-RemediationCheckOutdatedCriticalApps.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-RemediationCheckOutdatedCriticalAppsPriorityOnly {
    <#
    .SYNOPSIS
        Remediates ONLY priority/security-critical applications using winget.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/security/Invoke-RemediationCheckOutdatedCriticalAppsPriorityOnly.ps1.
        Original category: endpoints/remediation/security
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-RemediationCheckOutdatedCriticalAppsPriorityOnly -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/security/Invoke-RemediationCheckOutdatedCriticalAppsPriorityOnly.ps1
        Category: endpoints/remediation/security
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/security/Invoke-RemediationCheckOutdatedCriticalAppsPriorityOnly.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [Boolean]$EnableLogging,
        [Parameter()]
        [Boolean]$ForceCloseApps,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/security/Invoke-RemediationCheckOutdatedCriticalAppsPriorityOnly.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/security/Invoke-RemediationCheckOutdatedCriticalAppsPriorityOnly.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-RemediationCheckPageFileConfiguration {
    <#
    .SYNOPSIS
        Configures page file to system-managed.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Invoke-RemediationCheckPageFileConfiguration.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-RemediationCheckPageFileConfiguration -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Invoke-RemediationCheckPageFileConfiguration.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Invoke-RemediationCheckPageFileConfiguration.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Invoke-RemediationCheckPageFileConfiguration.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Invoke-RemediationCheckPageFileConfiguration.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-RemediationCheckSecurityBaseline {
    <#
    .SYNOPSIS
        Remediate security baseline drift
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/security/Invoke-RemediationCheckSecurityBaseline.ps1.
        Original category: endpoints/remediation/security
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-RemediationCheckSecurityBaseline -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/security/Invoke-RemediationCheckSecurityBaseline.ps1
        Category: endpoints/remediation/security
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/security/Invoke-RemediationCheckSecurityBaseline.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/security/Invoke-RemediationCheckSecurityBaseline.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/security/Invoke-RemediationCheckSecurityBaseline.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-RemediationCheckServiceFailures {
    <#
    .SYNOPSIS
        Attempts to restart failed critical services.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Invoke-RemediationCheckServiceFailures.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-RemediationCheckServiceFailures -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Invoke-RemediationCheckServiceFailures.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Invoke-RemediationCheckServiceFailures.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Invoke-RemediationCheckServiceFailures.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Invoke-RemediationCheckServiceFailures.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-RemediationCheckSharedFolders {
    <#
    .SYNOPSIS
        Removes unauthorized network shares.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/network/Invoke-RemediationCheckSharedFolders.ps1.
        Original category: endpoints/remediation/network
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-RemediationCheckSharedFolders -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/network/Invoke-RemediationCheckSharedFolders.ps1
        Category: endpoints/remediation/network
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/network/Invoke-RemediationCheckSharedFolders.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/network/Invoke-RemediationCheckSharedFolders.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/network/Invoke-RemediationCheckSharedFolders.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-RemediationCheckSystemEventErrors {
    <#
    .SYNOPSIS
        Provides guidance for critical system errors.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Invoke-RemediationCheckSystemEventErrors.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-RemediationCheckSystemEventErrors -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Invoke-RemediationCheckSystemEventErrors.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Invoke-RemediationCheckSystemEventErrors.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Invoke-RemediationCheckSystemEventErrors.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Invoke-RemediationCheckSystemEventErrors.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-RemediationCheckSystemStabilityIndex {
    <#
    .SYNOPSIS
        Provides stability improvement recommendations.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Invoke-RemediationCheckSystemStabilityIndex.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-RemediationCheckSystemStabilityIndex -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Invoke-RemediationCheckSystemStabilityIndex.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Invoke-RemediationCheckSystemStabilityIndex.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Invoke-RemediationCheckSystemStabilityIndex.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Invoke-RemediationCheckSystemStabilityIndex.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-RemediationCheckTPMStatus {
    <#
    .SYNOPSIS
        Attempts to remediate TPM issues.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/security/Invoke-RemediationCheckTPMStatus.ps1.
        Original category: endpoints/remediation/security
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-RemediationCheckTPMStatus -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/security/Invoke-RemediationCheckTPMStatus.ps1
        Category: endpoints/remediation/security
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/security/Invoke-RemediationCheckTPMStatus.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/security/Invoke-RemediationCheckTPMStatus.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/security/Invoke-RemediationCheckTPMStatus.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-RemediationCheckUnexpectedReboots {
    <#
    .SYNOPSIS
        Logs unexpected reboot information for IT analysis.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Invoke-RemediationCheckUnexpectedReboots.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-RemediationCheckUnexpectedReboots -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Invoke-RemediationCheckUnexpectedReboots.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Invoke-RemediationCheckUnexpectedReboots.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Invoke-RemediationCheckUnexpectedReboots.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Invoke-RemediationCheckUnexpectedReboots.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-RemediationCheckWindowsActivationGracePeriod {
    <#
    .SYNOPSIS
        Attempts to reactivate Windows before grace period expires.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Invoke-RemediationCheckWindowsActivationGracePeriod.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-RemediationCheckWindowsActivationGracePeriod -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Invoke-RemediationCheckWindowsActivationGracePeriod.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Invoke-RemediationCheckWindowsActivationGracePeriod.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Invoke-RemediationCheckWindowsActivationGracePeriod.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Invoke-RemediationCheckWindowsActivationGracePeriod.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-RemediationFixBitLockerNotEscrowedKeys {
    <#
    .SYNOPSIS
        Backup BitLocker recovery keys to Azure AD
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/security/Invoke-RemediationFixBitLockerNotEscrowedKeys.ps1.
        Original category: endpoints/remediation/security
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-RemediationFixBitLockerNotEscrowedKeys -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/security/Invoke-RemediationFixBitLockerNotEscrowedKeys.ps1
        Category: endpoints/remediation/security
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/security/Invoke-RemediationFixBitLockerNotEscrowedKeys.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/security/Invoke-RemediationFixBitLockerNotEscrowedKeys.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/security/Invoke-RemediationFixBitLockerNotEscrowedKeys.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-RemediationFixBrokenShortcuts {
    <#
    .SYNOPSIS
        Removes broken shortcuts from Desktop and Start Menu.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Invoke-RemediationFixBrokenShortcuts.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-RemediationFixBrokenShortcuts -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Invoke-RemediationFixBrokenShortcuts.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Invoke-RemediationFixBrokenShortcuts.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Invoke-RemediationFixBrokenShortcuts.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Invoke-RemediationFixBrokenShortcuts.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-RemediationFixCertificateExpiry {
    <#
    .SYNOPSIS
        Removes expired certificates.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/security/Invoke-RemediationFixCertificateExpiry.ps1.
        Original category: endpoints/remediation/security
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-RemediationFixCertificateExpiry -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/security/Invoke-RemediationFixCertificateExpiry.ps1
        Category: endpoints/remediation/security
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/security/Invoke-RemediationFixCertificateExpiry.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [Byte[]]$Blob,
        [Parameter()]
        [String]$CertificatesPath,
        [Parameter()]
        [String]$StoreLabel,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/security/Invoke-RemediationFixCertificateExpiry.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/security/Invoke-RemediationFixCertificateExpiry.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-RemediationFixCredentialManager {
    <#
    .SYNOPSIS
        Removes stale or orphaned credentials.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Invoke-RemediationFixCredentialManager.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-RemediationFixCredentialManager -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Invoke-RemediationFixCredentialManager.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Invoke-RemediationFixCredentialManager.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Invoke-RemediationFixCredentialManager.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Invoke-RemediationFixCredentialManager.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-RemediationFixDiskSpace {
    <#
    .SYNOPSIS
        Remediate low disk space by cleaning temporary files
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Invoke-RemediationFixDiskSpace.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-RemediationFixDiskSpace -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Invoke-RemediationFixDiskSpace.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Invoke-RemediationFixDiskSpace.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Invoke-RemediationFixDiskSpace.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Invoke-RemediationFixDiskSpace.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-RemediationFixDNSCache {
    <#
    .SYNOPSIS
        Flushes DNS cache and resets DNS client to resolve connectivity issues.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/network/Invoke-RemediationFixDNSCache.ps1.
        Original category: endpoints/remediation/network
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-RemediationFixDNSCache -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/network/Invoke-RemediationFixDNSCache.ps1
        Category: endpoints/remediation/network
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/network/Invoke-RemediationFixDNSCache.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/network/Invoke-RemediationFixDNSCache.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/network/Invoke-RemediationFixDNSCache.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-RemediationFixEdgeCacheSize {
    <#
    .SYNOPSIS
        Clears bloated Microsoft Edge browser cache.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Invoke-RemediationFixEdgeCacheSize.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-RemediationFixEdgeCacheSize -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Invoke-RemediationFixEdgeCacheSize.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Invoke-RemediationFixEdgeCacheSize.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Invoke-RemediationFixEdgeCacheSize.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Invoke-RemediationFixEdgeCacheSize.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-RemediationFixEventLogSize {
    <#
    .SYNOPSIS
        Clears bloated Windows Event Logs.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Invoke-RemediationFixEventLogSize.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-RemediationFixEventLogSize -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Invoke-RemediationFixEventLogSize.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Invoke-RemediationFixEventLogSize.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Invoke-RemediationFixEventLogSize.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Invoke-RemediationFixEventLogSize.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-RemediationFixNetworkAdapterPowerManagement {
    <#
    .SYNOPSIS
        Disables power management on network adapters.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/network/Invoke-RemediationFixNetworkAdapterPowerManagement.ps1.
        Original category: endpoints/remediation/network
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-RemediationFixNetworkAdapterPowerManagement -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/network/Invoke-RemediationFixNetworkAdapterPowerManagement.ps1
        Category: endpoints/remediation/network
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/network/Invoke-RemediationFixNetworkAdapterPowerManagement.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/network/Invoke-RemediationFixNetworkAdapterPowerManagement.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/network/Invoke-RemediationFixNetworkAdapterPowerManagement.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-RemediationFixOneDriveKnownFolderMove {
    <#
    .SYNOPSIS
        Remediates OneDrive Known Folder Move issues.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Invoke-RemediationFixOneDriveKnownFolderMove.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-RemediationFixOneDriveKnownFolderMove -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Invoke-RemediationFixOneDriveKnownFolderMove.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Invoke-RemediationFixOneDriveKnownFolderMove.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Invoke-RemediationFixOneDriveKnownFolderMove.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Invoke-RemediationFixOneDriveKnownFolderMove.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-RemediationFixOutdatedDrivers {
    <#
    .SYNOPSIS
        Installs available driver updates.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Invoke-RemediationFixOutdatedDrivers.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-RemediationFixOutdatedDrivers -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Invoke-RemediationFixOutdatedDrivers.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Invoke-RemediationFixOutdatedDrivers.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Invoke-RemediationFixOutdatedDrivers.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Invoke-RemediationFixOutdatedDrivers.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-RemediationFixPowerShellExecutionPolicy {
    <#
    .SYNOPSIS
        Sets PowerShell execution policy to RemoteSigned.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/security/Invoke-RemediationFixPowerShellExecutionPolicy.ps1.
        Original category: endpoints/remediation/security
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-RemediationFixPowerShellExecutionPolicy -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/security/Invoke-RemediationFixPowerShellExecutionPolicy.ps1
        Category: endpoints/remediation/security
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/security/Invoke-RemediationFixPowerShellExecutionPolicy.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/security/Invoke-RemediationFixPowerShellExecutionPolicy.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/security/Invoke-RemediationFixPowerShellExecutionPolicy.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-RemediationFixPrintSpooler {
    <#
    .SYNOPSIS
        Remediates Print Spooler service issues.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Invoke-RemediationFixPrintSpooler.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-RemediationFixPrintSpooler -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Invoke-RemediationFixPrintSpooler.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Invoke-RemediationFixPrintSpooler.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Invoke-RemediationFixPrintSpooler.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Invoke-RemediationFixPrintSpooler.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-RemediationFixSMBv1Protocol {
    <#
    .SYNOPSIS
        Disables SMBv1 protocol.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/network/Invoke-RemediationFixSMBv1Protocol.ps1.
        Original category: endpoints/remediation/network
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-RemediationFixSMBv1Protocol -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/network/Invoke-RemediationFixSMBv1Protocol.ps1
        Category: endpoints/remediation/network
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/network/Invoke-RemediationFixSMBv1Protocol.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/network/Invoke-RemediationFixSMBv1Protocol.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/network/Invoke-RemediationFixSMBv1Protocol.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-RemediationFixStaleProfiles {
    <#
    .SYNOPSIS
        Remove stale user profiles to free disk space
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Invoke-RemediationFixStaleProfiles.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-RemediationFixStaleProfiles -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Invoke-RemediationFixStaleProfiles.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Invoke-RemediationFixStaleProfiles.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Invoke-RemediationFixStaleProfiles.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Invoke-RemediationFixStaleProfiles.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-RemediationFixStartMenuLayout {
    <#
    .SYNOPSIS
        Repairs corrupted Start Menu layout.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Invoke-RemediationFixStartMenuLayout.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-RemediationFixStartMenuLayout -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Invoke-RemediationFixStartMenuLayout.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Invoke-RemediationFixStartMenuLayout.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Invoke-RemediationFixStartMenuLayout.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Invoke-RemediationFixStartMenuLayout.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-RemediationFixSystemFileCorruption {
    <#
    .SYNOPSIS
        Repairs Windows system file corruption.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Invoke-RemediationFixSystemFileCorruption.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-RemediationFixSystemFileCorruption -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Invoke-RemediationFixSystemFileCorruption.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Invoke-RemediationFixSystemFileCorruption.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Invoke-RemediationFixSystemFileCorruption.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Invoke-RemediationFixSystemFileCorruption.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-RemediationFixTaskSchedulerCorruption {
    <#
    .SYNOPSIS
        Remediates Task Scheduler corruption.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Invoke-RemediationFixTaskSchedulerCorruption.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-RemediationFixTaskSchedulerCorruption -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Invoke-RemediationFixTaskSchedulerCorruption.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Invoke-RemediationFixTaskSchedulerCorruption.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Invoke-RemediationFixTaskSchedulerCorruption.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Invoke-RemediationFixTaskSchedulerCorruption.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-RemediationFixTeamsCache {
    <#
    .SYNOPSIS
        Remediates Microsoft Teams cache issues.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Invoke-RemediationFixTeamsCache.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-RemediationFixTeamsCache -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Invoke-RemediationFixTeamsCache.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Invoke-RemediationFixTeamsCache.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Invoke-RemediationFixTeamsCache.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Invoke-RemediationFixTeamsCache.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-RemediationFixTempFiles {
    <#
    .SYNOPSIS
        Clean temp files older than 7 days
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Invoke-RemediationFixTempFiles.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-RemediationFixTempFiles -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Invoke-RemediationFixTempFiles.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Invoke-RemediationFixTempFiles.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Invoke-RemediationFixTempFiles.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Invoke-RemediationFixTempFiles.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-RemediationFixTimeSync {
    <#
    .SYNOPSIS
        Remediates time synchronization issues.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Invoke-RemediationFixTimeSync.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-RemediationFixTimeSync -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Invoke-RemediationFixTimeSync.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Invoke-RemediationFixTimeSync.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Invoke-RemediationFixTimeSync.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Invoke-RemediationFixTimeSync.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-RemediationFixWindowsLicenseActivation {
    <#
    .SYNOPSIS
        Attempts to activate Windows.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Invoke-RemediationFixWindowsLicenseActivation.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-RemediationFixWindowsLicenseActivation -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Invoke-RemediationFixWindowsLicenseActivation.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Invoke-RemediationFixWindowsLicenseActivation.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Invoke-RemediationFixWindowsLicenseActivation.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Invoke-RemediationFixWindowsLicenseActivation.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-RemediationFixWindowsPerformanceRecorder {
    <#
    .SYNOPSIS
        Stops stuck Windows Performance Recorder sessions.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Invoke-RemediationFixWindowsPerformanceRecorder.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-RemediationFixWindowsPerformanceRecorder -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Invoke-RemediationFixWindowsPerformanceRecorder.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Invoke-RemediationFixWindowsPerformanceRecorder.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Invoke-RemediationFixWindowsPerformanceRecorder.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Invoke-RemediationFixWindowsPerformanceRecorder.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-RemediationFixWindowsSearch {
    <#
    .SYNOPSIS
        Remediates Windows Search service issues.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Invoke-RemediationFixWindowsSearch.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-RemediationFixWindowsSearch -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Invoke-RemediationFixWindowsSearch.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Invoke-RemediationFixWindowsSearch.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Invoke-RemediationFixWindowsSearch.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Invoke-RemediationFixWindowsSearch.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-RemediationFixWindowsStoreLicensing {
    <#
    .SYNOPSIS
        Repairs Windows Store licensing issues.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Invoke-RemediationFixWindowsStoreLicensing.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-RemediationFixWindowsStoreLicensing -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Invoke-RemediationFixWindowsStoreLicensing.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Invoke-RemediationFixWindowsStoreLicensing.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Invoke-RemediationFixWindowsStoreLicensing.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Invoke-RemediationFixWindowsStoreLicensing.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-RemediationFixWindowsUpdateRebootPending {
    <#
    .SYNOPSIS
        Schedules a restart to clear a stuck Windows Update reboot-pending state.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Invoke-RemediationFixWindowsUpdateRebootPending.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-RemediationFixWindowsUpdateRebootPending -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Invoke-RemediationFixWindowsUpdateRebootPending.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Invoke-RemediationFixWindowsUpdateRebootPending.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Invoke-RemediationFixWindowsUpdateRebootPending.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Invoke-RemediationFixWindowsUpdateRebootPending.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-RemediationFixWindowsUpdateStuck {
    <#
    .SYNOPSIS
        Reset Windows Update components
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Invoke-RemediationFixWindowsUpdateStuck.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-RemediationFixWindowsUpdateStuck -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Invoke-RemediationFixWindowsUpdateStuck.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Invoke-RemediationFixWindowsUpdateStuck.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Invoke-RemediationFixWindowsUpdateStuck.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Invoke-RemediationFixWindowsUpdateStuck.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-RemediationKeyboardLayout {
    <#
    .SYNOPSIS
        Remediate the keyboard layout to UK English
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Invoke-RemediationKeyboardLayout.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-RemediationKeyboardLayout -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Invoke-RemediationKeyboardLayout.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Invoke-RemediationKeyboardLayout.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Invoke-RemediationKeyboardLayout.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Invoke-RemediationKeyboardLayout.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-RemediationLanguagePackAudit {
    <#
    .SYNOPSIS
        Remove unnecessary OS language packs
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Invoke-RemediationLanguagePackAudit.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-RemediationLanguagePackAudit -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Invoke-RemediationLanguagePackAudit.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Invoke-RemediationLanguagePackAudit.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Invoke-RemediationLanguagePackAudit.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Invoke-RemediationLanguagePackAudit.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-RemediationRegionLanguageSettings {
    <#
    .SYNOPSIS
        Remediate regional settings to UK standards
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Invoke-RemediationRegionLanguageSettings.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-RemediationRegionLanguageSettings -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Invoke-RemediationRegionLanguageSettings.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Invoke-RemediationRegionLanguageSettings.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Invoke-RemediationRegionLanguageSettings.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Invoke-RemediationRegionLanguageSettings.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-Sccm {
    <#
    .SYNOPSIS
        Remediation script for the SCCM client Proactive Remediation.
    .DESCRIPTION
        Proxies to scripts/endpoints/devices/sccm/remediate.ps1.
        Original category: endpoints/devices/sccm
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-Sccm -Verbose
    .NOTES
        Backing script: scripts/endpoints/devices/sccm/remediate.ps1
        Category: endpoints/devices/sccm
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/devices/sccm/remediate.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/devices/sccm/remediate.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/devices/sccm/remediate.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-SecurityComplianceScan {
    <#
    .SYNOPSIS
        Performs comprehensive security compliance scanning against industry frameworks.
    .DESCRIPTION
        Proxies to scripts/security/hardening/Invoke-SecurityComplianceScan.ps1.
        Original category: security/hardening
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-SecurityComplianceScan -Verbose
    .NOTES
        Backing script: scripts/security/hardening/Invoke-SecurityComplianceScan.ps1
        Category: security/hardening
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/security/hardening/Invoke-SecurityComplianceScan.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(Mandatory)]
        [String]$Framework,
        [Parameter(Mandatory)]
        [String]$TargetSystem,
        [Parameter()]
        [String]$Severity,
        [Parameter()]
        [String]$OutputFormat,
        [Parameter()]
        [String]$OutputPath,
        [Parameter()]
        [switch]$RemediationGuidance,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'security/hardening/Invoke-SecurityComplianceScan.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/security/hardening/Invoke-SecurityComplianceScan.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-TemplatesWinget {
    <#
    .SYNOPSIS
        Winget update remediation template
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/_templates/remediate.ps1.
        Original category: endpoints/remediation/winget/_templates
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-TemplatesWinget -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/_templates/remediate.ps1
        Category: endpoints/remediation/winget/_templates
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/_templates/remediate.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/_templates/remediate.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/_templates/remediate.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-Uptime {
    <#
    .SYNOPSIS
        Deploy the restart notification script and scheduled task
    .DESCRIPTION
        Proxies to scripts/endpoints/devices/uptime/remediate.ps1.
        Original category: endpoints/devices/uptime
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-Uptime -Verbose
    .NOTES
        Backing script: scripts/endpoints/devices/uptime/remediate.ps1
        Category: endpoints/devices/uptime
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/devices/uptime/remediate.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/devices/uptime/remediate.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/devices/uptime/remediate.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-UseWUServer {
    <#
    .SYNOPSIS
        Remove the UseWUServer Windows Update policy
    .DESCRIPTION
        Proxies to scripts/endpoints/devices/autopatch/V1/UseWUServer/Remediate.ps1.
        Original category: endpoints/devices/autopatch/V1/UseWUServer
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-UseWUServer -Verbose
    .NOTES
        Backing script: scripts/endpoints/devices/autopatch/V1/UseWUServer/Remediate.ps1
        Category: endpoints/devices/autopatch/V1/UseWUServer
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/devices/autopatch/V1/UseWUServer/Remediate.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/devices/autopatch/V1/UseWUServer/Remediate.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/devices/autopatch/V1/UseWUServer/Remediate.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-V1LegacyWinget {
    <#
    .SYNOPSIS
        Legacy winget update remediation template (V1)
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/_templates/remediate_v1_legacy.ps1.
        Original category: endpoints/remediation/winget/_templates
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-V1LegacyWinget -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/_templates/remediate_v1_legacy.ps1
        Category: endpoints/remediation/winget/_templates
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/_templates/remediate_v1_legacy.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/_templates/remediate_v1_legacy.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/_templates/remediate_v1_legacy.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-V2ForceCloseWinget {
    <#
    .SYNOPSIS
        Force close winget update script that automatically closes the app before updating.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/_templates/remediate_v2_force_close.ps1.
        Original category: endpoints/remediation/winget/_templates
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-V2ForceCloseWinget -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/_templates/remediate_v2_force_close.ps1
        Category: endpoints/remediation/winget/_templates
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/_templates/remediate_v2_force_close.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/_templates/remediate_v2_force_close.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/_templates/remediate_v2_force_close.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-V2StandardWinget {
    <#
    .SYNOPSIS
        Standard winget update script that waits for app to close before updating.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/_templates/remediate_v2_standard.ps1.
        Original category: endpoints/remediation/winget/_templates
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-V2StandardWinget -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/_templates/remediate_v2_standard.ps1
        Category: endpoints/remediation/winget/_templates
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/_templates/remediate_v2_standard.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/_templates/remediate_v2_standard.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/_templates/remediate_v2_standard.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-V3 {
    <#
    .SYNOPSIS
        Remediate Windows Update policy drift (Autopatch V3)
    .DESCRIPTION
        Proxies to scripts/endpoints/devices/autopatch/V3/remediate.ps1.
        Original category: endpoints/devices/autopatch/V3
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-V3 -Verbose
    .NOTES
        Backing script: scripts/endpoints/devices/autopatch/V3/remediate.ps1
        Category: endpoints/devices/autopatch/V3
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/devices/autopatch/V3/remediate.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/devices/autopatch/V3/remediate.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/devices/autopatch/V3/remediate.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-V3ForceCloseWinget {
    <#
    .SYNOPSIS
        Enhanced force close winget update script with user notification and retry logic (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/_templates/remediate_v3_force_close.ps1.
        Original category: endpoints/remediation/winget/_templates
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-V3ForceCloseWinget -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/_templates/remediate_v3_force_close.ps1
        Category: endpoints/remediation/winget/_templates
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/_templates/remediate_v3_force_close.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [String]$Message,
        [Parameter()]
        [String]$Level,
        [Parameter()]
        [String]$AppName,
        [Parameter()]
        [Int32]$Seconds,
        [Parameter()]
        [String]$ProcessName,
        [Parameter()]
        [Int32]$MaxAttempts,
        [Parameter()]
        [String]$Arguments,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/_templates/remediate_v3_force_close.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/_templates/remediate_v3_force_close.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-V3MaintenanceWindowWinget {
    <#
    .SYNOPSIS
        Maintenance window winget update script that only runs during specified hours (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/_templates/remediate_v3_maintenance_window.ps1.
        Original category: endpoints/remediation/winget/_templates
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-V3MaintenanceWindowWinget -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/_templates/remediate_v3_maintenance_window.ps1
        Category: endpoints/remediation/winget/_templates
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/_templates/remediate_v3_maintenance_window.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/_templates/remediate_v3_maintenance_window.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/_templates/remediate_v3_maintenance_window.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-V3StandardWinget {
    <#
    .SYNOPSIS
        Enhanced standard winget update script with retry logic and logging (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/_templates/remediate_v3_standard.ps1.
        Original category: endpoints/remediation/winget/_templates
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-V3StandardWinget -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/_templates/remediate_v3_standard.ps1
        Category: endpoints/remediation/winget/_templates
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/_templates/remediate_v3_standard.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/_templates/remediate_v3_standard.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/_templates/remediate_v3_standard.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-V4 {
    <#
    .SYNOPSIS
        Remediate Windows Update policy drift (Autopatch V4)
    .DESCRIPTION
        Proxies to scripts/endpoints/devices/autopatch/V4/remediate.ps1.
        Original category: endpoints/devices/autopatch/V4
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-V4 -Verbose
    .NOTES
        Backing script: scripts/endpoints/devices/autopatch/V4/remediate.ps1
        Category: endpoints/devices/autopatch/V4
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/devices/autopatch/V4/remediate.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/devices/autopatch/V4/remediate.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/devices/autopatch/V4/remediate.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-V5 {
    <#
    .SYNOPSIS
        Remediate Windows Update policy drift (Autopatch V5)
    .DESCRIPTION
        Proxies to scripts/endpoints/devices/autopatch/V5/remediate.ps1.
        Original category: endpoints/devices/autopatch/V5
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-V5 -Verbose
    .NOTES
        Backing script: scripts/endpoints/devices/autopatch/V5/remediate.ps1
        Category: endpoints/devices/autopatch/V5
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/devices/autopatch/V5/remediate.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/devices/autopatch/V5/remediate.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/devices/autopatch/V5/remediate.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-Winget1Password {
    <#
    .SYNOPSIS
        Remediation script for AgileBits 1Password via winget (V3) with retry logic and logging (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/security/1Password/Invoke-Winget1Password.ps1.
        Original category: endpoints/remediation/winget/security/1Password
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-Winget1Password -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/security/1Password/Invoke-Winget1Password.ps1
        Category: endpoints/remediation/winget/security/1Password
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/security/1Password/Invoke-Winget1Password.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/security/1Password/Invoke-Winget1Password.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/security/1Password/Invoke-Winget1Password.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-WingetAdobeReader32bit {
    <#
    .SYNOPSIS
        Standard update script for Adobe Acrobat Reader 32-bit (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/productivity/AdobeReader32bit/Invoke-WingetAdobeReader32bit.ps1.
        Original category: endpoints/remediation/winget/productivity/AdobeReader32bit
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-WingetAdobeReader32bit -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/productivity/AdobeReader32bit/Invoke-WingetAdobeReader32bit.ps1
        Category: endpoints/remediation/winget/productivity/AdobeReader32bit
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/productivity/AdobeReader32bit/Invoke-WingetAdobeReader32bit.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/productivity/AdobeReader32bit/Invoke-WingetAdobeReader32bit.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/productivity/AdobeReader32bit/Invoke-WingetAdobeReader32bit.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-WingetAdobeReader64bit {
    <#
    .SYNOPSIS
        Standard update script for Adobe Acrobat Reader 64-bit (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/productivity/AdobeReader64bit/Invoke-WingetAdobeReader64bit.ps1.
        Original category: endpoints/remediation/winget/productivity/AdobeReader64bit
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-WingetAdobeReader64bit -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/productivity/AdobeReader64bit/Invoke-WingetAdobeReader64bit.ps1
        Category: endpoints/remediation/winget/productivity/AdobeReader64bit
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/productivity/AdobeReader64bit/Invoke-WingetAdobeReader64bit.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/productivity/AdobeReader64bit/Invoke-WingetAdobeReader64bit.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/productivity/AdobeReader64bit/Invoke-WingetAdobeReader64bit.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-WingetAzureCLI {
    <#
    .SYNOPSIS
        Standard update script for Azure CLI (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/development/AzureCLI/Invoke-WingetAzureCLI.ps1.
        Original category: endpoints/remediation/winget/development/AzureCLI
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-WingetAzureCLI -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/development/AzureCLI/Invoke-WingetAzureCLI.ps1
        Category: endpoints/remediation/winget/development/AzureCLI
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/development/AzureCLI/Invoke-WingetAzureCLI.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/development/AzureCLI/Invoke-WingetAzureCLI.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/development/AzureCLI/Invoke-WingetAzureCLI.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-WingetCpp2008Redist {
    <#
    .SYNOPSIS
        Standard update script for C++ 2008 Redist (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/runtimes/Cpp2008Redist/Invoke-WingetCpp2008Redist.ps1.
        Original category: endpoints/remediation/winget/runtimes/Cpp2008Redist
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-WingetCpp2008Redist -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/runtimes/Cpp2008Redist/Invoke-WingetCpp2008Redist.ps1
        Category: endpoints/remediation/winget/runtimes/Cpp2008Redist
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/runtimes/Cpp2008Redist/Invoke-WingetCpp2008Redist.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/runtimes/Cpp2008Redist/Invoke-WingetCpp2008Redist.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/runtimes/Cpp2008Redist/Invoke-WingetCpp2008Redist.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-WingetCpp2010Redist {
    <#
    .SYNOPSIS
        Standard update script for Microsoft.VCRedist.2010.x86 (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/runtimes/Cpp2010Redist/Invoke-WingetCpp2010Redist.ps1.
        Original category: endpoints/remediation/winget/runtimes/Cpp2010Redist
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-WingetCpp2010Redist -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/runtimes/Cpp2010Redist/Invoke-WingetCpp2010Redist.ps1
        Category: endpoints/remediation/winget/runtimes/Cpp2010Redist
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/runtimes/Cpp2010Redist/Invoke-WingetCpp2010Redist.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/runtimes/Cpp2010Redist/Invoke-WingetCpp2010Redist.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/runtimes/Cpp2010Redist/Invoke-WingetCpp2010Redist.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-WingetCpp2012Redist {
    <#
    .SYNOPSIS
        Standard update script for Microsoft.VCRedist.2012.x64 (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/runtimes/Cpp2012Redist/Invoke-WingetCpp2012Redist.ps1.
        Original category: endpoints/remediation/winget/runtimes/Cpp2012Redist
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-WingetCpp2012Redist -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/runtimes/Cpp2012Redist/Invoke-WingetCpp2012Redist.ps1
        Category: endpoints/remediation/winget/runtimes/Cpp2012Redist
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/runtimes/Cpp2012Redist/Invoke-WingetCpp2012Redist.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/runtimes/Cpp2012Redist/Invoke-WingetCpp2012Redist.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/runtimes/Cpp2012Redist/Invoke-WingetCpp2012Redist.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-WingetCpp2013RedistX64 {
    <#
    .SYNOPSIS
        Standard update script for Microsoft.VCRedist.2013.x64 (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/runtimes/Cpp2013Redist-x64/Invoke-WingetCpp2013RedistX64.ps1.
        Original category: endpoints/remediation/winget/runtimes/Cpp2013Redist-x64
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-WingetCpp2013RedistX64 -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/runtimes/Cpp2013Redist-x64/Invoke-WingetCpp2013RedistX64.ps1
        Category: endpoints/remediation/winget/runtimes/Cpp2013Redist-x64
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/runtimes/Cpp2013Redist-x64/Invoke-WingetCpp2013RedistX64.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/runtimes/Cpp2013Redist-x64/Invoke-WingetCpp2013RedistX64.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/runtimes/Cpp2013Redist-x64/Invoke-WingetCpp2013RedistX64.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-WingetCpp2013RedistX86 {
    <#
    .SYNOPSIS
        Standard update script for Microsoft.VCRedist.2013.x86 (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/runtimes/Cpp2013Redist-x86/Invoke-WingetCpp2013RedistX86.ps1.
        Original category: endpoints/remediation/winget/runtimes/Cpp2013Redist-x86
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-WingetCpp2013RedistX86 -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/runtimes/Cpp2013Redist-x86/Invoke-WingetCpp2013RedistX86.ps1
        Category: endpoints/remediation/winget/runtimes/Cpp2013Redist-x86
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/runtimes/Cpp2013Redist-x86/Invoke-WingetCpp2013RedistX86.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/runtimes/Cpp2013Redist-x86/Invoke-WingetCpp2013RedistX86.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/runtimes/Cpp2013Redist-x86/Invoke-WingetCpp2013RedistX86.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-WingetCpp20152019RedistX64 {
    <#
    .SYNOPSIS
        Standard update script for Microsoft.VCRedist.2015+.x86 (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/runtimes/Cpp2015-2019Redist-x64/Invoke-WingetCpp20152019RedistX64.ps1.
        Original category: endpoints/remediation/winget/runtimes/Cpp2015-2019Redist-x64
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-WingetCpp20152019RedistX64 -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/runtimes/Cpp2015-2019Redist-x64/Invoke-WingetCpp20152019RedistX64.ps1
        Category: endpoints/remediation/winget/runtimes/Cpp2015-2019Redist-x64
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/runtimes/Cpp2015-2019Redist-x64/Invoke-WingetCpp20152019RedistX64.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/runtimes/Cpp2015-2019Redist-x64/Invoke-WingetCpp20152019RedistX64.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/runtimes/Cpp2015-2019Redist-x64/Invoke-WingetCpp20152019RedistX64.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-WingetCpp20152019RedistX86 {
    <#
    .SYNOPSIS
        Standard update script for Microsoft.VCRedist.2015+.x64 (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/runtimes/Cpp2015-2019Redist-x86/Invoke-WingetCpp20152019RedistX86.ps1.
        Original category: endpoints/remediation/winget/runtimes/Cpp2015-2019Redist-x86
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-WingetCpp20152019RedistX86 -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/runtimes/Cpp2015-2019Redist-x86/Invoke-WingetCpp20152019RedistX86.ps1
        Category: endpoints/remediation/winget/runtimes/Cpp2015-2019Redist-x86
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/runtimes/Cpp2015-2019Redist-x86/Invoke-WingetCpp20152019RedistX86.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/runtimes/Cpp2015-2019Redist-x86/Invoke-WingetCpp20152019RedistX86.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/runtimes/Cpp2015-2019Redist-x86/Invoke-WingetCpp20152019RedistX86.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-WingetDiscord {
    <#
    .SYNOPSIS
        Enhanced force close winget update script with user notification and retry logic (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/communication/Discord/Invoke-WingetDiscord.ps1.
        Original category: endpoints/remediation/winget/communication/Discord
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-WingetDiscord -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/communication/Discord/Invoke-WingetDiscord.ps1
        Category: endpoints/remediation/winget/communication/Discord
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/communication/Discord/Invoke-WingetDiscord.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [String]$Message,
        [Parameter()]
        [String]$Level,
        [Parameter()]
        [String]$AppName,
        [Parameter()]
        [Int32]$Seconds,
        [Parameter()]
        [String]$ProcessName,
        [Parameter()]
        [Int32]$MaxAttempts,
        [Parameter()]
        [String]$Arguments,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/communication/Discord/Invoke-WingetDiscord.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/communication/Discord/Invoke-WingetDiscord.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-WingetEdgeWebView2 {
    <#
    .SYNOPSIS
        Standard update script for Edge WebView2 (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/runtimes/EdgeWebView2/Invoke-WingetEdgeWebView2.ps1.
        Original category: endpoints/remediation/winget/runtimes/EdgeWebView2
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-WingetEdgeWebView2 -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/runtimes/EdgeWebView2/Invoke-WingetEdgeWebView2.ps1
        Category: endpoints/remediation/winget/runtimes/EdgeWebView2
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/runtimes/EdgeWebView2/Invoke-WingetEdgeWebView2.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/runtimes/EdgeWebView2/Invoke-WingetEdgeWebView2.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/runtimes/EdgeWebView2/Invoke-WingetEdgeWebView2.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-WingetFirefox {
    <#
    .SYNOPSIS
        Standard update script for Mozilla Firefox (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/browsers/Firefox/Invoke-WingetFirefox.ps1.
        Original category: endpoints/remediation/winget/browsers/Firefox
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-WingetFirefox -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/browsers/Firefox/Invoke-WingetFirefox.ps1
        Category: endpoints/remediation/winget/browsers/Firefox
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/browsers/Firefox/Invoke-WingetFirefox.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/browsers/Firefox/Invoke-WingetFirefox.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/browsers/Firefox/Invoke-WingetFirefox.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-WingetGit {
    <#
    .SYNOPSIS
        Force close update for Git (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/development/Git/Invoke-WingetGit.ps1.
        Original category: endpoints/remediation/winget/development/Git
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-WingetGit -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/development/Git/Invoke-WingetGit.ps1
        Category: endpoints/remediation/winget/development/Git
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/development/Git/Invoke-WingetGit.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/development/Git/Invoke-WingetGit.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/development/Git/Invoke-WingetGit.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-WingetGoogleChrome {
    <#
    .SYNOPSIS
        Standard update script for Google Chrome (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/browsers/GoogleChrome/Invoke-WingetGoogleChrome.ps1.
        Original category: endpoints/remediation/winget/browsers/GoogleChrome
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-WingetGoogleChrome -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/browsers/GoogleChrome/Invoke-WingetGoogleChrome.ps1
        Category: endpoints/remediation/winget/browsers/GoogleChrome
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/browsers/GoogleChrome/Invoke-WingetGoogleChrome.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/browsers/GoogleChrome/Invoke-WingetGoogleChrome.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/browsers/GoogleChrome/Invoke-WingetGoogleChrome.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-WingetLenovoDockManager {
    <#
    .SYNOPSIS
        Standard update script for Lenovo Dock Manager (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/vendor-specific/LenovoDockManager/Invoke-WingetLenovoDockManager.ps1.
        Original category: endpoints/remediation/winget/vendor-specific/LenovoDockManager
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-WingetLenovoDockManager -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/vendor-specific/LenovoDockManager/Invoke-WingetLenovoDockManager.ps1
        Category: endpoints/remediation/winget/vendor-specific/LenovoDockManager
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/vendor-specific/LenovoDockManager/Invoke-WingetLenovoDockManager.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/vendor-specific/LenovoDockManager/Invoke-WingetLenovoDockManager.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/vendor-specific/LenovoDockManager/Invoke-WingetLenovoDockManager.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-WingetLenovoSystemUpdate {
    <#
    .SYNOPSIS
        Standard update script for Lenovo System Update (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/vendor-specific/LenovoSystemUpdate/Invoke-WingetLenovoSystemUpdate.ps1.
        Original category: endpoints/remediation/winget/vendor-specific/LenovoSystemUpdate
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-WingetLenovoSystemUpdate -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/vendor-specific/LenovoSystemUpdate/Invoke-WingetLenovoSystemUpdate.ps1
        Category: endpoints/remediation/winget/vendor-specific/LenovoSystemUpdate
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/vendor-specific/LenovoSystemUpdate/Invoke-WingetLenovoSystemUpdate.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/vendor-specific/LenovoSystemUpdate/Invoke-WingetLenovoSystemUpdate.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/vendor-specific/LenovoSystemUpdate/Invoke-WingetLenovoSystemUpdate.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-WingetMicrosoftTeams {
    <#
    .SYNOPSIS
        Enhanced force close winget update script with user notification and retry logic (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/productivity/MicrosoftTeams/Invoke-WingetMicrosoftTeams.ps1.
        Original category: endpoints/remediation/winget/productivity/MicrosoftTeams
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-WingetMicrosoftTeams -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/productivity/MicrosoftTeams/Invoke-WingetMicrosoftTeams.ps1
        Category: endpoints/remediation/winget/productivity/MicrosoftTeams
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/productivity/MicrosoftTeams/Invoke-WingetMicrosoftTeams.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [String]$Message,
        [Parameter()]
        [String]$Level,
        [Parameter()]
        [String]$AppName,
        [Parameter()]
        [Int32]$Seconds,
        [Parameter()]
        [String]$ProcessName,
        [Parameter()]
        [Int32]$MaxAttempts,
        [Parameter()]
        [String]$Arguments,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/productivity/MicrosoftTeams/Invoke-WingetMicrosoftTeams.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/productivity/MicrosoftTeams/Invoke-WingetMicrosoftTeams.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-WingetNotepadPlusPlus {
    <#
    .SYNOPSIS
        Standard update script for NotePad++ (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/productivity/NotepadPlusPlus/Invoke-WingetNotepadPlusPlus.ps1.
        Original category: endpoints/remediation/winget/productivity/NotepadPlusPlus
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-WingetNotepadPlusPlus -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/productivity/NotepadPlusPlus/Invoke-WingetNotepadPlusPlus.ps1
        Category: endpoints/remediation/winget/productivity/NotepadPlusPlus
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/productivity/NotepadPlusPlus/Invoke-WingetNotepadPlusPlus.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/productivity/NotepadPlusPlus/Invoke-WingetNotepadPlusPlus.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/productivity/NotepadPlusPlus/Invoke-WingetNotepadPlusPlus.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-WingetOBS {
    <#
    .SYNOPSIS
        Standard update script for OBS Studio (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/media/OBS/Invoke-WingetOBS.ps1.
        Original category: endpoints/remediation/winget/media/OBS
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-WingetOBS -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/media/OBS/Invoke-WingetOBS.ps1
        Category: endpoints/remediation/winget/media/OBS
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/media/OBS/Invoke-WingetOBS.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/media/OBS/Invoke-WingetOBS.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/media/OBS/Invoke-WingetOBS.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-WingetOhMyPosh {
    <#
    .SYNOPSIS
        Standard update script for Oh My Posh (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/development/OhMyPosh/Invoke-WingetOhMyPosh.ps1.
        Original category: endpoints/remediation/winget/development/OhMyPosh
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-WingetOhMyPosh -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/development/OhMyPosh/Invoke-WingetOhMyPosh.ps1
        Category: endpoints/remediation/winget/development/OhMyPosh
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/development/OhMyPosh/Invoke-WingetOhMyPosh.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/development/OhMyPosh/Invoke-WingetOhMyPosh.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/development/OhMyPosh/Invoke-WingetOhMyPosh.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-WingetPowerShell7MaintenanceWindow {
    <#
    .SYNOPSIS
        Maintenance window winget update script that only runs during specified hours (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/development/PowerShell7/Invoke-WingetPowerShell7MaintenanceWindow.ps1.
        Original category: endpoints/remediation/winget/development/PowerShell7
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-WingetPowerShell7MaintenanceWindow -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/development/PowerShell7/Invoke-WingetPowerShell7MaintenanceWindow.ps1
        Category: endpoints/remediation/winget/development/PowerShell7
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/development/PowerShell7/Invoke-WingetPowerShell7MaintenanceWindow.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/development/PowerShell7/Invoke-WingetPowerShell7MaintenanceWindow.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/development/PowerShell7/Invoke-WingetPowerShell7MaintenanceWindow.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-WingetRemoteDesktop {
    <#
    .SYNOPSIS
        Standard update script for Microsoft Remote Desktop (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/productivity/RemoteDesktop/Invoke-WingetRemoteDesktop.ps1.
        Original category: endpoints/remediation/winget/productivity/RemoteDesktop
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-WingetRemoteDesktop -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/productivity/RemoteDesktop/Invoke-WingetRemoteDesktop.ps1
        Category: endpoints/remediation/winget/productivity/RemoteDesktop
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/productivity/RemoteDesktop/Invoke-WingetRemoteDesktop.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/productivity/RemoteDesktop/Invoke-WingetRemoteDesktop.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/productivity/RemoteDesktop/Invoke-WingetRemoteDesktop.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-WingetSevenZip {
    <#
    .SYNOPSIS
        Enhanced force close winget update script with user notification and retry logic (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/utilities/SevenZip/Invoke-WingetSevenZip.ps1.
        Original category: endpoints/remediation/winget/utilities/SevenZip
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-WingetSevenZip -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/utilities/SevenZip/Invoke-WingetSevenZip.ps1
        Category: endpoints/remediation/winget/utilities/SevenZip
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/utilities/SevenZip/Invoke-WingetSevenZip.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [String]$Message,
        [Parameter()]
        [String]$Level,
        [Parameter()]
        [String]$AppName,
        [Parameter()]
        [Int32]$Seconds,
        [Parameter()]
        [String]$ProcessName,
        [Parameter()]
        [Int32]$MaxAttempts,
        [Parameter()]
        [String]$Arguments,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/utilities/SevenZip/Invoke-WingetSevenZip.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/utilities/SevenZip/Invoke-WingetSevenZip.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-WingetSlack {
    <#
    .SYNOPSIS
        Enhanced force close winget update script with user notification and retry logic (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/communication/Slack/Invoke-WingetSlack.ps1.
        Original category: endpoints/remediation/winget/communication/Slack
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-WingetSlack -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/communication/Slack/Invoke-WingetSlack.ps1
        Category: endpoints/remediation/winget/communication/Slack
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/communication/Slack/Invoke-WingetSlack.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [String]$Message,
        [Parameter()]
        [String]$Level,
        [Parameter()]
        [String]$AppName,
        [Parameter()]
        [Int32]$Seconds,
        [Parameter()]
        [String]$ProcessName,
        [Parameter()]
        [Int32]$MaxAttempts,
        [Parameter()]
        [String]$Arguments,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/communication/Slack/Invoke-WingetSlack.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/communication/Slack/Invoke-WingetSlack.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-WingetSSMS {
    <#
    .SYNOPSIS
        Standard update script for SQL Server Management Studio (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/development/SSMS/Invoke-WingetSSMS.ps1.
        Original category: endpoints/remediation/winget/development/SSMS
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-WingetSSMS -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/development/SSMS/Invoke-WingetSSMS.ps1
        Category: endpoints/remediation/winget/development/SSMS
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/development/SSMS/Invoke-WingetSSMS.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/development/SSMS/Invoke-WingetSSMS.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/development/SSMS/Invoke-WingetSSMS.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-WingetTeamViewerFull {
    <#
    .SYNOPSIS
        Standard update script for TeamViewer Full (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/remote-access/TeamViewerFull/Invoke-WingetTeamViewerFull.ps1.
        Original category: endpoints/remediation/winget/remote-access/TeamViewerFull
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-WingetTeamViewerFull -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/remote-access/TeamViewerFull/Invoke-WingetTeamViewerFull.ps1
        Category: endpoints/remediation/winget/remote-access/TeamViewerFull
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/remote-access/TeamViewerFull/Invoke-WingetTeamViewerFull.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/remote-access/TeamViewerFull/Invoke-WingetTeamViewerFull.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/remote-access/TeamViewerFull/Invoke-WingetTeamViewerFull.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-WingetTeamViewerFullForceClose {
    <#
    .SYNOPSIS
        Standard update script for TeamViewer Full (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/remote-access/TeamViewerFull/Invoke-WingetTeamViewerFullForceClose.ps1.
        Original category: endpoints/remediation/winget/remote-access/TeamViewerFull
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-WingetTeamViewerFullForceClose -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/remote-access/TeamViewerFull/Invoke-WingetTeamViewerFullForceClose.ps1
        Category: endpoints/remediation/winget/remote-access/TeamViewerFull
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/remote-access/TeamViewerFull/Invoke-WingetTeamViewerFullForceClose.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/remote-access/TeamViewerFull/Invoke-WingetTeamViewerFullForceClose.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/remote-access/TeamViewerFull/Invoke-WingetTeamViewerFullForceClose.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-WingetTeamViewerHost {
    <#
    .SYNOPSIS
        Standard update script for TeamViewer Host (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/remote-access/TeamViewerHost/Invoke-WingetTeamViewerHost.ps1.
        Original category: endpoints/remediation/winget/remote-access/TeamViewerHost
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-WingetTeamViewerHost -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/remote-access/TeamViewerHost/Invoke-WingetTeamViewerHost.ps1
        Category: endpoints/remediation/winget/remote-access/TeamViewerHost
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/remote-access/TeamViewerHost/Invoke-WingetTeamViewerHost.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/remote-access/TeamViewerHost/Invoke-WingetTeamViewerHost.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/remote-access/TeamViewerHost/Invoke-WingetTeamViewerHost.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-WingetVisualStudioCode {
    <#
    .SYNOPSIS
        Standard update script for Visual Studio Code (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/development/VisualStudioCode/Invoke-WingetVisualStudioCode.ps1.
        Original category: endpoints/remediation/winget/development/VisualStudioCode
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-WingetVisualStudioCode -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/development/VisualStudioCode/Invoke-WingetVisualStudioCode.ps1
        Category: endpoints/remediation/winget/development/VisualStudioCode
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/development/VisualStudioCode/Invoke-WingetVisualStudioCode.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/development/VisualStudioCode/Invoke-WingetVisualStudioCode.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/development/VisualStudioCode/Invoke-WingetVisualStudioCode.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-WingetVisualStudioPro2019 {
    <#
    .SYNOPSIS
        Standard update script for Visual Studio Professional 2019 (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/development/VisualStudioPro2019/Invoke-WingetVisualStudioPro2019.ps1.
        Original category: endpoints/remediation/winget/development/VisualStudioPro2019
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-WingetVisualStudioPro2019 -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/development/VisualStudioPro2019/Invoke-WingetVisualStudioPro2019.ps1
        Category: endpoints/remediation/winget/development/VisualStudioPro2019
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/development/VisualStudioPro2019/Invoke-WingetVisualStudioPro2019.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/development/VisualStudioPro2019/Invoke-WingetVisualStudioPro2019.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/development/VisualStudioPro2019/Invoke-WingetVisualStudioPro2019.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-WingetVisualStudioPro2022 {
    <#
    .SYNOPSIS
        Standard update script for Visual Studio Professional 2022 (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/development/VisualStudioPro2022/Invoke-WingetVisualStudioPro2022.ps1.
        Original category: endpoints/remediation/winget/development/VisualStudioPro2022
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-WingetVisualStudioPro2022 -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/development/VisualStudioPro2022/Invoke-WingetVisualStudioPro2022.ps1
        Category: endpoints/remediation/winget/development/VisualStudioPro2022
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/development/VisualStudioPro2022/Invoke-WingetVisualStudioPro2022.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/development/VisualStudioPro2022/Invoke-WingetVisualStudioPro2022.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/development/VisualStudioPro2022/Invoke-WingetVisualStudioPro2022.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-WingetVLCMediaPlayer {
    <#
    .SYNOPSIS
        Standard update script for VLC Media Player (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/media/VLCMediaPlayer/Invoke-WingetVLCMediaPlayer.ps1.
        Original category: endpoints/remediation/winget/media/VLCMediaPlayer
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-WingetVLCMediaPlayer -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/media/VLCMediaPlayer/Invoke-WingetVLCMediaPlayer.ps1
        Category: endpoints/remediation/winget/media/VLCMediaPlayer
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/media/VLCMediaPlayer/Invoke-WingetVLCMediaPlayer.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/media/VLCMediaPlayer/Invoke-WingetVLCMediaPlayer.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/media/VLCMediaPlayer/Invoke-WingetVLCMediaPlayer.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-WingetWinSCP {
    <#
    .SYNOPSIS
        Standard update script for WinSCP (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/remote-access/WinSCP/Invoke-WingetWinSCP.ps1.
        Original category: endpoints/remediation/winget/remote-access/WinSCP
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-WingetWinSCP -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/remote-access/WinSCP/Invoke-WingetWinSCP.ps1
        Category: endpoints/remediation/winget/remote-access/WinSCP
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/remote-access/WinSCP/Invoke-WingetWinSCP.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/remote-access/WinSCP/Invoke-WingetWinSCP.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/remote-access/WinSCP/Invoke-WingetWinSCP.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-WingetZoom {
    <#
    .SYNOPSIS
        Standard update script for Zoomies (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/media/Zoom/Invoke-WingetZoom.ps1.
        Original category: endpoints/remediation/winget/media/Zoom
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-WingetZoom -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/media/Zoom/Invoke-WingetZoom.ps1
        Category: endpoints/remediation/winget/media/Zoom
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/media/Zoom/Invoke-WingetZoom.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/media/Zoom/Invoke-WingetZoom.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/media/Zoom/Invoke-WingetZoom.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Invoke-WUServer {
    <#
    .SYNOPSIS
        Remove the WUServer Windows Update policy
    .DESCRIPTION
        Proxies to scripts/endpoints/devices/autopatch/V1/WUServer/Remediate.ps1.
        Original category: endpoints/devices/autopatch/V1/WUServer
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Invoke-WUServer -Verbose
    .NOTES
        Backing script: scripts/endpoints/devices/autopatch/V1/WUServer/Remediate.ps1
        Category: endpoints/devices/autopatch/V1/WUServer
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/devices/autopatch/V1/WUServer/Remediate.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/devices/autopatch/V1/WUServer/Remediate.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/devices/autopatch/V1/WUServer/Remediate.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function New-AzureComputeGalleryImage {
    <#
    .SYNOPSIS
        Creates a cloned virtual machine and an Azure Compute Gallery (ACG) image version from a gold VM.
    .DESCRIPTION
        Proxies to scripts/cloud/azure/avd/New-AzureComputeGalleryImage.ps1.
        Original category: cloud/azure/avd
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        New-AzureComputeGalleryImage -Verbose
    .NOTES
        Backing script: scripts/cloud/azure/avd/New-AzureComputeGalleryImage.ps1
        Category: cloud/azure/avd
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/cloud/azure/avd/New-AzureComputeGalleryImage.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [String]$ConfigFile,
        [Parameter()]
        [switch]$GenerateConfig,
        [Parameter()]
        [switch]$Interactive,
        [Parameter()]
        [String]$TenantId,
        [Parameter()]
        [String]$SubscriptionId,
        [Parameter()]
        [String]$Location,
        [Parameter()]
        [String]$SourceVMName,
        [Parameter()]
        [String]$SourceVMResourceGroup,
        [Parameter()]
        [String]$GalleryResourceGroup,
        [Parameter()]
        [String]$GalleryName,
        [Parameter()]
        [String]$ImageDefinitionName,
        [Parameter()]
        [String]$VMSize,
        [Parameter()]
        [String]$VNetName,
        [Parameter()]
        [String]$VNetResourceGroup,
        [Parameter()]
        [String]$SubnetName,
        [Parameter()]
        [String]$VersioningStrategy,
        [Parameter()]
        [switch]$SkipAgentCheck,
        [Parameter()]
        [switch]$SkipCleanup,
        [Parameter()]
        [switch]$SkipPreFlightChecks,
        [Parameter()]
        [switch]$Force,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'cloud/azure/avd/New-AzureComputeGalleryImage.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/cloud/azure/avd/New-AzureComputeGalleryImage.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function New-BulkWingetUpdater {
    <#
    .SYNOPSIS
        Universal winget package updater that works for ANY application.
    .DESCRIPTION
        Proxies to scripts/endpoints/intune/deployment/New-BulkWingetUpdater.ps1.
        Original category: endpoints/intune/deployment
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        New-BulkWingetUpdater -Verbose
    .NOTES
        Backing script: scripts/endpoints/intune/deployment/New-BulkWingetUpdater.ps1
        Category: endpoints/intune/deployment
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/intune/deployment/New-BulkWingetUpdater.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(Mandatory)]
        [String]$AppName,
        [Parameter(Mandatory)]
        [String]$WingetID,
        [Parameter(Mandatory)]
        [String]$ProcessName,
        [Parameter()]
        [String]$OutputPath,
        [Parameter()]
        [switch]$ForceUpdate,
        [Parameter(Mandatory)]
        [switch]$GenerateBatch,
        [Parameter(Mandatory)]
        [String]$CSVPath,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/intune/deployment/New-BulkWingetUpdater.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/intune/deployment/New-BulkWingetUpdater.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function New-IntuneWinPackage {
    <#
    .SYNOPSIS
        Bulk converts installers to .intunewin format for Intune deployment.
    .DESCRIPTION
        Proxies to scripts/endpoints/intune/deployment/New-IntuneWinPackage.ps1.
        Original category: endpoints/intune/deployment
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        New-IntuneWinPackage -Verbose
    .NOTES
        Backing script: scripts/endpoints/intune/deployment/New-IntuneWinPackage.ps1
        Category: endpoints/intune/deployment
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/intune/deployment/New-IntuneWinPackage.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [String]$SourceFolder,
        [Parameter()]
        [String]$OutputFolder,
        [Parameter()]
        [String]$SetupFile,
        [Parameter()]
        [String]$ContentPrepToolPath,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/intune/deployment/New-IntuneWinPackage.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/intune/deployment/New-IntuneWinPackage.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function New-WeeklyRebootSchedule {
    <#
    .SYNOPSIS
        Creates a scheduled task to reboot Windows Server on a weekly basis.
    .DESCRIPTION
        Proxies to scripts/infrastructure/windows/system/New-WeeklyRebootSchedule.ps1.
        Original category: infrastructure/windows/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        New-WeeklyRebootSchedule -Verbose
    .NOTES
        Backing script: scripts/infrastructure/windows/system/New-WeeklyRebootSchedule.ps1
        Category: infrastructure/windows/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/infrastructure/windows/system/New-WeeklyRebootSchedule.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [String]$DayOfWeek,
        [Parameter()]
        [String]$Time,
        [Parameter()]
        [String]$TaskName,
        [Parameter()]
        [Int32]$RebootDelay,
        [Parameter()]
        [switch]$Force,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'infrastructure/windows/system/New-WeeklyRebootSchedule.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/infrastructure/windows/system/New-WeeklyRebootSchedule.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function New-Win32AppTemplate {
    <#
    .SYNOPSIS
        Generates a Win32 app deployment template with all necessary files and documentation.
    .DESCRIPTION
        Proxies to scripts/endpoints/intune/deployment/New-Win32AppTemplate.ps1.
        Original category: endpoints/intune/deployment
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        New-Win32AppTemplate -Verbose
    .NOTES
        Backing script: scripts/endpoints/intune/deployment/New-Win32AppTemplate.ps1
        Category: endpoints/intune/deployment
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/intune/deployment/New-Win32AppTemplate.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [String]$AppName,
        [Parameter()]
        [String]$InstallCommand,
        [Parameter()]
        [String]$UninstallCommand,
        [Parameter()]
        [String]$DetectionType,
        [Parameter()]
        [String]$OutputFolder,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/intune/deployment/New-Win32AppTemplate.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/intune/deployment/New-Win32AppTemplate.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function New-WingetRemediationScript {
    <#
    .SYNOPSIS
        Generates Intune Proactive Remediation scripts for Winget package updates.
    .DESCRIPTION
        Proxies to scripts/endpoints/intune/deployment/New-WingetRemediationScript.ps1.
        Original category: endpoints/intune/deployment
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        New-WingetRemediationScript -Verbose
    .NOTES
        Backing script: scripts/endpoints/intune/deployment/New-WingetRemediationScript.ps1
        Category: endpoints/intune/deployment
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/intune/deployment/New-WingetRemediationScript.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [String]$PackageId,
        [Parameter()]
        [String]$OutputFolder,
        [Parameter()]
        [String]$AppDisplayName,
        [Parameter()]
        [switch]$IncludeReadme,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/intune/deployment/New-WingetRemediationScript.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/intune/deployment/New-WingetRemediationScript.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function New-WingetSourceConfig {
    <#
    .SYNOPSIS
        Configures custom winget sources for enterprise environments.
    .DESCRIPTION
        Proxies to scripts/endpoints/intune/deployment/New-WingetSourceConfig.ps1.
        Original category: endpoints/intune/deployment
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        New-WingetSourceConfig -Verbose
    .NOTES
        Backing script: scripts/endpoints/intune/deployment/New-WingetSourceConfig.ps1
        Category: endpoints/intune/deployment
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/intune/deployment/New-WingetSourceConfig.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(Mandatory)]
        [String]$SourceName,
        [Parameter(Mandatory)]
        [String]$SourceURL,
        [Parameter()]
        [String]$SourceType,
        [Parameter()]
        [switch]$RemoveDefaultSources,
        [Parameter()]
        [switch]$ResetSources,
        [Parameter()]
        [switch]$ExportConfig,
        [Parameter()]
        [String]$ImportConfig,
        [Parameter()]
        [String]$OutputPath,
        [Parameter()]
        [switch]$GenerateIntuneScript,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/intune/deployment/New-WingetSourceConfig.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/intune/deployment/New-WingetSourceConfig.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Optimize-AzureVMs {
    <#
    .SYNOPSIS
        Analyzes and optimizes Azure Virtual Machine configurations for cost and performance.
    .DESCRIPTION
        Proxies to scripts/cloud/azure/compute/Azure-VirtualMachines/Optimize-AzureVMs.ps1.
        Original category: cloud/azure/compute/Azure-VirtualMachines
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Optimize-AzureVMs -Verbose
    .NOTES
        Backing script: scripts/cloud/azure/compute/Azure-VirtualMachines/Optimize-AzureVMs.ps1
        Category: cloud/azure/compute/Azure-VirtualMachines
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/cloud/azure/compute/Azure-VirtualMachines/Optimize-AzureVMs.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [String]$SubscriptionId,
        [Parameter()]
        [String]$ResourceGroupName,
        [Parameter()]
        [Int32]$DaysToAnalyze,
        [Parameter()]
        [Int32]$UtilizationThreshold,
        [Parameter()]
        [switch]$GenerateRecommendations,
        [Parameter()]
        [String]$OutputFormat,
        [Parameter()]
        [String]$OutputPath,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'cloud/azure/compute/Azure-VirtualMachines/Optimize-AzureVMs.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/cloud/azure/compute/Azure-VirtualMachines/Optimize-AzureVMs.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Optimize-DockerCleanup {
    <#
    .SYNOPSIS
        Clean up Docker resources and optimize storage.
    .DESCRIPTION
        Proxies to scripts/cloud/containers/Optimize-DockerCleanup.ps1.
        Original category: cloud/containers
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Optimize-DockerCleanup -Verbose
    .NOTES
        Backing script: scripts/cloud/containers/Optimize-DockerCleanup.ps1
        Category: cloud/containers
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/cloud/containers/Optimize-DockerCleanup.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [switch]$RemoveStoppedContainers,
        [Parameter()]
        [switch]$RemoveDanglingImages,
        [Parameter()]
        [switch]$RemoveUnusedVolumes,
        [Parameter()]
        [switch]$PruneBuildCache,
        [Parameter()]
        [switch]$Force,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'cloud/containers/Optimize-DockerCleanup.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/cloud/containers/Optimize-DockerCleanup.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Optimize-IISConfiguration {
    <#
    .SYNOPSIS
        Optimize IIS configuration for performance and security.
    .DESCRIPTION
        Proxies to scripts/infrastructure/web/iis/Optimize-IISConfiguration.ps1.
        Original category: infrastructure/web/iis
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Optimize-IISConfiguration -Verbose
    .NOTES
        Backing script: scripts/infrastructure/web/iis/Optimize-IISConfiguration.ps1
        Category: infrastructure/web/iis
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/infrastructure/web/iis/Optimize-IISConfiguration.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [switch]$ApplyPerformanceOptimizations,
        [Parameter()]
        [switch]$ApplySecurityHardening,
        [Parameter()]
        [switch]$EnableHTTP2,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'infrastructure/web/iis/Optimize-IISConfiguration.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/infrastructure/web/iis/Optimize-IISConfiguration.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Optimize-ServerStorage {
    <#
    .SYNOPSIS
        Automated disk cleanup and optimization for Windows Server 2016-2022.
    .DESCRIPTION
        Proxies to scripts/infrastructure/windows/storage/Optimize-ServerStorage.ps1.
        Original category: infrastructure/windows/storage
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Optimize-ServerStorage -Verbose
    .NOTES
        Backing script: scripts/infrastructure/windows/storage/Optimize-ServerStorage.ps1
        Category: infrastructure/windows/storage
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/infrastructure/windows/storage/Optimize-ServerStorage.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [String]$DriveLetter,
        [Parameter()]
        [switch]$IncludeWindowsUpdate,
        [Parameter()]
        [switch]$IncludeIISLogs,
        [Parameter()]
        [Int32]$IISLogRetentionDays,
        [Parameter()]
        [switch]$IncludeDISM,
        [Parameter()]
        [switch]$Force,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'infrastructure/windows/storage/Optimize-ServerStorage.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/infrastructure/windows/storage/Optimize-ServerStorage.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Optimize-WindowsServices {
    <#
    .SYNOPSIS
        Optimizes Windows service startup types for better performance and security.
    .DESCRIPTION
        Proxies to scripts/utilities/Optimize-WindowsServices.ps1.
        Original category: utilities
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Optimize-WindowsServices -Verbose
    .NOTES
        Backing script: scripts/utilities/Optimize-WindowsServices.ps1
        Category: utilities
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/utilities/Optimize-WindowsServices.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(Mandatory)]
        [String]$Mode,
        [Parameter()] [Alias('Profile')]
        [String]$ProfilePath,
        [Parameter()]
        [String]$BackupPath,
        [Parameter()]
        [switch]$ApplyChanges,
        [Parameter()]
        [switch]$ExportHTML,
        [Parameter()]
        [switch]$ExportCSV,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
        if ($boundParams.ContainsKey('ProfilePath')) { $boundParams['Profile'] = $boundParams['ProfilePath']; $null = $boundParams.Remove('ProfilePath') }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'utilities/Optimize-WindowsServices.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/utilities/Optimize-WindowsServices.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Optimize-WsusServer {
    <#
    .SYNOPSIS
        Comprehensive Windows Server Update Services (WSUS) optimization and maintenance script.
    .DESCRIPTION
        Proxies to scripts/infrastructure/windows/system/Optimize-WsusServer.ps1.
        Original category: infrastructure/windows/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Optimize-WsusServer -Verbose
    .NOTES
        Backing script: scripts/infrastructure/windows/system/Optimize-WsusServer.ps1
        Category: infrastructure/windows/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/infrastructure/windows/system/Optimize-WsusServer.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [switch]$Interactive,
        [Parameter()]
        [String]$ConfigFile,
        [Parameter()]
        [switch]$DeclineSupersededUpdates,
        [Parameter()]
        [switch]$DeepClean,
        [Parameter()]
        [switch]$DisableDrivers,
        [Parameter()]
        [switch]$CheckConfig,
        [Parameter()]
        [switch]$OptimizeServer,
        [Parameter()]
        [switch]$OptimizeDatabase,
        [Parameter()]
        [switch]$CreateTasks,
        [Parameter()]
        [String]$LogPath,
        [Parameter()]
        [String]$SqlServerInstance,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'infrastructure/windows/system/Optimize-WsusServer.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/infrastructure/windows/system/Optimize-WsusServer.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Remove-SysprepBlockers {
    <#
    .SYNOPSIS
        Detect and remove applications that block Windows Sysprep.
    .DESCRIPTION
        Proxies to scripts/cloud/azure/avd/Remove-SysprepBlockers.ps1.
        Original category: cloud/azure/avd
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Remove-SysprepBlockers -Verbose
    .NOTES
        Backing script: scripts/cloud/azure/avd/Remove-SysprepBlockers.ps1
        Category: cloud/azure/avd
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/cloud/azure/avd/Remove-SysprepBlockers.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [switch]$Force,
        [Parameter()]
        [String]$LogPath,
        [Parameter()]
        [switch]$ExportBlockersList,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'cloud/azure/avd/Remove-SysprepBlockers.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/cloud/azure/avd/Remove-SysprepBlockers.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Remove-USlangpack {
    <#
    .SYNOPSIS
        Removes US English (en-US) language packs from Windows 11 24H2.
    .DESCRIPTION
        Proxies to scripts/cloud/azure/avd/removeUSlangpack.ps1.
        Original category: cloud/azure/avd
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Remove-USlangpack -Verbose
    .NOTES
        Backing script: scripts/cloud/azure/avd/removeUSlangpack.ps1
        Category: cloud/azure/avd
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/cloud/azure/avd/removeUSlangpack.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [switch]$Force,
        [Parameter()]
        [switch]$KeepKeyboard,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'cloud/azure/avd/removeUSlangpack.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/cloud/azure/avd/removeUSlangpack.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Remove-USLanguagePack {
    <#
    .SYNOPSIS
        Removes US English (en-US) language packs from Windows Server 2016-2022.
    .DESCRIPTION
        Proxies to scripts/infrastructure/windows/system/Remove-USLanguagePack.ps1.
        Original category: infrastructure/windows/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Remove-USLanguagePack -Verbose
    .NOTES
        Backing script: scripts/infrastructure/windows/system/Remove-USLanguagePack.ps1
        Category: infrastructure/windows/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/infrastructure/windows/system/Remove-USLanguagePack.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [switch]$Force,
        [Parameter()]
        [switch]$KeepKeyboard,
        [Parameter()]
        [switch]$BackupFirst,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'infrastructure/windows/system/Remove-USLanguagePack.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/infrastructure/windows/system/Remove-USLanguagePack.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Reset-NetworkStack {
    <#
    .SYNOPSIS
        Resets Windows network stack to resolve connectivity issues.
    .DESCRIPTION
        Proxies to scripts/infrastructure/network/Reset-NetworkStack.ps1.
        Original category: infrastructure/network
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Reset-NetworkStack -Verbose
    .NOTES
        Backing script: scripts/infrastructure/network/Reset-NetworkStack.ps1
        Category: infrastructure/network
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/infrastructure/network/Reset-NetworkStack.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [switch]$ResetFirewall,
        [Parameter()]
        [switch]$ResetProxy,
        [Parameter()]
        [switch]$FlushDNS,
        [Parameter()]
        [switch]$ResetAdapters,
        [Parameter()]
        [switch]$CreateRestorePoint,
        [Parameter()]
        [switch]$Force,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'infrastructure/network/Reset-NetworkStack.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/infrastructure/network/Reset-NetworkStack.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Reset-WindowsUpdate {
    <#
    .SYNOPSIS
        Resets Windows Update components to resolve update issues on Windows Server 2016-2022.
    .DESCRIPTION
        Proxies to scripts/infrastructure/windows/system/Reset-WindowsUpdate.ps1.
        Original category: infrastructure/windows/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Reset-WindowsUpdate -Verbose
    .NOTES
        Backing script: scripts/infrastructure/windows/system/Reset-WindowsUpdate.ps1
        Category: infrastructure/windows/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/infrastructure/windows/system/Reset-WindowsUpdate.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [switch]$FullReset,
        [Parameter()]
        [switch]$RunDismRepair,
        [Parameter()]
        [switch]$LegacyReset,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'infrastructure/windows/system/Reset-WindowsUpdate.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/infrastructure/windows/system/Reset-WindowsUpdate.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Set-EnglishUKRegion {
    <#
    .SYNOPSIS
        Configures Windows Server to use English (UK) regional settings system-wide.
    .DESCRIPTION
        Proxies to scripts/infrastructure/windows/system/Set-EnglishUKRegion.ps1.
        Original category: infrastructure/windows/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Set-EnglishUKRegion -Verbose
    .NOTES
        Backing script: scripts/infrastructure/windows/system/Set-EnglishUKRegion.ps1
        Category: infrastructure/windows/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/infrastructure/windows/system/Set-EnglishUKRegion.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [String]$TimeZone,
        [Parameter()]
        [switch]$ApplyToExistingUsers,
        [Parameter()]
        [switch]$SkipTimeZone,
        [Parameter()]
        [switch]$SkipKeyboard,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'infrastructure/windows/system/Set-EnglishUKRegion.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/infrastructure/windows/system/Set-EnglishUKRegion.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Set-MailboxRegionalSettings {
    <#
    .SYNOPSIS
        Checks and configures Exchange Online mailbox regional and calendar settings.
    .DESCRIPTION
        Proxies to scripts/collaboration/microsoft365/exchange-online/Set-MailboxRegionalSettings.ps1.
        Original category: collaboration/microsoft365/exchange-online
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Set-MailboxRegionalSettings -Verbose
    .NOTES
        Backing script: scripts/collaboration/microsoft365/exchange-online/Set-MailboxRegionalSettings.ps1
        Category: collaboration/microsoft365/exchange-online
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/collaboration/microsoft365/exchange-online/Set-MailboxRegionalSettings.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [String]$UserPrincipalName,
        [Parameter()]
        [switch]$AllMailboxes,
        [Parameter()]
        [switch]$AuditOnly,
        [Parameter()]
        [switch]$Apply,
        [Parameter()]
        [String]$TimeZone,
        [Parameter()]
        [String]$DateFormat,
        [Parameter()]
        [String]$TimeFormat,
        [Parameter()]
        [String]$Language,
        [Parameter()]
        [String]$WorkDays,
        [Parameter()]
        [String]$WorkingHoursStartTime,
        [Parameter()]
        [String]$WorkingHoursEndTime,
        [Parameter()]
        [switch]$ExportHTML,
        [Parameter()]
        [switch]$ExportCSV,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'collaboration/microsoft365/exchange-online/Set-MailboxRegionalSettings.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/collaboration/microsoft365/exchange-online/Set-MailboxRegionalSettings.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Set-OneDriveRegionalSettings {
    <#
    .SYNOPSIS
        Checks and configures OneDrive personal site regional settings.
    .DESCRIPTION
        Proxies to scripts/collaboration/microsoft365/sharepoint-onedrive/Set-OneDriveRegionalSettings.ps1.
        Original category: collaboration/microsoft365/sharepoint-onedrive
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Set-OneDriveRegionalSettings -Verbose
    .NOTES
        Backing script: scripts/collaboration/microsoft365/sharepoint-onedrive/Set-OneDriveRegionalSettings.ps1
        Category: collaboration/microsoft365/sharepoint-onedrive
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/collaboration/microsoft365/sharepoint-onedrive/Set-OneDriveRegionalSettings.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [String]$UserPrincipalName,
        [Parameter()]
        [switch]$AllOneDriveSites,
        [Parameter()]
        [switch]$AuditOnly,
        [Parameter()]
        [switch]$Apply,
        [Parameter()]
        [Int32]$TimeZone,
        [Parameter()]
        [Int32]$LocaleId,
        [Parameter()]
        [switch]$ExportHTML,
        [Parameter()]
        [switch]$ExportCSV,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'collaboration/microsoft365/sharepoint-onedrive/Set-OneDriveRegionalSettings.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/collaboration/microsoft365/sharepoint-onedrive/Set-OneDriveRegionalSettings.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Set-OrganizationDefaults {
    <#
    .SYNOPSIS
        Audits and configures the organization-wide PreferredLanguage setting in Microsoft 365.
    .DESCRIPTION
        Proxies to scripts/collaboration/microsoft365/azure-ad/Set-OrganizationDefaults.ps1.
        Original category: collaboration/microsoft365/azure-ad
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Set-OrganizationDefaults -Verbose
    .NOTES
        Backing script: scripts/collaboration/microsoft365/azure-ad/Set-OrganizationDefaults.ps1
        Category: collaboration/microsoft365/azure-ad
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/collaboration/microsoft365/azure-ad/Set-OrganizationDefaults.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [String]$UsageLocation,
        [Parameter()]
        [String]$PreferredLanguage,
        [Parameter()]
        [switch]$AuditOnly,
        [Parameter()]
        [switch]$Apply,
        [Parameter()]
        [switch]$ExportHTML,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'collaboration/microsoft365/azure-ad/Set-OrganizationDefaults.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/collaboration/microsoft365/azure-ad/Set-OrganizationDefaults.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Set-PowerPlatformRegionalSettings {
    <#
    .SYNOPSIS
        Checks and configures Power Platform environment regional settings.
    .DESCRIPTION
        Proxies to scripts/collaboration/microsoft365/power-platform/Set-PowerPlatformRegionalSettings.ps1.
        Original category: collaboration/microsoft365/power-platform
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Set-PowerPlatformRegionalSettings -Verbose
    .NOTES
        Backing script: scripts/collaboration/microsoft365/power-platform/Set-PowerPlatformRegionalSettings.ps1
        Category: collaboration/microsoft365/power-platform
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/collaboration/microsoft365/power-platform/Set-PowerPlatformRegionalSettings.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [String]$EnvironmentName,
        [Parameter()]
        [switch]$AllEnvironments,
        [Parameter()]
        [switch]$AuditOnly,
        [Parameter()]
        [switch]$Apply,
        [Parameter()]
        [Int32]$BaseLanguage,
        [Parameter()]
        [String]$CurrencyCode,
        [Parameter()]
        [switch]$ExportHTML,
        [Parameter()]
        [switch]$ExportCSV,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'collaboration/microsoft365/power-platform/Set-PowerPlatformRegionalSettings.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/collaboration/microsoft365/power-platform/Set-PowerPlatformRegionalSettings.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Set-SiteRegionalSettings {
    <#
    .SYNOPSIS
        Checks and configures SharePoint Online site collection regional settings.
    .DESCRIPTION
        Proxies to scripts/collaboration/microsoft365/sharepoint-onedrive/Set-SiteRegionalSettings.ps1.
        Original category: collaboration/microsoft365/sharepoint-onedrive
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Set-SiteRegionalSettings -Verbose
    .NOTES
        Backing script: scripts/collaboration/microsoft365/sharepoint-onedrive/Set-SiteRegionalSettings.ps1
        Category: collaboration/microsoft365/sharepoint-onedrive
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/collaboration/microsoft365/sharepoint-onedrive/Set-SiteRegionalSettings.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [String]$SiteUrl,
        [Parameter()]
        [switch]$AllSites,
        [Parameter()]
        [switch]$AuditOnly,
        [Parameter()]
        [switch]$Apply,
        [Parameter()]
        [Int32]$TimeZone,
        [Parameter()]
        [Int32]$LocaleId,
        [Parameter()]
        [Int32]$CalendarType,
        [Parameter()]
        [Int32]$WorkDayStartHour,
        [Parameter()]
        [Int32]$WorkDayEndHour,
        [Parameter()]
        [Int32]$FirstDayOfWeek,
        [Parameter()]
        [Boolean]$Time24,
        [Parameter()]
        [switch]$ExportHTML,
        [Parameter()]
        [switch]$ExportCSV,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'collaboration/microsoft365/sharepoint-onedrive/Set-SiteRegionalSettings.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/collaboration/microsoft365/sharepoint-onedrive/Set-SiteRegionalSettings.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Set-TeamsRegionalSettings {
    <#
    .SYNOPSIS
        Checks and configures Microsoft Teams regional settings for users.
    .DESCRIPTION
        Proxies to scripts/collaboration/microsoft365/teams/Set-TeamsRegionalSettings.ps1.
        Original category: collaboration/microsoft365/teams
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Set-TeamsRegionalSettings -Verbose
    .NOTES
        Backing script: scripts/collaboration/microsoft365/teams/Set-TeamsRegionalSettings.ps1
        Category: collaboration/microsoft365/teams
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/collaboration/microsoft365/teams/Set-TeamsRegionalSettings.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [String]$UserPrincipalName,
        [Parameter()]
        [switch]$AllUsers,
        [Parameter()]
        [switch]$AuditOnly,
        [Parameter()]
        [switch]$Apply,
        [Parameter()]
        [String]$TimeZone,
        [Parameter()]
        [switch]$ExportHTML,
        [Parameter()]
        [switch]$ExportCSV,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'collaboration/microsoft365/teams/Set-TeamsRegionalSettings.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/collaboration/microsoft365/teams/Set-TeamsRegionalSettings.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Set-UserLanguageSettings {
    <#
    .SYNOPSIS
        Checks and configures Microsoft 365 user language and region settings.
    .DESCRIPTION
        Proxies to scripts/collaboration/microsoft365/azure-ad/Set-UserLanguageSettings.ps1.
        Original category: collaboration/microsoft365/azure-ad
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Set-UserLanguageSettings -Verbose
    .NOTES
        Backing script: scripts/collaboration/microsoft365/azure-ad/Set-UserLanguageSettings.ps1
        Category: collaboration/microsoft365/azure-ad
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/collaboration/microsoft365/azure-ad/Set-UserLanguageSettings.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [String]$UserPrincipalName,
        [Parameter()]
        [switch]$AllUsers,
        [Parameter()]
        [switch]$AuditOnly,
        [Parameter()]
        [switch]$Apply,
        [Parameter()]
        [String]$DisplayLanguage,
        [Parameter()]
        [String]$RegionalFormat,
        [Parameter()]
        [String]$TimeZone,
        [Parameter()]
        [switch]$ExportHTML,
        [Parameter()]
        [switch]$ExportCSV,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'collaboration/microsoft365/azure-ad/Set-UserLanguageSettings.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/collaboration/microsoft365/azure-ad/Set-UserLanguageSettings.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Sync-UserGroupToPrimaryDeviceGroup {
    <#
    .SYNOPSIS
        Syncs users' primary Intune devices from a user group to a device group.
    .DESCRIPTION
        Proxies to scripts/utilities/Sync-UserGroupToPrimaryDeviceGroup.ps1.
        Original category: utilities
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Sync-UserGroupToPrimaryDeviceGroup -Verbose
    .NOTES
        Backing script: scripts/utilities/Sync-UserGroupToPrimaryDeviceGroup.ps1
        Category: utilities
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/utilities/Sync-UserGroupToPrimaryDeviceGroup.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [String]$SourceGroupName,
        [Parameter()]
        [String]$TargetGroupName,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'utilities/Sync-UserGroupToPrimaryDeviceGroup.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/utilities/Sync-UserGroupToPrimaryDeviceGroup.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-AdobeRum {
    <#
    .SYNOPSIS
        Detect Adobe Creative Cloud app updates via Remote Update Manager
    .DESCRIPTION
        Proxies to scripts/endpoints/devices/adobe-rum/detect.ps1.
        Original category: endpoints/devices/adobe-rum
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-AdobeRum -Verbose
    .NOTES
        Backing script: scripts/endpoints/devices/adobe-rum/detect.ps1
        Category: endpoints/devices/adobe-rum
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/devices/adobe-rum/detect.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/devices/adobe-rum/detect.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/devices/adobe-rum/detect.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-APIHealth {
    <#
    .SYNOPSIS
        Tests API endpoint health, performance, and compliance.
    .DESCRIPTION
        Proxies to scripts/data/api/Test-APIHealth.ps1.
        Original category: data/api
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-APIHealth -Verbose
    .NOTES
        Backing script: scripts/data/api/Test-APIHealth.ps1
        Category: data/api
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/data/api/Test-APIHealth.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [String]$EndpointsFile,
        [Parameter()]
        [String]$SingleEndpoint,
        [Parameter()]
        [String]$Method,
        [Parameter()]
        [Int32]$ExpectedStatusCode,
        [Parameter()]
        [Int32]$MaxResponseTime,
        [Parameter()]
        [String]$OutputFormat,
        [Parameter()]
        [String]$OutputPath,
        [Parameter()]
        [switch]$RunContinuous,
        [Parameter()]
        [Int32]$IntervalSeconds,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'data/api/Test-APIHealth.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/data/api/Test-APIHealth.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-BackupIntegrity {
    <#
    .SYNOPSIS
        Tests integrity and validity of Windows Server Backup archives.
    .DESCRIPTION
        Proxies to scripts/infrastructure/windows/backup-recovery/Test-BackupIntegrity.ps1.
        Original category: infrastructure/windows/backup-recovery
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-BackupIntegrity -Verbose
    .NOTES
        Backing script: scripts/infrastructure/windows/backup-recovery/Test-BackupIntegrity.ps1
        Category: infrastructure/windows/backup-recovery
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/infrastructure/windows/backup-recovery/Test-BackupIntegrity.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [String]$BackupLocation,
        [Parameter()]
        [switch]$TestRestore,
        [Parameter()]
        [switch]$CheckVSS,
        [Parameter()]
        [Int32]$DaysToCheck,
        [Parameter()]
        [switch]$ExportHTML,
        [Parameter()]
        [switch]$ExportCSV,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'infrastructure/windows/backup-recovery/Test-BackupIntegrity.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/infrastructure/windows/backup-recovery/Test-BackupIntegrity.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-BicepTemplates {
    <#
    .SYNOPSIS
        Validates and tests Azure Bicep templates for best practices and security.
    .DESCRIPTION
        Proxies to scripts/automation/iac/Test-BicepTemplates.ps1.
        Original category: automation/iac
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-BicepTemplates -Verbose
    .NOTES
        Backing script: scripts/automation/iac/Test-BicepTemplates.ps1
        Category: automation/iac
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/automation/iac/Test-BicepTemplates.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(Mandatory)]
        [String]$TemplatePath,
        [Parameter()]
        [String]$ParameterFile,
        [Parameter()]
        [switch]$IncludeWhatIf,
        [Parameter()]
        [String]$SubscriptionId,
        [Parameter()]
        [String]$ResourceGroupName,
        [Parameter()]
        [String]$OutputFormat,
        [Parameter()]
        [String]$OutputPath,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'automation/iac/Test-BicepTemplates.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/automation/iac/Test-BicepTemplates.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-BitLockerKeyBackup {
    <#
    .SYNOPSIS
        Verifies the Azure AD and Domain join status of a device.
    .DESCRIPTION
        Proxies to scripts/endpoints/devices/bitlocker/Detect_BitLockerKeyBackup.ps1.
        Original category: endpoints/devices/bitlocker
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-BitLockerKeyBackup -Verbose
    .NOTES
        Backing script: scripts/endpoints/devices/bitlocker/Detect_BitLockerKeyBackup.ps1
        Category: endpoints/devices/bitlocker
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/devices/bitlocker/Detect_BitLockerKeyBackup.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [Int32]$nDays,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/devices/bitlocker/Detect_BitLockerKeyBackup.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/devices/bitlocker/Detect_BitLockerKeyBackup.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-CertificateExpiration {
    <#
    .SYNOPSIS
        Monitors SSL/TLS certificate expiration for local and remote certificates.
    .DESCRIPTION
        Proxies to scripts/infrastructure/windows/security/Test-CertificateExpiration.ps1.
        Original category: infrastructure/windows/security
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-CertificateExpiration -Verbose
    .NOTES
        Backing script: scripts/infrastructure/windows/security/Test-CertificateExpiration.ps1
        Category: infrastructure/windows/security
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/infrastructure/windows/security/Test-CertificateExpiration.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [switch]$CheckLocal,
        [Parameter()]
        [switch]$CheckRemote,
        [Parameter(Mandatory)]
        [String[]]$Endpoints,
        [Parameter()]
        [Int32]$WarningDays,
        [Parameter()]
        [Int32]$CriticalDays,
        [Parameter()]
        [String]$CertStorePath,
        [Parameter()]
        [switch]$IncludeExpired,
        [Parameter()]
        [switch]$ExportHTML,
        [Parameter()]
        [switch]$ExportCSV,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'infrastructure/windows/security/Test-CertificateExpiration.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/infrastructure/windows/security/Test-CertificateExpiration.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-CISBenchmark {
    <#
    .SYNOPSIS
        CIS Benchmark compliance testing for Windows Server.
    .DESCRIPTION
        Proxies to scripts/security/compliance/frameworks/Test-CISBenchmark.ps1.
        Original category: security/compliance/frameworks
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-CISBenchmark -Verbose
    .NOTES
        Backing script: scripts/security/compliance/frameworks/Test-CISBenchmark.ps1
        Category: security/compliance/frameworks
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/security/compliance/frameworks/Test-CISBenchmark.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [String]$Level,
        [Parameter()]
        [switch]$ExportHTML,
        [Parameter()]
        [String]$OutputPath,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'security/compliance/frameworks/Test-CISBenchmark.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/security/compliance/frameworks/Test-CISBenchmark.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-DisableWindowsUpdateAccess {
    <#
    .SYNOPSIS
        Detect the NoAutoUpdate Windows Update policy (DisableWindowsUpdateAccess pair)
    .DESCRIPTION
        Proxies to scripts/endpoints/devices/autopatch/V1/DisableWindowsUpdateAccess/Detect.ps1.
        Original category: endpoints/devices/autopatch/V1/DisableWindowsUpdateAccess
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-DisableWindowsUpdateAccess -Verbose
    .NOTES
        Backing script: scripts/endpoints/devices/autopatch/V1/DisableWindowsUpdateAccess/Detect.ps1
        Category: endpoints/devices/autopatch/V1/DisableWindowsUpdateAccess
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/devices/autopatch/V1/DisableWindowsUpdateAccess/Detect.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/devices/autopatch/V1/DisableWindowsUpdateAccess/Detect.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/devices/autopatch/V1/DisableWindowsUpdateAccess/Detect.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-DoNotConnectToWindowsUpdateInternetLocations {
    <#
    .SYNOPSIS
        Detect the DoNotConnectToWindowsUpdateInternetLocations Windows Update policy
    .DESCRIPTION
        Proxies to scripts/endpoints/devices/autopatch/V1/DoNotConnectToWindowsUpdateInternetLocations/Detect.ps1.
        Original category: endpoints/devices/autopatch/V1/DoNotConnectToWindowsUpdateInternetLocations
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-DoNotConnectToWindowsUpdateInternetLocations -Verbose
    .NOTES
        Backing script: scripts/endpoints/devices/autopatch/V1/DoNotConnectToWindowsUpdateInternetLocations/Detect.ps1
        Category: endpoints/devices/autopatch/V1/DoNotConnectToWindowsUpdateInternetLocations
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/devices/autopatch/V1/DoNotConnectToWindowsUpdateInternetLocations/Detect.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/devices/autopatch/V1/DoNotConnectToWindowsUpdateInternetLocations/Detect.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/devices/autopatch/V1/DoNotConnectToWindowsUpdateInternetLocations/Detect.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-Drivers {
    <#
    .SYNOPSIS
        Detect if the AMD Radeon driver block is applied
    .DESCRIPTION
        Proxies to scripts/endpoints/devices/drivers/detect.ps1.
        Original category: endpoints/devices/drivers
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-Drivers -Verbose
    .NOTES
        Backing script: scripts/endpoints/devices/drivers/detect.ps1
        Category: endpoints/devices/drivers
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/devices/drivers/detect.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/devices/drivers/detect.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/devices/drivers/detect.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-IntuneConnectivity {
    <#
    .SYNOPSIS
        Tests connectivity to all required Intune service endpoints.
    .DESCRIPTION
        Proxies to scripts/endpoints/intune/maintenance/Test-IntuneConnectivity.ps1.
        Original category: endpoints/intune/maintenance
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-IntuneConnectivity -Verbose
    .NOTES
        Backing script: scripts/endpoints/intune/maintenance/Test-IntuneConnectivity.ps1
        Category: endpoints/intune/maintenance
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/intune/maintenance/Test-IntuneConnectivity.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/intune/maintenance/Test-IntuneConnectivity.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/intune/maintenance/Test-IntuneConnectivity.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-IntuneConnectivityTests {
    <#
    .SYNOPSIS
        Pester tests for Test-IntuneConnectivity.ps1.
    .DESCRIPTION
        Proxies to scripts/endpoints/intune/maintenance/Test-IntuneConnectivity.Tests.ps1.
        Original category: endpoints/intune/maintenance
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-IntuneConnectivityTests -Verbose
    .NOTES
        Backing script: scripts/endpoints/intune/maintenance/Test-IntuneConnectivity.Tests.ps1
        Category: endpoints/intune/maintenance
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/intune/maintenance/Test-IntuneConnectivity.Tests.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/intune/maintenance/Test-IntuneConnectivity.Tests.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/intune/maintenance/Test-IntuneConnectivity.Tests.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-NetworkConnectivity {
    <#
    .SYNOPSIS
        Comprehensive network connectivity and troubleshooting script.
    .DESCRIPTION
        Proxies to scripts/infrastructure/network/Test-NetworkConnectivity.ps1.
        Original category: infrastructure/network
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-NetworkConnectivity -Verbose
    .NOTES
        Backing script: scripts/infrastructure/network/Test-NetworkConnectivity.ps1
        Category: infrastructure/network
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/infrastructure/network/Test-NetworkConnectivity.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [String]$OutputFormat,
        [Parameter()]
        [String]$OutputPath,
        [Parameter()]
        [String[]]$TestEndpoints,
        [Parameter()]
        [String[]]$DNSServers,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'infrastructure/network/Test-NetworkConnectivity.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/infrastructure/network/Test-NetworkConnectivity.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-NetworkDiagnostics {
    <#
    .SYNOPSIS
        Performs comprehensive network diagnostics and troubleshooting.
    .DESCRIPTION
        Proxies to scripts/infrastructure/network/Test-NetworkDiagnostics.ps1.
        Original category: infrastructure/network
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-NetworkDiagnostics -Verbose
    .NOTES
        Backing script: scripts/infrastructure/network/Test-NetworkDiagnostics.ps1
        Category: infrastructure/network
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/infrastructure/network/Test-NetworkDiagnostics.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [String]$OutputPath,
        [Parameter()]
        [switch]$TestInternetConnectivity,
        [Parameter()]
        [switch]$TraceRoute,
        [Parameter()]
        [switch]$TestDNS,
        [Parameter()]
        [switch]$ScanPorts,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'infrastructure/network/Test-NetworkDiagnostics.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/infrastructure/network/Test-NetworkDiagnostics.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-NoAutoUpdate {
    <#
    .SYNOPSIS
        Detect the NoAutoUpdate Windows Update policy
    .DESCRIPTION
        Proxies to scripts/endpoints/devices/autopatch/V1/NoAutoUpdate/Detect.ps1.
        Original category: endpoints/devices/autopatch/V1/NoAutoUpdate
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-NoAutoUpdate -Verbose
    .NOTES
        Backing script: scripts/endpoints/devices/autopatch/V1/NoAutoUpdate/Detect.ps1
        Category: endpoints/devices/autopatch/V1/NoAutoUpdate
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/devices/autopatch/V1/NoAutoUpdate/Detect.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/devices/autopatch/V1/NoAutoUpdate/Detect.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/devices/autopatch/V1/NoAutoUpdate/Detect.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-RemediationCheckApplicationCrashes {
    <#
    .SYNOPSIS
        Monitors application crashes and hangs.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Test-RemediationCheckApplicationCrashes.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-RemediationCheckApplicationCrashes -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Test-RemediationCheckApplicationCrashes.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Test-RemediationCheckApplicationCrashes.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Test-RemediationCheckApplicationCrashes.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Test-RemediationCheckApplicationCrashes.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-RemediationCheckBatteryHealth {
    <#
    .SYNOPSIS
        Checks laptop battery health and capacity degradation.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Test-RemediationCheckBatteryHealth.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-RemediationCheckBatteryHealth -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Test-RemediationCheckBatteryHealth.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Test-RemediationCheckBatteryHealth.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Test-RemediationCheckBatteryHealth.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Test-RemediationCheckBatteryHealth.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-RemediationCheckBootPerformance {
    <#
    .SYNOPSIS
        Monitors Windows boot performance and startup time.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Test-RemediationCheckBootPerformance.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-RemediationCheckBootPerformance -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Test-RemediationCheckBootPerformance.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Test-RemediationCheckBootPerformance.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Test-RemediationCheckBootPerformance.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Test-RemediationCheckBootPerformance.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-RemediationCheckDefenderHealthStatus {
    <#
    .SYNOPSIS
        Detects if Windows Defender is healthy and properly configured.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/security/Test-RemediationCheckDefenderHealthStatus.ps1.
        Original category: endpoints/remediation/security
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-RemediationCheckDefenderHealthStatus -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/security/Test-RemediationCheckDefenderHealthStatus.ps1
        Category: endpoints/remediation/security
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/security/Test-RemediationCheckDefenderHealthStatus.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/security/Test-RemediationCheckDefenderHealthStatus.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/security/Test-RemediationCheckDefenderHealthStatus.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-RemediationCheckDeviceHealthScore {
    <#
    .SYNOPSIS
        Calculates comprehensive device health score for reporting.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Test-RemediationCheckDeviceHealthScore.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-RemediationCheckDeviceHealthScore -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Test-RemediationCheckDeviceHealthScore.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Test-RemediationCheckDeviceHealthScore.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Test-RemediationCheckDeviceHealthScore.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Test-RemediationCheckDeviceHealthScore.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-RemediationCheckDeviceUptime {
    <#
    .SYNOPSIS
        Monitors device uptime and recommends reboots for devices up too long.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Test-RemediationCheckDeviceUptime.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-RemediationCheckDeviceUptime -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Test-RemediationCheckDeviceUptime.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Test-RemediationCheckDeviceUptime.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Test-RemediationCheckDeviceUptime.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Test-RemediationCheckDeviceUptime.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-RemediationCheckDiskHealth {
    <#
    .SYNOPSIS
        Detects disk health issues.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Test-RemediationCheckDiskHealth.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-RemediationCheckDiskHealth -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Test-RemediationCheckDiskHealth.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Test-RemediationCheckDiskHealth.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Test-RemediationCheckDiskHealth.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Test-RemediationCheckDiskHealth.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-RemediationCheckHardwareErrors {
    <#
    .SYNOPSIS
        Monitors hardware errors and failures.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Test-RemediationCheckHardwareErrors.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-RemediationCheckHardwareErrors -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Test-RemediationCheckHardwareErrors.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Test-RemediationCheckHardwareErrors.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Test-RemediationCheckHardwareErrors.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Test-RemediationCheckHardwareErrors.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-RemediationCheckLocalAdminAccounts {
    <#
    .SYNOPSIS
        Detects unauthorized local administrator accounts.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/security/Test-RemediationCheckLocalAdminAccounts.ps1.
        Original category: endpoints/remediation/security
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-RemediationCheckLocalAdminAccounts -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/security/Test-RemediationCheckLocalAdminAccounts.ps1
        Category: endpoints/remediation/security
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/security/Test-RemediationCheckLocalAdminAccounts.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/security/Test-RemediationCheckLocalAdminAccounts.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/security/Test-RemediationCheckLocalAdminAccounts.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-RemediationCheckMemoryDiagnostics {
    <#
    .SYNOPSIS
        Checks for memory errors in event logs.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Test-RemediationCheckMemoryDiagnostics.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-RemediationCheckMemoryDiagnostics -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Test-RemediationCheckMemoryDiagnostics.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Test-RemediationCheckMemoryDiagnostics.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Test-RemediationCheckMemoryDiagnostics.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Test-RemediationCheckMemoryDiagnostics.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-RemediationCheckMicrosoftStoreAppsHealth {
    <#
    .SYNOPSIS
        Detects Microsoft Store apps registration issues.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Test-RemediationCheckMicrosoftStoreAppsHealth.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-RemediationCheckMicrosoftStoreAppsHealth -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Test-RemediationCheckMicrosoftStoreAppsHealth.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Test-RemediationCheckMicrosoftStoreAppsHealth.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Test-RemediationCheckMicrosoftStoreAppsHealth.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Test-RemediationCheckMicrosoftStoreAppsHealth.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-RemediationCheckOutdatedCriticalApps {
    <#
    .SYNOPSIS
        Detects outdated critical applications using winget.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/security/Test-RemediationCheckOutdatedCriticalApps.ps1.
        Original category: endpoints/remediation/security
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-RemediationCheckOutdatedCriticalApps -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/security/Test-RemediationCheckOutdatedCriticalApps.ps1
        Category: endpoints/remediation/security
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/security/Test-RemediationCheckOutdatedCriticalApps.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/security/Test-RemediationCheckOutdatedCriticalApps.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/security/Test-RemediationCheckOutdatedCriticalApps.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-RemediationCheckPageFileConfiguration {
    <#
    .SYNOPSIS
        Checks page file configuration.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Test-RemediationCheckPageFileConfiguration.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-RemediationCheckPageFileConfiguration -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Test-RemediationCheckPageFileConfiguration.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Test-RemediationCheckPageFileConfiguration.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Test-RemediationCheckPageFileConfiguration.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Test-RemediationCheckPageFileConfiguration.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-RemediationCheckSecurityBaseline {
    <#
    .SYNOPSIS
        Detect security baseline drift
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/security/Test-RemediationCheckSecurityBaseline.ps1.
        Original category: endpoints/remediation/security
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-RemediationCheckSecurityBaseline -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/security/Test-RemediationCheckSecurityBaseline.ps1
        Category: endpoints/remediation/security
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/security/Test-RemediationCheckSecurityBaseline.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/security/Test-RemediationCheckSecurityBaseline.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/security/Test-RemediationCheckSecurityBaseline.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-RemediationCheckServiceFailures {
    <#
    .SYNOPSIS
        Monitors Windows service failures and crashes.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Test-RemediationCheckServiceFailures.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-RemediationCheckServiceFailures -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Test-RemediationCheckServiceFailures.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Test-RemediationCheckServiceFailures.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Test-RemediationCheckServiceFailures.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Test-RemediationCheckServiceFailures.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-RemediationCheckSharedFolders {
    <#
    .SYNOPSIS
        Detects unauthorized network shares on the device.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/network/Test-RemediationCheckSharedFolders.ps1.
        Original category: endpoints/remediation/network
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-RemediationCheckSharedFolders -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/network/Test-RemediationCheckSharedFolders.ps1
        Category: endpoints/remediation/network
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/network/Test-RemediationCheckSharedFolders.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/network/Test-RemediationCheckSharedFolders.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/network/Test-RemediationCheckSharedFolders.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-RemediationCheckSystemEventErrors {
    <#
    .SYNOPSIS
        Monitors critical system event log errors.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Test-RemediationCheckSystemEventErrors.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-RemediationCheckSystemEventErrors -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Test-RemediationCheckSystemEventErrors.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Test-RemediationCheckSystemEventErrors.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Test-RemediationCheckSystemEventErrors.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Test-RemediationCheckSystemEventErrors.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-RemediationCheckSystemStabilityIndex {
    <#
    .SYNOPSIS
        Checks Windows Reliability Monitor stability index score.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Test-RemediationCheckSystemStabilityIndex.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-RemediationCheckSystemStabilityIndex -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Test-RemediationCheckSystemStabilityIndex.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Test-RemediationCheckSystemStabilityIndex.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Test-RemediationCheckSystemStabilityIndex.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Test-RemediationCheckSystemStabilityIndex.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-RemediationCheckTPMStatus {
    <#
    .SYNOPSIS
        Detects TPM (Trusted Platform Module) issues.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/security/Test-RemediationCheckTPMStatus.ps1.
        Original category: endpoints/remediation/security
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-RemediationCheckTPMStatus -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/security/Test-RemediationCheckTPMStatus.ps1
        Category: endpoints/remediation/security
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/security/Test-RemediationCheckTPMStatus.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/security/Test-RemediationCheckTPMStatus.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/security/Test-RemediationCheckTPMStatus.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-RemediationCheckUnexpectedReboots {
    <#
    .SYNOPSIS
        Detects unexpected system reboots and crashes.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Test-RemediationCheckUnexpectedReboots.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-RemediationCheckUnexpectedReboots -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Test-RemediationCheckUnexpectedReboots.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Test-RemediationCheckUnexpectedReboots.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Test-RemediationCheckUnexpectedReboots.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Test-RemediationCheckUnexpectedReboots.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-RemediationCheckWindowsActivationGracePeriod {
    <#
    .SYNOPSIS
        Checks Windows activation grace period status.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Test-RemediationCheckWindowsActivationGracePeriod.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-RemediationCheckWindowsActivationGracePeriod -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Test-RemediationCheckWindowsActivationGracePeriod.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Test-RemediationCheckWindowsActivationGracePeriod.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Test-RemediationCheckWindowsActivationGracePeriod.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Test-RemediationCheckWindowsActivationGracePeriod.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-RemediationFixBitLockerNotEscrowedKeys {
    <#
    .SYNOPSIS
        Detect BitLocker volumes missing a recovery password protector
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/security/Test-RemediationFixBitLockerNotEscrowedKeys.ps1.
        Original category: endpoints/remediation/security
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-RemediationFixBitLockerNotEscrowedKeys -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/security/Test-RemediationFixBitLockerNotEscrowedKeys.ps1
        Category: endpoints/remediation/security
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/security/Test-RemediationFixBitLockerNotEscrowedKeys.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/security/Test-RemediationFixBitLockerNotEscrowedKeys.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/security/Test-RemediationFixBitLockerNotEscrowedKeys.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-RemediationFixBrokenShortcuts {
    <#
    .SYNOPSIS
        Detects broken shortcuts on Desktop and Start Menu.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Test-RemediationFixBrokenShortcuts.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-RemediationFixBrokenShortcuts -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Test-RemediationFixBrokenShortcuts.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Test-RemediationFixBrokenShortcuts.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Test-RemediationFixBrokenShortcuts.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Test-RemediationFixBrokenShortcuts.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-RemediationFixCertificateExpiry {
    <#
    .SYNOPSIS
        Detects expired or expiring certificates.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/security/Test-RemediationFixCertificateExpiry.ps1.
        Original category: endpoints/remediation/security
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-RemediationFixCertificateExpiry -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/security/Test-RemediationFixCertificateExpiry.ps1
        Category: endpoints/remediation/security
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/security/Test-RemediationFixCertificateExpiry.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/security/Test-RemediationFixCertificateExpiry.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/security/Test-RemediationFixCertificateExpiry.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-RemediationFixCredentialManager {
    <#
    .SYNOPSIS
        Detects stale or orphaned credentials in Windows Credential Manager.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Test-RemediationFixCredentialManager.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-RemediationFixCredentialManager -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Test-RemediationFixCredentialManager.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Test-RemediationFixCredentialManager.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Test-RemediationFixCredentialManager.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Test-RemediationFixCredentialManager.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-RemediationFixDiskSpace {
    <#
    .SYNOPSIS
        Detect low disk space on fixed drives
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Test-RemediationFixDiskSpace.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-RemediationFixDiskSpace -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Test-RemediationFixDiskSpace.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Test-RemediationFixDiskSpace.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Test-RemediationFixDiskSpace.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Test-RemediationFixDiskSpace.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-RemediationFixDNSCache {
    <#
    .SYNOPSIS
        Detects DNS resolution issues by testing connectivity to common domains.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/network/Test-RemediationFixDNSCache.ps1.
        Original category: endpoints/remediation/network
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-RemediationFixDNSCache -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/network/Test-RemediationFixDNSCache.ps1
        Category: endpoints/remediation/network
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/network/Test-RemediationFixDNSCache.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/network/Test-RemediationFixDNSCache.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/network/Test-RemediationFixDNSCache.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-RemediationFixEdgeCacheSize {
    <#
    .SYNOPSIS
        Detects bloated Microsoft Edge browser cache.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Test-RemediationFixEdgeCacheSize.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-RemediationFixEdgeCacheSize -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Test-RemediationFixEdgeCacheSize.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Test-RemediationFixEdgeCacheSize.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Test-RemediationFixEdgeCacheSize.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Test-RemediationFixEdgeCacheSize.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-RemediationFixEventLogSize {
    <#
    .SYNOPSIS
        Detects bloated Windows Event Logs.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Test-RemediationFixEventLogSize.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-RemediationFixEventLogSize -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Test-RemediationFixEventLogSize.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Test-RemediationFixEventLogSize.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Test-RemediationFixEventLogSize.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Test-RemediationFixEventLogSize.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-RemediationFixNetworkAdapterPowerManagement {
    <#
    .SYNOPSIS
        Detects network adapters with problematic power management settings.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/network/Test-RemediationFixNetworkAdapterPowerManagement.ps1.
        Original category: endpoints/remediation/network
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-RemediationFixNetworkAdapterPowerManagement -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/network/Test-RemediationFixNetworkAdapterPowerManagement.ps1
        Category: endpoints/remediation/network
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/network/Test-RemediationFixNetworkAdapterPowerManagement.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/network/Test-RemediationFixNetworkAdapterPowerManagement.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/network/Test-RemediationFixNetworkAdapterPowerManagement.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-RemediationFixOneDriveKnownFolderMove {
    <#
    .SYNOPSIS
        Detects OneDrive Known Folder Move (KFM) issues.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Test-RemediationFixOneDriveKnownFolderMove.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-RemediationFixOneDriveKnownFolderMove -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Test-RemediationFixOneDriveKnownFolderMove.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Test-RemediationFixOneDriveKnownFolderMove.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Test-RemediationFixOneDriveKnownFolderMove.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Test-RemediationFixOneDriveKnownFolderMove.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-RemediationFixOutdatedDrivers {
    <#
    .SYNOPSIS
        Detects outdated or missing critical drivers.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Test-RemediationFixOutdatedDrivers.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-RemediationFixOutdatedDrivers -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Test-RemediationFixOutdatedDrivers.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Test-RemediationFixOutdatedDrivers.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Test-RemediationFixOutdatedDrivers.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Test-RemediationFixOutdatedDrivers.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-RemediationFixPowerShellExecutionPolicy {
    <#
    .SYNOPSIS
        Detects improper PowerShell execution policy settings.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/security/Test-RemediationFixPowerShellExecutionPolicy.ps1.
        Original category: endpoints/remediation/security
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-RemediationFixPowerShellExecutionPolicy -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/security/Test-RemediationFixPowerShellExecutionPolicy.ps1
        Category: endpoints/remediation/security
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/security/Test-RemediationFixPowerShellExecutionPolicy.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/security/Test-RemediationFixPowerShellExecutionPolicy.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/security/Test-RemediationFixPowerShellExecutionPolicy.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-RemediationFixPrintSpooler {
    <#
    .SYNOPSIS
        Detects issues with the Print Spooler service.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Test-RemediationFixPrintSpooler.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-RemediationFixPrintSpooler -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Test-RemediationFixPrintSpooler.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Test-RemediationFixPrintSpooler.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Test-RemediationFixPrintSpooler.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Test-RemediationFixPrintSpooler.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-RemediationFixSMBv1Protocol {
    <#
    .SYNOPSIS
        Detects if SMBv1 protocol is enabled.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/network/Test-RemediationFixSMBv1Protocol.ps1.
        Original category: endpoints/remediation/network
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-RemediationFixSMBv1Protocol -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/network/Test-RemediationFixSMBv1Protocol.ps1
        Category: endpoints/remediation/network
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/network/Test-RemediationFixSMBv1Protocol.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/network/Test-RemediationFixSMBv1Protocol.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/network/Test-RemediationFixSMBv1Protocol.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-RemediationFixStaleProfiles {
    <#
    .SYNOPSIS
        Detect stale user profiles not used recently
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Test-RemediationFixStaleProfiles.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-RemediationFixStaleProfiles -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Test-RemediationFixStaleProfiles.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Test-RemediationFixStaleProfiles.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Test-RemediationFixStaleProfiles.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Test-RemediationFixStaleProfiles.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-RemediationFixStartMenuLayout {
    <#
    .SYNOPSIS
        Detects corrupted Start Menu layout.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Test-RemediationFixStartMenuLayout.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-RemediationFixStartMenuLayout -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Test-RemediationFixStartMenuLayout.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Test-RemediationFixStartMenuLayout.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Test-RemediationFixStartMenuLayout.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Test-RemediationFixStartMenuLayout.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-RemediationFixSystemFileCorruption {
    <#
    .SYNOPSIS
        Detects Windows system file corruption.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Test-RemediationFixSystemFileCorruption.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-RemediationFixSystemFileCorruption -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Test-RemediationFixSystemFileCorruption.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Test-RemediationFixSystemFileCorruption.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Test-RemediationFixSystemFileCorruption.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Test-RemediationFixSystemFileCorruption.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-RemediationFixTaskSchedulerCorruption {
    <#
    .SYNOPSIS
        Detects Task Scheduler corruption or service issues.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Test-RemediationFixTaskSchedulerCorruption.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-RemediationFixTaskSchedulerCorruption -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Test-RemediationFixTaskSchedulerCorruption.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Test-RemediationFixTaskSchedulerCorruption.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Test-RemediationFixTaskSchedulerCorruption.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Test-RemediationFixTaskSchedulerCorruption.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-RemediationFixTeamsCache {
    <#
    .SYNOPSIS
        Detects Microsoft Teams cache issues.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Test-RemediationFixTeamsCache.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-RemediationFixTeamsCache -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Test-RemediationFixTeamsCache.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Test-RemediationFixTeamsCache.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Test-RemediationFixTeamsCache.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Test-RemediationFixTeamsCache.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-RemediationFixTempFiles {
    <#
    .SYNOPSIS
        Detect excessive temp files
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Test-RemediationFixTempFiles.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-RemediationFixTempFiles -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Test-RemediationFixTempFiles.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Test-RemediationFixTempFiles.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Test-RemediationFixTempFiles.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Test-RemediationFixTempFiles.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-RemediationFixTimeSync {
    <#
    .SYNOPSIS
        Detects time synchronization issues on Windows devices.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Test-RemediationFixTimeSync.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-RemediationFixTimeSync -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Test-RemediationFixTimeSync.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Test-RemediationFixTimeSync.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Test-RemediationFixTimeSync.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Test-RemediationFixTimeSync.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-RemediationFixWindowsLicenseActivation {
    <#
    .SYNOPSIS
        Detects Windows license activation issues.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Test-RemediationFixWindowsLicenseActivation.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-RemediationFixWindowsLicenseActivation -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Test-RemediationFixWindowsLicenseActivation.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Test-RemediationFixWindowsLicenseActivation.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Test-RemediationFixWindowsLicenseActivation.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Test-RemediationFixWindowsLicenseActivation.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-RemediationFixWindowsPerformanceRecorder {
    <#
    .SYNOPSIS
        Detects stuck Windows Performance Recorder (WPR) or ETW sessions.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Test-RemediationFixWindowsPerformanceRecorder.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-RemediationFixWindowsPerformanceRecorder -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Test-RemediationFixWindowsPerformanceRecorder.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Test-RemediationFixWindowsPerformanceRecorder.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Test-RemediationFixWindowsPerformanceRecorder.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Test-RemediationFixWindowsPerformanceRecorder.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-RemediationFixWindowsSearch {
    <#
    .SYNOPSIS
        Detects issues with Windows Search service.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Test-RemediationFixWindowsSearch.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-RemediationFixWindowsSearch -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Test-RemediationFixWindowsSearch.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Test-RemediationFixWindowsSearch.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Test-RemediationFixWindowsSearch.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Test-RemediationFixWindowsSearch.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-RemediationFixWindowsStoreLicensing {
    <#
    .SYNOPSIS
        Detects Windows Store app licensing issues.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Test-RemediationFixWindowsStoreLicensing.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-RemediationFixWindowsStoreLicensing -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Test-RemediationFixWindowsStoreLicensing.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Test-RemediationFixWindowsStoreLicensing.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Test-RemediationFixWindowsStoreLicensing.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Test-RemediationFixWindowsStoreLicensing.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-RemediationFixWindowsUpdateRebootPending {
    <#
    .SYNOPSIS
        Detects stuck "reboot pending" state for Windows Update.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Test-RemediationFixWindowsUpdateRebootPending.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-RemediationFixWindowsUpdateRebootPending -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Test-RemediationFixWindowsUpdateRebootPending.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Test-RemediationFixWindowsUpdateRebootPending.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Test-RemediationFixWindowsUpdateRebootPending.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Test-RemediationFixWindowsUpdateRebootPending.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-RemediationFixWindowsUpdateStuck {
    <#
    .SYNOPSIS
        Detect Windows Update stuck for more than 7 days
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Test-RemediationFixWindowsUpdateStuck.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-RemediationFixWindowsUpdateStuck -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Test-RemediationFixWindowsUpdateStuck.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Test-RemediationFixWindowsUpdateStuck.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Test-RemediationFixWindowsUpdateStuck.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Test-RemediationFixWindowsUpdateStuck.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-RemediationKeyboardLayout {
    <#
    .SYNOPSIS
        Detect if a UK keyboard layout is the primary input method
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Test-RemediationKeyboardLayout.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-RemediationKeyboardLayout -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Test-RemediationKeyboardLayout.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Test-RemediationKeyboardLayout.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Test-RemediationKeyboardLayout.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Test-RemediationKeyboardLayout.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-RemediationLanguagePackAudit {
    <#
    .SYNOPSIS
        Detect unnecessary OS language packs
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Test-RemediationLanguagePackAudit.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-RemediationLanguagePackAudit -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Test-RemediationLanguagePackAudit.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Test-RemediationLanguagePackAudit.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Test-RemediationLanguagePackAudit.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Test-RemediationLanguagePackAudit.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-RemediationRegionLanguageSettings {
    <#
    .SYNOPSIS
        Detect if regional settings match UK standards
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/system/Test-RemediationRegionLanguageSettings.ps1.
        Original category: endpoints/remediation/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-RemediationRegionLanguageSettings -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/system/Test-RemediationRegionLanguageSettings.ps1
        Category: endpoints/remediation/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/system/Test-RemediationRegionLanguageSettings.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/system/Test-RemediationRegionLanguageSettings.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/system/Test-RemediationRegionLanguageSettings.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-Sccm {
    <#
    .SYNOPSIS
        Detect if the SCCM client is installed
    .DESCRIPTION
        Proxies to scripts/endpoints/devices/sccm/detect.ps1.
        Original category: endpoints/devices/sccm
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-Sccm -Verbose
    .NOTES
        Backing script: scripts/endpoints/devices/sccm/detect.ps1
        Category: endpoints/devices/sccm
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/devices/sccm/detect.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/devices/sccm/detect.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/devices/sccm/detect.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-SecurityFeatures {
    <#
    .SYNOPSIS
        Tests modern Windows security features and configurations.
    .DESCRIPTION
        Proxies to scripts/security/compliance/frameworks/Test-SecurityFeatures.ps1.
        Original category: security/compliance/frameworks
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-SecurityFeatures -Verbose
    .NOTES
        Backing script: scripts/security/compliance/frameworks/Test-SecurityFeatures.ps1
        Category: security/compliance/frameworks
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/security/compliance/frameworks/Test-SecurityFeatures.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [switch]$ShowRecommendations,
        [Parameter()]
        [switch]$ExportReport,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'security/compliance/frameworks/Test-SecurityFeatures.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/security/compliance/frameworks/Test-SecurityFeatures.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-ServerConnectivity {
    <#
    .SYNOPSIS
        Tests network connectivity to remote servers including ping, port checks, and DNS resolution.
    .DESCRIPTION
        Proxies to scripts/infrastructure/windows/network/Test-ServerConnectivity.ps1.
        Original category: infrastructure/windows/network
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-ServerConnectivity -Verbose
    .NOTES
        Backing script: scripts/infrastructure/windows/network/Test-ServerConnectivity.ps1
        Category: infrastructure/windows/network
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/infrastructure/windows/network/Test-ServerConnectivity.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(Mandatory)]
        [String[]]$ComputerName,
        [Parameter()]
        [Int32[]]$Port,
        [Parameter()]
        [Int32]$PingCount,
        [Parameter()]
        [Int32]$Timeout,
        [Parameter()]
        [switch]$IncludeTraceRoute,
        [Parameter()]
        [switch]$ExportHTML,
        [Parameter()]
        [switch]$ExportCSV,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'infrastructure/windows/network/Test-ServerConnectivity.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/infrastructure/windows/network/Test-ServerConnectivity.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-ServerHardening {
    <#
    .SYNOPSIS
        Checks Windows Server security hardening compliance against industry best practices.
    .DESCRIPTION
        Proxies to scripts/infrastructure/windows/security/Test-ServerHardening.ps1.
        Original category: infrastructure/windows/security
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-ServerHardening -Verbose
    .NOTES
        Backing script: scripts/infrastructure/windows/security/Test-ServerHardening.ps1
        Category: infrastructure/windows/security
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/infrastructure/windows/security/Test-ServerHardening.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [String]$Baseline,
        [Parameter()]
        [switch]$IncludeRemediation,
        [Parameter()]
        [switch]$ExportHTML,
        [Parameter()]
        [switch]$ExportCSV,
        [Parameter()]
        [switch]$AutoFix,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'infrastructure/windows/security/Test-ServerHardening.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/infrastructure/windows/security/Test-ServerHardening.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-SystemIntegrity {
    <#
    .SYNOPSIS
        Performs comprehensive system integrity checks on Windows Server 2016-2022.
    .DESCRIPTION
        Proxies to scripts/infrastructure/windows/system/Check-SystemIntegrity.ps1.
        Original category: infrastructure/windows/system
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-SystemIntegrity -Verbose
    .NOTES
        Backing script: scripts/infrastructure/windows/system/Check-SystemIntegrity.ps1
        Category: infrastructure/windows/system
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/infrastructure/windows/system/Check-SystemIntegrity.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [switch]$QuickScan,
        [Parameter()]
        [switch]$AutoRepair,
        [Parameter()]
        [switch]$GenerateReport,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'infrastructure/windows/system/Check-SystemIntegrity.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/infrastructure/windows/system/Check-SystemIntegrity.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-TemplatesWinget {
    <#
    .SYNOPSIS
        Winget update detection template
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/_templates/detect.ps1.
        Original category: endpoints/remediation/winget/_templates
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-TemplatesWinget -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/_templates/detect.ps1
        Category: endpoints/remediation/winget/_templates
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/_templates/detect.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/_templates/detect.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/_templates/detect.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-TerraformConfiguration {
    <#
    .SYNOPSIS
        Validates Terraform configurations for syntax, security, and best practices.
    .DESCRIPTION
        Proxies to scripts/automation/iac/Test-TerraformConfiguration.ps1.
        Original category: automation/iac
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-TerraformConfiguration -Verbose
    .NOTES
        Backing script: scripts/automation/iac/Test-TerraformConfiguration.ps1
        Category: automation/iac
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/automation/iac/Test-TerraformConfiguration.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(Mandatory)]
        [String]$ConfigPath,
        [Parameter()]
        [switch]$IncludePlan,
        [Parameter()]
        [switch]$IncludeSecurityScan,
        [Parameter()]
        [String]$OutputFormat,
        [Parameter()]
        [String]$OutputPath,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'automation/iac/Test-TerraformConfiguration.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/automation/iac/Test-TerraformConfiguration.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-Uptime {
    <#
    .SYNOPSIS
        Detect if the device has not been restarted for 4 or more days
    .DESCRIPTION
        Proxies to scripts/endpoints/devices/uptime/detect.ps1.
        Original category: endpoints/devices/uptime
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-Uptime -Verbose
    .NOTES
        Backing script: scripts/endpoints/devices/uptime/detect.ps1
        Category: endpoints/devices/uptime
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/devices/uptime/detect.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/devices/uptime/detect.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/devices/uptime/detect.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-UseWUServer {
    <#
    .SYNOPSIS
        Detect the UseWUServer Windows Update policy
    .DESCRIPTION
        Proxies to scripts/endpoints/devices/autopatch/V1/UseWUServer/Detect.ps1.
        Original category: endpoints/devices/autopatch/V1/UseWUServer
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-UseWUServer -Verbose
    .NOTES
        Backing script: scripts/endpoints/devices/autopatch/V1/UseWUServer/Detect.ps1
        Category: endpoints/devices/autopatch/V1/UseWUServer
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/devices/autopatch/V1/UseWUServer/Detect.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/devices/autopatch/V1/UseWUServer/Detect.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/devices/autopatch/V1/UseWUServer/Detect.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-V1LegacyWinget {
    <#
    .SYNOPSIS
        Legacy winget update detection template (V1)
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/_templates/detect_v1_legacy.ps1.
        Original category: endpoints/remediation/winget/_templates
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-V1LegacyWinget -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/_templates/detect_v1_legacy.ps1
        Category: endpoints/remediation/winget/_templates
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/_templates/detect_v1_legacy.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/_templates/detect_v1_legacy.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/_templates/detect_v1_legacy.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-V2Winget {
    <#
    .SYNOPSIS
        Winget update detection script for Intune Proactive Remediations.
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/_templates/detect_v2.ps1.
        Original category: endpoints/remediation/winget/_templates
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-V2Winget -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/_templates/detect_v2.ps1
        Category: endpoints/remediation/winget/_templates
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/_templates/detect_v2.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/_templates/detect_v2.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/_templates/detect_v2.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-V3 {
    <#
    .SYNOPSIS
        Detect Windows Update policy drift (Autopatch V3)
    .DESCRIPTION
        Proxies to scripts/endpoints/devices/autopatch/V3/detect.ps1.
        Original category: endpoints/devices/autopatch/V3
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-V3 -Verbose
    .NOTES
        Backing script: scripts/endpoints/devices/autopatch/V3/detect.ps1
        Category: endpoints/devices/autopatch/V3
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/devices/autopatch/V3/detect.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/devices/autopatch/V3/detect.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/devices/autopatch/V3/detect.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-V3Winget {
    <#
    .SYNOPSIS
        Enhanced winget update detection script for Intune Proactive Remediations (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/_templates/detect_v3.ps1.
        Original category: endpoints/remediation/winget/_templates
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-V3Winget -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/_templates/detect_v3.ps1
        Category: endpoints/remediation/winget/_templates
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/_templates/detect_v3.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/_templates/detect_v3.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/_templates/detect_v3.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-V4 {
    <#
    .SYNOPSIS
        Detect Windows Update policy drift (Autopatch V4)
    .DESCRIPTION
        Proxies to scripts/endpoints/devices/autopatch/V4/detect.ps1.
        Original category: endpoints/devices/autopatch/V4
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-V4 -Verbose
    .NOTES
        Backing script: scripts/endpoints/devices/autopatch/V4/detect.ps1
        Category: endpoints/devices/autopatch/V4
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/devices/autopatch/V4/detect.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/devices/autopatch/V4/detect.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/devices/autopatch/V4/detect.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-V5 {
    <#
    .SYNOPSIS
        Detect Windows Update policy drift (Autopatch V5)
    .DESCRIPTION
        Proxies to scripts/endpoints/devices/autopatch/V5/detect.ps1.
        Original category: endpoints/devices/autopatch/V5
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-V5 -Verbose
    .NOTES
        Backing script: scripts/endpoints/devices/autopatch/V5/detect.ps1
        Category: endpoints/devices/autopatch/V5
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/devices/autopatch/V5/detect.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/devices/autopatch/V5/detect.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/devices/autopatch/V5/detect.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-Winget1Password {
    <#
    .SYNOPSIS
        Enhanced winget update detection script for Intune Proactive Remediations (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/security/1Password/Test-Winget1Password.ps1.
        Original category: endpoints/remediation/winget/security/1Password
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-Winget1Password -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/security/1Password/Test-Winget1Password.ps1
        Category: endpoints/remediation/winget/security/1Password
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/security/1Password/Test-Winget1Password.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/security/1Password/Test-Winget1Password.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/security/1Password/Test-Winget1Password.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-WingetAdobeReader32bit {
    <#
    .SYNOPSIS
        Enhanced winget update detection script for Intune Proactive Remediations (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/productivity/AdobeReader32bit/Test-WingetAdobeReader32bit.ps1.
        Original category: endpoints/remediation/winget/productivity/AdobeReader32bit
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-WingetAdobeReader32bit -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/productivity/AdobeReader32bit/Test-WingetAdobeReader32bit.ps1
        Category: endpoints/remediation/winget/productivity/AdobeReader32bit
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/productivity/AdobeReader32bit/Test-WingetAdobeReader32bit.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/productivity/AdobeReader32bit/Test-WingetAdobeReader32bit.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/productivity/AdobeReader32bit/Test-WingetAdobeReader32bit.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-WingetAdobeReader64bit {
    <#
    .SYNOPSIS
        Winget update detection script for Adobe Acrobat Reader 64-bit (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/productivity/AdobeReader64bit/Test-WingetAdobeReader64bit.ps1.
        Original category: endpoints/remediation/winget/productivity/AdobeReader64bit
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-WingetAdobeReader64bit -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/productivity/AdobeReader64bit/Test-WingetAdobeReader64bit.ps1
        Category: endpoints/remediation/winget/productivity/AdobeReader64bit
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/productivity/AdobeReader64bit/Test-WingetAdobeReader64bit.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/productivity/AdobeReader64bit/Test-WingetAdobeReader64bit.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/productivity/AdobeReader64bit/Test-WingetAdobeReader64bit.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-WingetAzureCLI {
    <#
    .SYNOPSIS
        Winget update detection script for Azure CLI (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/development/AzureCLI/Test-WingetAzureCLI.ps1.
        Original category: endpoints/remediation/winget/development/AzureCLI
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-WingetAzureCLI -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/development/AzureCLI/Test-WingetAzureCLI.ps1
        Category: endpoints/remediation/winget/development/AzureCLI
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/development/AzureCLI/Test-WingetAzureCLI.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/development/AzureCLI/Test-WingetAzureCLI.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/development/AzureCLI/Test-WingetAzureCLI.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-WingetCpp2008Redist {
    <#
    .SYNOPSIS
        Winget update detection script for C++ 2008 Redist (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/runtimes/Cpp2008Redist/Test-WingetCpp2008Redist.ps1.
        Original category: endpoints/remediation/winget/runtimes/Cpp2008Redist
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-WingetCpp2008Redist -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/runtimes/Cpp2008Redist/Test-WingetCpp2008Redist.ps1
        Category: endpoints/remediation/winget/runtimes/Cpp2008Redist
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/runtimes/Cpp2008Redist/Test-WingetCpp2008Redist.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/runtimes/Cpp2008Redist/Test-WingetCpp2008Redist.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/runtimes/Cpp2008Redist/Test-WingetCpp2008Redist.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-WingetCpp2010Redist {
    <#
    .SYNOPSIS
        Winget update detection script for Microsoft.VCRedist.2010.x86 (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/runtimes/Cpp2010Redist/Test-WingetCpp2010Redist.ps1.
        Original category: endpoints/remediation/winget/runtimes/Cpp2010Redist
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-WingetCpp2010Redist -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/runtimes/Cpp2010Redist/Test-WingetCpp2010Redist.ps1
        Category: endpoints/remediation/winget/runtimes/Cpp2010Redist
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/runtimes/Cpp2010Redist/Test-WingetCpp2010Redist.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/runtimes/Cpp2010Redist/Test-WingetCpp2010Redist.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/runtimes/Cpp2010Redist/Test-WingetCpp2010Redist.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-WingetCpp2012Redist {
    <#
    .SYNOPSIS
        Winget update detection script for Microsoft.VCRedist.2012.x64 (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/runtimes/Cpp2012Redist/Test-WingetCpp2012Redist.ps1.
        Original category: endpoints/remediation/winget/runtimes/Cpp2012Redist
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-WingetCpp2012Redist -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/runtimes/Cpp2012Redist/Test-WingetCpp2012Redist.ps1
        Category: endpoints/remediation/winget/runtimes/Cpp2012Redist
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/runtimes/Cpp2012Redist/Test-WingetCpp2012Redist.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/runtimes/Cpp2012Redist/Test-WingetCpp2012Redist.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/runtimes/Cpp2012Redist/Test-WingetCpp2012Redist.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-WingetCpp2013RedistX64 {
    <#
    .SYNOPSIS
        Winget update detection script for Microsoft.VCRedist.2013.x64 (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/runtimes/Cpp2013Redist-x64/Test-WingetCpp2013RedistX64.ps1.
        Original category: endpoints/remediation/winget/runtimes/Cpp2013Redist-x64
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-WingetCpp2013RedistX64 -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/runtimes/Cpp2013Redist-x64/Test-WingetCpp2013RedistX64.ps1
        Category: endpoints/remediation/winget/runtimes/Cpp2013Redist-x64
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/runtimes/Cpp2013Redist-x64/Test-WingetCpp2013RedistX64.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/runtimes/Cpp2013Redist-x64/Test-WingetCpp2013RedistX64.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/runtimes/Cpp2013Redist-x64/Test-WingetCpp2013RedistX64.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-WingetCpp2013RedistX86 {
    <#
    .SYNOPSIS
        Winget update detection script for Microsoft.VCRedist.2013.x86 (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/runtimes/Cpp2013Redist-x86/Test-WingetCpp2013RedistX86.ps1.
        Original category: endpoints/remediation/winget/runtimes/Cpp2013Redist-x86
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-WingetCpp2013RedistX86 -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/runtimes/Cpp2013Redist-x86/Test-WingetCpp2013RedistX86.ps1
        Category: endpoints/remediation/winget/runtimes/Cpp2013Redist-x86
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/runtimes/Cpp2013Redist-x86/Test-WingetCpp2013RedistX86.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/runtimes/Cpp2013Redist-x86/Test-WingetCpp2013RedistX86.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/runtimes/Cpp2013Redist-x86/Test-WingetCpp2013RedistX86.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-WingetCpp20152019RedistX64 {
    <#
    .SYNOPSIS
        Winget update detection script for Microsoft.VCRedist.2015+.x86 (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/runtimes/Cpp2015-2019Redist-x64/Test-WingetCpp20152019RedistX64.ps1.
        Original category: endpoints/remediation/winget/runtimes/Cpp2015-2019Redist-x64
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-WingetCpp20152019RedistX64 -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/runtimes/Cpp2015-2019Redist-x64/Test-WingetCpp20152019RedistX64.ps1
        Category: endpoints/remediation/winget/runtimes/Cpp2015-2019Redist-x64
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/runtimes/Cpp2015-2019Redist-x64/Test-WingetCpp20152019RedistX64.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/runtimes/Cpp2015-2019Redist-x64/Test-WingetCpp20152019RedistX64.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/runtimes/Cpp2015-2019Redist-x64/Test-WingetCpp20152019RedistX64.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-WingetCpp20152019RedistX86 {
    <#
    .SYNOPSIS
        Winget update detection script for Microsoft.VCRedist.2015+.x64 (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/runtimes/Cpp2015-2019Redist-x86/Test-WingetCpp20152019RedistX86.ps1.
        Original category: endpoints/remediation/winget/runtimes/Cpp2015-2019Redist-x86
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-WingetCpp20152019RedistX86 -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/runtimes/Cpp2015-2019Redist-x86/Test-WingetCpp20152019RedistX86.ps1
        Category: endpoints/remediation/winget/runtimes/Cpp2015-2019Redist-x86
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/runtimes/Cpp2015-2019Redist-x86/Test-WingetCpp20152019RedistX86.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/runtimes/Cpp2015-2019Redist-x86/Test-WingetCpp20152019RedistX86.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/runtimes/Cpp2015-2019Redist-x86/Test-WingetCpp20152019RedistX86.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-WingetDiscord {
    <#
    .SYNOPSIS
        Enhanced winget update detection script for Intune Proactive Remediations (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/communication/Discord/Test-WingetDiscord.ps1.
        Original category: endpoints/remediation/winget/communication/Discord
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-WingetDiscord -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/communication/Discord/Test-WingetDiscord.ps1
        Category: endpoints/remediation/winget/communication/Discord
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/communication/Discord/Test-WingetDiscord.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/communication/Discord/Test-WingetDiscord.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/communication/Discord/Test-WingetDiscord.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-WingetEdgeWebView2 {
    <#
    .SYNOPSIS
        Winget update detection script for Edge WebView2 (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/runtimes/EdgeWebView2/Test-WingetEdgeWebView2.ps1.
        Original category: endpoints/remediation/winget/runtimes/EdgeWebView2
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-WingetEdgeWebView2 -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/runtimes/EdgeWebView2/Test-WingetEdgeWebView2.ps1
        Category: endpoints/remediation/winget/runtimes/EdgeWebView2
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/runtimes/EdgeWebView2/Test-WingetEdgeWebView2.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/runtimes/EdgeWebView2/Test-WingetEdgeWebView2.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/runtimes/EdgeWebView2/Test-WingetEdgeWebView2.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-WingetFirefox {
    <#
    .SYNOPSIS
        Winget update detection script for Mozilla Firefox (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/browsers/Firefox/Test-WingetFirefox.ps1.
        Original category: endpoints/remediation/winget/browsers/Firefox
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-WingetFirefox -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/browsers/Firefox/Test-WingetFirefox.ps1
        Category: endpoints/remediation/winget/browsers/Firefox
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/browsers/Firefox/Test-WingetFirefox.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/browsers/Firefox/Test-WingetFirefox.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/browsers/Firefox/Test-WingetFirefox.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-WingetGit {
    <#
    .SYNOPSIS
        Winget update detection for Git (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/development/Git/Test-WingetGit.ps1.
        Original category: endpoints/remediation/winget/development/Git
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-WingetGit -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/development/Git/Test-WingetGit.ps1
        Category: endpoints/remediation/winget/development/Git
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/development/Git/Test-WingetGit.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/development/Git/Test-WingetGit.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/development/Git/Test-WingetGit.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-WingetGoogleChrome {
    <#
    .SYNOPSIS
        Winget update detection script for Google Chrome (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/browsers/GoogleChrome/Test-WingetGoogleChrome.ps1.
        Original category: endpoints/remediation/winget/browsers/GoogleChrome
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-WingetGoogleChrome -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/browsers/GoogleChrome/Test-WingetGoogleChrome.ps1
        Category: endpoints/remediation/winget/browsers/GoogleChrome
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/browsers/GoogleChrome/Test-WingetGoogleChrome.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/browsers/GoogleChrome/Test-WingetGoogleChrome.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/browsers/GoogleChrome/Test-WingetGoogleChrome.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-WingetLenovoDockManager {
    <#
    .SYNOPSIS
        Winget update detection script for Lenovo Dock Manager (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/vendor-specific/LenovoDockManager/Test-WingetLenovoDockManager.ps1.
        Original category: endpoints/remediation/winget/vendor-specific/LenovoDockManager
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-WingetLenovoDockManager -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/vendor-specific/LenovoDockManager/Test-WingetLenovoDockManager.ps1
        Category: endpoints/remediation/winget/vendor-specific/LenovoDockManager
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/vendor-specific/LenovoDockManager/Test-WingetLenovoDockManager.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/vendor-specific/LenovoDockManager/Test-WingetLenovoDockManager.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/vendor-specific/LenovoDockManager/Test-WingetLenovoDockManager.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-WingetLenovoSystemUpdate {
    <#
    .SYNOPSIS
        Winget update detection script for Lenovo System Update (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/vendor-specific/LenovoSystemUpdate/Test-WingetLenovoSystemUpdate.ps1.
        Original category: endpoints/remediation/winget/vendor-specific/LenovoSystemUpdate
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-WingetLenovoSystemUpdate -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/vendor-specific/LenovoSystemUpdate/Test-WingetLenovoSystemUpdate.ps1
        Category: endpoints/remediation/winget/vendor-specific/LenovoSystemUpdate
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/vendor-specific/LenovoSystemUpdate/Test-WingetLenovoSystemUpdate.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/vendor-specific/LenovoSystemUpdate/Test-WingetLenovoSystemUpdate.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/vendor-specific/LenovoSystemUpdate/Test-WingetLenovoSystemUpdate.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-WingetMicrosoftTeams {
    <#
    .SYNOPSIS
        Enhanced winget update detection script for Intune Proactive Remediations (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/productivity/MicrosoftTeams/Test-WingetMicrosoftTeams.ps1.
        Original category: endpoints/remediation/winget/productivity/MicrosoftTeams
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-WingetMicrosoftTeams -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/productivity/MicrosoftTeams/Test-WingetMicrosoftTeams.ps1
        Category: endpoints/remediation/winget/productivity/MicrosoftTeams
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/productivity/MicrosoftTeams/Test-WingetMicrosoftTeams.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/productivity/MicrosoftTeams/Test-WingetMicrosoftTeams.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/productivity/MicrosoftTeams/Test-WingetMicrosoftTeams.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-WingetNotepadPlusPlus {
    <#
    .SYNOPSIS
        Winget update detection script for NotePad++ (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/productivity/NotepadPlusPlus/Test-WingetNotepadPlusPlus.ps1.
        Original category: endpoints/remediation/winget/productivity/NotepadPlusPlus
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-WingetNotepadPlusPlus -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/productivity/NotepadPlusPlus/Test-WingetNotepadPlusPlus.ps1
        Category: endpoints/remediation/winget/productivity/NotepadPlusPlus
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/productivity/NotepadPlusPlus/Test-WingetNotepadPlusPlus.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/productivity/NotepadPlusPlus/Test-WingetNotepadPlusPlus.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/productivity/NotepadPlusPlus/Test-WingetNotepadPlusPlus.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-WingetOBS {
    <#
    .SYNOPSIS
        Winget update detection script for OBS Studio (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/media/OBS/Test-WingetOBS.ps1.
        Original category: endpoints/remediation/winget/media/OBS
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-WingetOBS -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/media/OBS/Test-WingetOBS.ps1
        Category: endpoints/remediation/winget/media/OBS
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/media/OBS/Test-WingetOBS.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/media/OBS/Test-WingetOBS.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/media/OBS/Test-WingetOBS.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-WingetOhMyPosh {
    <#
    .SYNOPSIS
        Winget update detection script for Oh My Posh (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/development/OhMyPosh/Test-WingetOhMyPosh.ps1.
        Original category: endpoints/remediation/winget/development/OhMyPosh
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-WingetOhMyPosh -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/development/OhMyPosh/Test-WingetOhMyPosh.ps1
        Category: endpoints/remediation/winget/development/OhMyPosh
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/development/OhMyPosh/Test-WingetOhMyPosh.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/development/OhMyPosh/Test-WingetOhMyPosh.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/development/OhMyPosh/Test-WingetOhMyPosh.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-WingetPowerShell7 {
    <#
    .SYNOPSIS
        Winget update detection for PowerShell 7 (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/development/PowerShell7/Test-WingetPowerShell7.ps1.
        Original category: endpoints/remediation/winget/development/PowerShell7
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-WingetPowerShell7 -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/development/PowerShell7/Test-WingetPowerShell7.ps1
        Category: endpoints/remediation/winget/development/PowerShell7
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/development/PowerShell7/Test-WingetPowerShell7.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/development/PowerShell7/Test-WingetPowerShell7.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/development/PowerShell7/Test-WingetPowerShell7.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-WingetRemoteDesktop {
    <#
    .SYNOPSIS
        Winget update detection script for Microsoft Remote Desktop (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/productivity/RemoteDesktop/Test-WingetRemoteDesktop.ps1.
        Original category: endpoints/remediation/winget/productivity/RemoteDesktop
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-WingetRemoteDesktop -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/productivity/RemoteDesktop/Test-WingetRemoteDesktop.ps1
        Category: endpoints/remediation/winget/productivity/RemoteDesktop
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/productivity/RemoteDesktop/Test-WingetRemoteDesktop.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/productivity/RemoteDesktop/Test-WingetRemoteDesktop.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/productivity/RemoteDesktop/Test-WingetRemoteDesktop.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-WingetSevenZip {
    <#
    .SYNOPSIS
        Enhanced winget update detection script for Intune Proactive Remediations (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/utilities/SevenZip/Test-WingetSevenZip.ps1.
        Original category: endpoints/remediation/winget/utilities/SevenZip
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-WingetSevenZip -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/utilities/SevenZip/Test-WingetSevenZip.ps1
        Category: endpoints/remediation/winget/utilities/SevenZip
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/utilities/SevenZip/Test-WingetSevenZip.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/utilities/SevenZip/Test-WingetSevenZip.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/utilities/SevenZip/Test-WingetSevenZip.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-WingetSlack {
    <#
    .SYNOPSIS
        Enhanced winget update detection script for Intune Proactive Remediations (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/communication/Slack/Test-WingetSlack.ps1.
        Original category: endpoints/remediation/winget/communication/Slack
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-WingetSlack -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/communication/Slack/Test-WingetSlack.ps1
        Category: endpoints/remediation/winget/communication/Slack
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/communication/Slack/Test-WingetSlack.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/communication/Slack/Test-WingetSlack.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/communication/Slack/Test-WingetSlack.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-WingetSSMS {
    <#
    .SYNOPSIS
        Winget update detection script for SQL Server Management Studio (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/development/SSMS/Test-WingetSSMS.ps1.
        Original category: endpoints/remediation/winget/development/SSMS
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-WingetSSMS -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/development/SSMS/Test-WingetSSMS.ps1
        Category: endpoints/remediation/winget/development/SSMS
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/development/SSMS/Test-WingetSSMS.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/development/SSMS/Test-WingetSSMS.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/development/SSMS/Test-WingetSSMS.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-WingetTeamViewerFull {
    <#
    .SYNOPSIS
        Winget update detection script for TeamViewer Full (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/remote-access/TeamViewerFull/Test-WingetTeamViewerFull.ps1.
        Original category: endpoints/remediation/winget/remote-access/TeamViewerFull
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-WingetTeamViewerFull -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/remote-access/TeamViewerFull/Test-WingetTeamViewerFull.ps1
        Category: endpoints/remediation/winget/remote-access/TeamViewerFull
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/remote-access/TeamViewerFull/Test-WingetTeamViewerFull.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/remote-access/TeamViewerFull/Test-WingetTeamViewerFull.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/remote-access/TeamViewerFull/Test-WingetTeamViewerFull.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-WingetTeamViewerHost {
    <#
    .SYNOPSIS
        Winget update detection script for TeamViewer Host (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/remote-access/TeamViewerHost/Test-WingetTeamViewerHost.ps1.
        Original category: endpoints/remediation/winget/remote-access/TeamViewerHost
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-WingetTeamViewerHost -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/remote-access/TeamViewerHost/Test-WingetTeamViewerHost.ps1
        Category: endpoints/remediation/winget/remote-access/TeamViewerHost
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/remote-access/TeamViewerHost/Test-WingetTeamViewerHost.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/remote-access/TeamViewerHost/Test-WingetTeamViewerHost.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/remote-access/TeamViewerHost/Test-WingetTeamViewerHost.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-WingetVisualStudioCode {
    <#
    .SYNOPSIS
        Winget update detection script for Visual Studio Code (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/development/VisualStudioCode/Test-WingetVisualStudioCode.ps1.
        Original category: endpoints/remediation/winget/development/VisualStudioCode
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-WingetVisualStudioCode -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/development/VisualStudioCode/Test-WingetVisualStudioCode.ps1
        Category: endpoints/remediation/winget/development/VisualStudioCode
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/development/VisualStudioCode/Test-WingetVisualStudioCode.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/development/VisualStudioCode/Test-WingetVisualStudioCode.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/development/VisualStudioCode/Test-WingetVisualStudioCode.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-WingetVisualStudioPro2019 {
    <#
    .SYNOPSIS
        Winget update detection script for Visual Studio Professional 2019 (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/development/VisualStudioPro2019/Test-WingetVisualStudioPro2019.ps1.
        Original category: endpoints/remediation/winget/development/VisualStudioPro2019
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-WingetVisualStudioPro2019 -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/development/VisualStudioPro2019/Test-WingetVisualStudioPro2019.ps1
        Category: endpoints/remediation/winget/development/VisualStudioPro2019
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/development/VisualStudioPro2019/Test-WingetVisualStudioPro2019.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/development/VisualStudioPro2019/Test-WingetVisualStudioPro2019.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/development/VisualStudioPro2019/Test-WingetVisualStudioPro2019.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-WingetVisualStudioPro2022 {
    <#
    .SYNOPSIS
        Winget update detection script for Visual Studio Professional 2022 (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/development/VisualStudioPro2022/Test-WingetVisualStudioPro2022.ps1.
        Original category: endpoints/remediation/winget/development/VisualStudioPro2022
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-WingetVisualStudioPro2022 -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/development/VisualStudioPro2022/Test-WingetVisualStudioPro2022.ps1
        Category: endpoints/remediation/winget/development/VisualStudioPro2022
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/development/VisualStudioPro2022/Test-WingetVisualStudioPro2022.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/development/VisualStudioPro2022/Test-WingetVisualStudioPro2022.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/development/VisualStudioPro2022/Test-WingetVisualStudioPro2022.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-WingetVLCMediaPlayer {
    <#
    .SYNOPSIS
        Winget update detection script for VLC Media Player (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/media/VLCMediaPlayer/Test-WingetVLCMediaPlayer.ps1.
        Original category: endpoints/remediation/winget/media/VLCMediaPlayer
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-WingetVLCMediaPlayer -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/media/VLCMediaPlayer/Test-WingetVLCMediaPlayer.ps1
        Category: endpoints/remediation/winget/media/VLCMediaPlayer
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/media/VLCMediaPlayer/Test-WingetVLCMediaPlayer.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/media/VLCMediaPlayer/Test-WingetVLCMediaPlayer.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/media/VLCMediaPlayer/Test-WingetVLCMediaPlayer.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-WingetWinSCP {
    <#
    .SYNOPSIS
        Winget update detection script for WinSCP (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/remote-access/WinSCP/Test-WingetWinSCP.ps1.
        Original category: endpoints/remediation/winget/remote-access/WinSCP
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-WingetWinSCP -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/remote-access/WinSCP/Test-WingetWinSCP.ps1
        Category: endpoints/remediation/winget/remote-access/WinSCP
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/remote-access/WinSCP/Test-WingetWinSCP.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/remote-access/WinSCP/Test-WingetWinSCP.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/remote-access/WinSCP/Test-WingetWinSCP.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-WingetZoom {
    <#
    .SYNOPSIS
        Winget update detection script for Zoomies (V3).
    .DESCRIPTION
        Proxies to scripts/endpoints/remediation/winget/media/Zoom/Test-WingetZoom.ps1.
        Original category: endpoints/remediation/winget/media/Zoom
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-WingetZoom -Verbose
    .NOTES
        Backing script: scripts/endpoints/remediation/winget/media/Zoom/Test-WingetZoom.ps1
        Category: endpoints/remediation/winget/media/Zoom
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/remediation/winget/media/Zoom/Test-WingetZoom.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/remediation/winget/media/Zoom/Test-WingetZoom.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/remediation/winget/media/Zoom/Test-WingetZoom.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Test-WUServer {
    <#
    .SYNOPSIS
        Detect the WUServer Windows Update policy
    .DESCRIPTION
        Proxies to scripts/endpoints/devices/autopatch/V1/WUServer/Detect.ps1.
        Original category: endpoints/devices/autopatch/V1/WUServer
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Test-WUServer -Verbose
    .NOTES
        Backing script: scripts/endpoints/devices/autopatch/V1/WUServer/Detect.ps1
        Category: endpoints/devices/autopatch/V1/WUServer
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/devices/autopatch/V1/WUServer/Detect.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/devices/autopatch/V1/WUServer/Detect.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/devices/autopatch/V1/WUServer/Detect.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Unblock-AMDDriver {
    <#
    .SYNOPSIS
        Remove the AMD Radeon driver block
    .DESCRIPTION
        Proxies to scripts/endpoints/devices/drivers/UnblockAMDDriver.ps1.
        Original category: endpoints/devices/drivers
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Unblock-AMDDriver -Verbose
    .NOTES
        Backing script: scripts/endpoints/devices/drivers/UnblockAMDDriver.ps1
        Category: endpoints/devices/drivers
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/endpoints/devices/drivers/UnblockAMDDriver.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'endpoints/devices/drivers/UnblockAMDDriver.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/endpoints/devices/drivers/UnblockAMDDriver.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Update-AllAppsWinget {
    <#
    .SYNOPSIS
        Updates all installed applications using winget in system context.
    .DESCRIPTION
        Proxies to scripts/utilities/Update-AllAppsWinget.ps1.
        Original category: utilities
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Update-AllAppsWinget -Verbose
    .NOTES
        Backing script: scripts/utilities/Update-AllAppsWinget.ps1
        Category: utilities
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/utilities/Update-AllAppsWinget.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [String]$LogPath,
        [Parameter()]
        [Int32]$MaxRetries,
        [Parameter()]
        [switch]$SkipDependencyCheck,
        [Parameter()]
        [String]$UpdateSource,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'utilities/Update-AllAppsWinget.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/utilities/Update-AllAppsWinget.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Update-DotNetRuntimes {
    <#
    .SYNOPSIS
        Enterprise .NET runtime maintenance tool with interactive menu and full automation. v2.5 - Production-ready with comp...
    .DESCRIPTION
        Proxies to scripts/utilities/Update-DotNetRuntimes.ps1.
        Original category: utilities
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Update-DotNetRuntimes -Verbose
    .NOTES
        Backing script: scripts/utilities/Update-DotNetRuntimes.ps1
        Category: utilities
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/utilities/Update-DotNetRuntimes.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [switch]$OneShotCleanup,
        [Parameter()]
        [switch]$NonInteractive,
        [Parameter()]
        [switch]$Approve,
        [Parameter()]
        [switch]$RemoveEol,
        [Parameter()]
        [switch]$CleanupLowerPatches,
        [Parameter()]
        [switch]$AutoInstallUninstallTool,
        [Parameter()]
        [switch]$ForceFileCleanup,
        [Parameter()]
        [switch]$IncludeHostingBundle,
        [Parameter()]
        [String]$DependencyCheck,
        [Parameter()]
        [switch]$ForceEolRemoval,
        [Parameter()]
        [String[]]$ProtectChannels,
        [Parameter()]
        [switch]$PreferOldestLts,
        [Parameter()]
        [switch]$PlanOnly,
        [Parameter()]
        [switch]$Quiet,
        [Parameter()]
        [switch]$DryRun,
        [Parameter()]
        [String]$UninstallToolMsiUrl,
        [Parameter()]
        [String]$Arch,
        [Parameter()]
        [switch]$LtsOnly,
        [Parameter()]
        [String[]]$IncludeChannels,
        [Parameter()]
        [Version]$MinVersion,
        [Parameter()]
        [String]$LogPath,
        [Parameter()]
        [String]$ReportPath,
        [Parameter()]
        [String]$JsonSummaryPath,
        [Parameter()]
        [switch]$EnableRollback,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'utilities/Update-DotNetRuntimes.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/utilities/Update-DotNetRuntimes.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Update-M365Apps {
    <#
    .SYNOPSIS
        M365 Apps Update Manager
    .DESCRIPTION
        Proxies to scripts/cloud/azure/avd/Update-M365Apps.ps1.
        Original category: cloud/azure/avd
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Update-M365Apps -Verbose
    .NOTES
        Backing script: scripts/cloud/azure/avd/Update-M365Apps.ps1
        Category: cloud/azure/avd
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/cloud/azure/avd/Update-M365Apps.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [String]$InstallPath,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'cloud/azure/avd/Update-M365Apps.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/cloud/azure/avd/Update-M365Apps.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

function Update-M365AppsOfficeApps {
    <#
    .SYNOPSIS
        M365 Apps Update Manager
    .DESCRIPTION
        Proxies to scripts/collaboration/microsoft365/office-apps/Update-M365Apps.ps1.
        Original category: collaboration/microsoft365/office-apps
    .PARAMETER WhatIf
        Shows what would happen without executing.
    .EXAMPLE
        Update-M365AppsOfficeApps -Verbose
    .NOTES
        Backing script: scripts/collaboration/microsoft365/office-apps/Update-M365Apps.ps1
        Category: collaboration/microsoft365/office-apps
    #>
    [System.Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseSingularNouns', '', Justification = 'Wrapper name mirrors backing script filename scripts/collaboration/microsoft365/office-apps/Update-M365Apps.ps1')]
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'Medium')]
    param(
        [Parameter()]
        [String]$Message,
        [Parameter()]
        [String]$Color,
        [Parameter()]
        [switch]$NoNewLine,
        [Parameter()]
        [String]$ChannelGuid,
        [Parameter()]
        [String]$InstalledVersion,
        [Parameter()]
        [String]$LatestVersion,
        [Parameter()]
        [Boolean]$IsUpdate,
        [Parameter()]
        [String]$DefaultChoice,
        [Parameter()]
        [String]$ChannelName,
        [Parameter(ValueFromRemainingArguments)]
        [object[]]$RemainingArgs
    )
    begin {
        $boundParams = @{}
        foreach ($k in $PSBoundParameters.Keys) {
            if ($k -ne 'RemainingArgs') { $boundParams[$k] = $PSBoundParameters[$k] }
        }
    }
    process {
        $target = Join-Path $script:ScriptsRoot 'collaboration/microsoft365/office-apps/Update-M365Apps.ps1'
        if (-not (Test-Path -LiteralPath $target)) {
            $alt = Join-Path $script:RepoRootResolved "scripts/collaboration/microsoft365/office-apps/Update-M365Apps.ps1"
            if (Test-Path -LiteralPath $alt) { $target = $alt }
        }
        if ($PSCmdlet.ShouldProcess($target, 'Invoke script')) {
            if ($RemainingArgs -and $RemainingArgs.Count -gt 0) {
                & $target @boundParams @RemainingArgs
            }
            else {
                & $target @boundParams
            }
        }
    }
}

# END GENERATED
#endregion

$script:FunctionsToExport = @(
    'Add-LenovoFriendlyModelNames',
    'Backup-GroupPolicies',
    'Backup-IISConfiguration',
    'Block-AMDDriver',
    'Compare-ConfigurationDrift',
    'Export-IntuneConfiguration',
    'Export-WingetPackageList',
    'Find-GPOConflicts',
    'Find-InactiveADComputers',
    'Find-PolicyConflicts',
    'Find-StaleDevices',
    'Get-ADHealthCheck',
    'Get-ADUserAudit',
    'Get-AntivirusStatus',
    'Get-AppInstallationStatus',
    'Get-AppInstallErrorReport',
    'Get-AutopilotDeploymentReport',
    'Get-AWSResourceInventory',
    'Get-AzureADGuestAudit',
    'Get-AzureADLicenseReport',
    'Get-AzureResourceHealth',
    'Get-BackupStatus',
    'Get-BatteryHealth',
    'Get-BitLockerStatus',
    'Get-BUScript',
    'Get-DefenderO365ThreatReport',
    'Get-DeviceComplianceReport',
    'Get-DeviceGroupMembership',
    'Get-DeviceHealthScore',
    'Get-DiskReport',
    'Get-DistributionListAudit',
    'Get-DockerHealthCheck',
    'Get-EventLogReport',
    'Get-ExchangeServerHealth',
    'Get-ExpiredCertificates',
    'Get-FailedLoginReport',
    'Get-FirewallRulesReport',
    'Get-GPOReport',
    'Get-HyperVHealth',
    'Get-IISHealthCheck',
    'Get-IISLogAnalyzer',
    'Get-InactiveUserReport',
    'Get-IntuneDevicePrimaryUsers',
    'Get-KubernetesHealthCheck',
    'Get-LargeFilesReport',
    'Get-LocalAdminAudit',
    'Get-M365UserInfo',
    'Get-MailboxHealth',
    'Get-MailFlowAnalysis',
    'Get-MDEDeviceHealth',
    'Get-MySQLHealth',
    'Get-NetworkConfiguration',
    'Get-OneDriveUsageReport',
    'Get-OpenPortScan',
    'Get-PerformanceReport',
    'Get-PerformanceTrends',
    'Get-PolicyAssignmentReport',
    'Get-PostgreSQLHealth',
    'Get-PowerPlatformGovernance',
    'Get-PrintServerHealth',
    'Get-SecurityBaseline',
    'Get-SecurityEventAudit',
    'Get-ServiceAccountAudit',
    'Get-SharedMailboxReport',
    'Get-SoftwareInventory',
    'Get-SoftwareLicenseCompliance',
    'Get-SQLServerHealth',
    'Get-SystemHealthCheck',
    'Get-SystemResourceTrends',
    'Get-TeamsReport',
    'Get-USBDeviceAudit',
    'Get-UserAccessReport',
    'Get-UserDeviceAffinity',
    'Get-UserLockoutReport',
    'Get-UserMailboxPermissions',
    'Get-UserMailRules',
    'Get-VMBackupCompliance',
    'Get-VMSecurityConfig',
    'Get-VMwareHealth',
    'Get-WindowsUpdateCompliance',
    'Get-WingetUpdateCompliance',
    'Install-TeamsAVD',
    'Install-TeamsAVDTests',
    'Invoke-AdobeRum',
    'Invoke-AnalyzeBuildPerformance',
    'Invoke-APDetection',
    'Invoke-APRemediation',
    'Invoke-BUScript',
    'Invoke-DeviceBulkActions',
    'Invoke-DisableWindowsUpdateAccess',
    'Invoke-DoNotConnectToWindowsUpdateInternetLocations',
    'Invoke-Drivers',
    'Invoke-FixBitLockerKeyBackup',
    'Invoke-GenerateWingetScripts',
    'Invoke-ManageFirewallRules',
    'Invoke-ManageQuarantinedEmails',
    'Invoke-ManageQuarantinedEmailsTests',
    'Invoke-ManageRestorePoints',
    'Invoke-MonitorAzureAPIManagement',
    'Invoke-MonitorAzureDevOpsPipelines',
    'Invoke-MonitorAzureKeyVaults',
    'Invoke-MonitorAzureResources',
    'Invoke-MonitorGitHubActions',
    'Invoke-MonitorGitLabCI',
    'Invoke-MonitorMongoDBHealth',
    'Invoke-MonitorServerHealth',
    'Invoke-NoAutoUpdate',
    'Invoke-RemediationCheckApplicationCrashes',
    'Invoke-RemediationCheckBatteryHealth',
    'Invoke-RemediationCheckBootPerformance',
    'Invoke-RemediationCheckDefenderHealthStatus',
    'Invoke-RemediationCheckDeviceHealthScore',
    'Invoke-RemediationCheckDeviceUptime',
    'Invoke-RemediationCheckDiskHealth',
    'Invoke-RemediationCheckHardwareErrors',
    'Invoke-RemediationCheckLocalAdminAccounts',
    'Invoke-RemediationCheckMemoryDiagnostics',
    'Invoke-RemediationCheckMicrosoftStoreAppsHealth',
    'Invoke-RemediationCheckOutdatedCriticalApps',
    'Invoke-RemediationCheckOutdatedCriticalAppsPriorityOnly',
    'Invoke-RemediationCheckPageFileConfiguration',
    'Invoke-RemediationCheckSecurityBaseline',
    'Invoke-RemediationCheckServiceFailures',
    'Invoke-RemediationCheckSharedFolders',
    'Invoke-RemediationCheckSystemEventErrors',
    'Invoke-RemediationCheckSystemStabilityIndex',
    'Invoke-RemediationCheckTPMStatus',
    'Invoke-RemediationCheckUnexpectedReboots',
    'Invoke-RemediationCheckWindowsActivationGracePeriod',
    'Invoke-RemediationFixBitLockerNotEscrowedKeys',
    'Invoke-RemediationFixBrokenShortcuts',
    'Invoke-RemediationFixCertificateExpiry',
    'Invoke-RemediationFixCredentialManager',
    'Invoke-RemediationFixDiskSpace',
    'Invoke-RemediationFixDNSCache',
    'Invoke-RemediationFixEdgeCacheSize',
    'Invoke-RemediationFixEventLogSize',
    'Invoke-RemediationFixNetworkAdapterPowerManagement',
    'Invoke-RemediationFixOneDriveKnownFolderMove',
    'Invoke-RemediationFixOutdatedDrivers',
    'Invoke-RemediationFixPowerShellExecutionPolicy',
    'Invoke-RemediationFixPrintSpooler',
    'Invoke-RemediationFixSMBv1Protocol',
    'Invoke-RemediationFixStaleProfiles',
    'Invoke-RemediationFixStartMenuLayout',
    'Invoke-RemediationFixSystemFileCorruption',
    'Invoke-RemediationFixTaskSchedulerCorruption',
    'Invoke-RemediationFixTeamsCache',
    'Invoke-RemediationFixTempFiles',
    'Invoke-RemediationFixTimeSync',
    'Invoke-RemediationFixWindowsLicenseActivation',
    'Invoke-RemediationFixWindowsPerformanceRecorder',
    'Invoke-RemediationFixWindowsSearch',
    'Invoke-RemediationFixWindowsStoreLicensing',
    'Invoke-RemediationFixWindowsUpdateRebootPending',
    'Invoke-RemediationFixWindowsUpdateStuck',
    'Invoke-RemediationKeyboardLayout',
    'Invoke-RemediationLanguagePackAudit',
    'Invoke-RemediationRegionLanguageSettings',
    'Invoke-Sccm',
    'Invoke-SecurityComplianceScan',
    'Invoke-TemplatesWinget',
    'Invoke-Uptime',
    'Invoke-UseWUServer',
    'Invoke-V1LegacyWinget',
    'Invoke-V2ForceCloseWinget',
    'Invoke-V2StandardWinget',
    'Invoke-V3',
    'Invoke-V3ForceCloseWinget',
    'Invoke-V3MaintenanceWindowWinget',
    'Invoke-V3StandardWinget',
    'Invoke-V4',
    'Invoke-V5',
    'Invoke-Winget1Password',
    'Invoke-WingetAdobeReader32bit',
    'Invoke-WingetAdobeReader64bit',
    'Invoke-WingetAzureCLI',
    'Invoke-WingetCpp2008Redist',
    'Invoke-WingetCpp2010Redist',
    'Invoke-WingetCpp2012Redist',
    'Invoke-WingetCpp2013RedistX64',
    'Invoke-WingetCpp2013RedistX86',
    'Invoke-WingetCpp20152019RedistX64',
    'Invoke-WingetCpp20152019RedistX86',
    'Invoke-WingetDiscord',
    'Invoke-WingetEdgeWebView2',
    'Invoke-WingetFirefox',
    'Invoke-WingetGit',
    'Invoke-WingetGoogleChrome',
    'Invoke-WingetLenovoDockManager',
    'Invoke-WingetLenovoSystemUpdate',
    'Invoke-WingetMicrosoftTeams',
    'Invoke-WingetNotepadPlusPlus',
    'Invoke-WingetOBS',
    'Invoke-WingetOhMyPosh',
    'Invoke-WingetPowerShell7MaintenanceWindow',
    'Invoke-WingetRemoteDesktop',
    'Invoke-WingetSevenZip',
    'Invoke-WingetSlack',
    'Invoke-WingetSSMS',
    'Invoke-WingetTeamViewerFull',
    'Invoke-WingetTeamViewerFullForceClose',
    'Invoke-WingetTeamViewerHost',
    'Invoke-WingetVisualStudioCode',
    'Invoke-WingetVisualStudioPro2019',
    'Invoke-WingetVisualStudioPro2022',
    'Invoke-WingetVLCMediaPlayer',
    'Invoke-WingetWinSCP',
    'Invoke-WingetZoom',
    'Invoke-WUServer',
    'New-AzureComputeGalleryImage',
    'New-BulkWingetUpdater',
    'New-IntuneWinPackage',
    'New-WeeklyRebootSchedule',
    'New-Win32AppTemplate',
    'New-WingetRemediationScript',
    'New-WingetSourceConfig',
    'Optimize-AzureVMs',
    'Optimize-DockerCleanup',
    'Optimize-IISConfiguration',
    'Optimize-ServerStorage',
    'Optimize-WindowsServices',
    'Optimize-WsusServer',
    'Register-BUCompleter',
    'Remove-SysprepBlockers',
    'Remove-USlangpack',
    'Remove-USLanguagePack',
    'Reset-NetworkStack',
    'Reset-WindowsUpdate',
    'Set-EnglishUKRegion',
    'Set-MailboxRegionalSettings',
    'Set-OneDriveRegionalSettings',
    'Set-OrganizationDefaults',
    'Set-PowerPlatformRegionalSettings',
    'Set-SiteRegionalSettings',
    'Set-TeamsRegionalSettings',
    'Set-UserLanguageSettings',
    'Sync-UserGroupToPrimaryDeviceGroup',
    'Test-AdobeRum',
    'Test-APIHealth',
    'Test-BackupIntegrity',
    'Test-BicepTemplates',
    'Test-BitLockerKeyBackup',
    'Test-CertificateExpiration',
    'Test-CISBenchmark',
    'Test-DisableWindowsUpdateAccess',
    'Test-DoNotConnectToWindowsUpdateInternetLocations',
    'Test-Drivers',
    'Test-IntuneConnectivity',
    'Test-IntuneConnectivityTests',
    'Test-NetworkConnectivity',
    'Test-NetworkDiagnostics',
    'Test-NoAutoUpdate',
    'Test-RemediationCheckApplicationCrashes',
    'Test-RemediationCheckBatteryHealth',
    'Test-RemediationCheckBootPerformance',
    'Test-RemediationCheckDefenderHealthStatus',
    'Test-RemediationCheckDeviceHealthScore',
    'Test-RemediationCheckDeviceUptime',
    'Test-RemediationCheckDiskHealth',
    'Test-RemediationCheckHardwareErrors',
    'Test-RemediationCheckLocalAdminAccounts',
    'Test-RemediationCheckMemoryDiagnostics',
    'Test-RemediationCheckMicrosoftStoreAppsHealth',
    'Test-RemediationCheckOutdatedCriticalApps',
    'Test-RemediationCheckPageFileConfiguration',
    'Test-RemediationCheckSecurityBaseline',
    'Test-RemediationCheckServiceFailures',
    'Test-RemediationCheckSharedFolders',
    'Test-RemediationCheckSystemEventErrors',
    'Test-RemediationCheckSystemStabilityIndex',
    'Test-RemediationCheckTPMStatus',
    'Test-RemediationCheckUnexpectedReboots',
    'Test-RemediationCheckWindowsActivationGracePeriod',
    'Test-RemediationFixBitLockerNotEscrowedKeys',
    'Test-RemediationFixBrokenShortcuts',
    'Test-RemediationFixCertificateExpiry',
    'Test-RemediationFixCredentialManager',
    'Test-RemediationFixDiskSpace',
    'Test-RemediationFixDNSCache',
    'Test-RemediationFixEdgeCacheSize',
    'Test-RemediationFixEventLogSize',
    'Test-RemediationFixNetworkAdapterPowerManagement',
    'Test-RemediationFixOneDriveKnownFolderMove',
    'Test-RemediationFixOutdatedDrivers',
    'Test-RemediationFixPowerShellExecutionPolicy',
    'Test-RemediationFixPrintSpooler',
    'Test-RemediationFixSMBv1Protocol',
    'Test-RemediationFixStaleProfiles',
    'Test-RemediationFixStartMenuLayout',
    'Test-RemediationFixSystemFileCorruption',
    'Test-RemediationFixTaskSchedulerCorruption',
    'Test-RemediationFixTeamsCache',
    'Test-RemediationFixTempFiles',
    'Test-RemediationFixTimeSync',
    'Test-RemediationFixWindowsLicenseActivation',
    'Test-RemediationFixWindowsPerformanceRecorder',
    'Test-RemediationFixWindowsSearch',
    'Test-RemediationFixWindowsStoreLicensing',
    'Test-RemediationFixWindowsUpdateRebootPending',
    'Test-RemediationFixWindowsUpdateStuck',
    'Test-RemediationKeyboardLayout',
    'Test-RemediationLanguagePackAudit',
    'Test-RemediationRegionLanguageSettings',
    'Test-Sccm',
    'Test-SecurityFeatures',
    'Test-ServerConnectivity',
    'Test-ServerHardening',
    'Test-SystemIntegrity',
    'Test-TemplatesWinget',
    'Test-TerraformConfiguration',
    'Test-Uptime',
    'Test-UseWUServer',
    'Test-V1LegacyWinget',
    'Test-V2Winget',
    'Test-V3',
    'Test-V3Winget',
    'Test-V4',
    'Test-V5',
    'Test-Winget1Password',
    'Test-WingetAdobeReader32bit',
    'Test-WingetAdobeReader64bit',
    'Test-WingetAzureCLI',
    'Test-WingetCpp2008Redist',
    'Test-WingetCpp2010Redist',
    'Test-WingetCpp2012Redist',
    'Test-WingetCpp2013RedistX64',
    'Test-WingetCpp2013RedistX86',
    'Test-WingetCpp20152019RedistX64',
    'Test-WingetCpp20152019RedistX86',
    'Test-WingetDiscord',
    'Test-WingetEdgeWebView2',
    'Test-WingetFirefox',
    'Test-WingetGit',
    'Test-WingetGoogleChrome',
    'Test-WingetLenovoDockManager',
    'Test-WingetLenovoSystemUpdate',
    'Test-WingetMicrosoftTeams',
    'Test-WingetNotepadPlusPlus',
    'Test-WingetOBS',
    'Test-WingetOhMyPosh',
    'Test-WingetPowerShell7',
    'Test-WingetRemoteDesktop',
    'Test-WingetSevenZip',
    'Test-WingetSlack',
    'Test-WingetSSMS',
    'Test-WingetTeamViewerFull',
    'Test-WingetTeamViewerHost',
    'Test-WingetVisualStudioCode',
    'Test-WingetVisualStudioPro2019',
    'Test-WingetVisualStudioPro2022',
    'Test-WingetVLCMediaPlayer',
    'Test-WingetWinSCP',
    'Test-WingetZoom',
    'Test-WUServer',
    'Unblock-AMDDriver',
    'Update-AllAppsWinget',
    'Update-DotNetRuntimes',
    'Update-M365Apps',
    'Update-M365AppsOfficeApps'
)
Export-ModuleMember -Function $script:FunctionsToExport
